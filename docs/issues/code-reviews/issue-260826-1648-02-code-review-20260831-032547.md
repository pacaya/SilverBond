# Code review — ISSUE-260826-1648-02 (2026-08-31T03:25:47Z)

Scope: issue diff vs c03d72dc0d46e917add19d93c31ab15e66bdaf02 · Reviewers: Claude + Codex (cross-verified, adjudicated inline)

Reviewers raised 13 findings (Claude 10, Codex 3). After cross-verification and adjudication: 5 kept
(one of them a promoted in-diff duplication smell), 1 merged as a duplicate, 1 deferred, 6 dismissed.

### M1. Brief's mandated confirmation of the structured-vs-free-text asymmetry is absent from the change (FIXED)
> **Fix:** Three-line comment added at the outcome padding gate (`src/model.rs:3010-3012`) recording that structured outcomes are compared untrimmed, that a mismatch yields an unsuccessful result, and that the caller returns before `select_next_decision`. No `src/runtime.rs` change, no follow-up record filed. Both reviewers traced every clause to the runtime and confirmed it does not soften the unreachability framing.

**Severity:** MEDIUM · **File:** `src/model.rs:2987`, `src/runtime.rs:4374-4396` · **Source:** Claude (CLAUDE-1) · **Cross-reference:** Codex AGREE

The Agent Brief's **Out of scope** requires the change to confirm, in writing, that the
structured-vs-free-text asymmetry in outcome selection becomes harmless once no valid document can
declare a padded outcome — "confirm that this is so and say so in the change". Nothing in the diff
states it.

Both reviewers traced the asymmetry and it is real: the structured arm (`src/runtime.rs:4374-4381`)
compares the model's JSON `outcome` field against the declared outcomes with neither side trimmed,
while the free-text arm (`src/runtime.rs:4389-4396`) trims the response before matching.

**Adjudication of the second half.** Claude further argued the brief's "if it turns out to survive
the fix, file it rather than widening this issue" branch applies, and recommended filing a follow-up
record. Verified against the source and rejected: the brief separately bans **"trimming anywhere in
the runtime"**, so the asymmetric code *must* survive this change by construction — the brief's
condition is about the asymmetry becoming **harmless**, not about the code being removed. It is
harmless, and both reviewers' traces agree on why: a structured mismatch yields
`DecideOutcomeSelection::Failed`, which sets `result.success = false` (`src/runtime.rs:4348`), and
the caller returns at `src/runtime.rs:4554` (`if !task_result.result.success`) before
`select_next_decision` is ever reached. No follow-up record is warranted; only the missing
confirmation is.

**Fix:** Add the confirmation the brief asks for — a concise comment beside the new outcome check
stating why the runtime's untrimmed structured comparison is now harmless (declared outcomes are
unpadded, and a failed selection never reaches branch routing). Do not file a follow-up issue and do
not touch `src/runtime.rs`.

---

### M2. Empty `outcomes` list returns before the new branch-edge whitespace check (FIXED)
> **Fix:** Early `return` removed; `branch_edges` hoisted above the emptiness test; the five outcome-dependent checks moved into an `else` block so an empty outcome list still suppresses exactly what the old `return` suppressed; the branch-edge whitespace scan moved outside so it always runs. Regression `validates_decide_rejects_padded_branch_edge_when_outcomes_empty` added. Both reviewers verified the suppression invariant directly against the restructured control flow. Follow-on consequence filed as L13.

**Severity:** MEDIUM · **File:** `src/model.rs:2958-2968`, `src/model.rs:3055` · **Source:** Codex (CODEX-1) · **Cross-reference:** Claude AGREE (argued LOW)

`validate_decide_node_config` reports the empty-`outcomes` error and then `return`s at
`src/model.rs:2968`, before the branch-edge loop. A decide node with `outcomes: []` and an outgoing
branch edge labelled `" yes "` therefore gets the no-outcomes error but never the whitespace-specific
signal, leaving acceptance criterion 3's "branch-edge labels padded is refused independently of how
its outcomes are declared" incomplete at that shape.

**Severity note (recorded, not resolved).** Claude agreed the mechanism is real but argued LOW: the
same early return already suppresses three pre-existing decide checks (branch-count
`src/model.rs:3027`, empty-label `src/model.rs:3039`, correspondence `src/model.rs:3010`), the
document is rejected either way, and criterion 3's "independently" clause is aimed at not being
refused as a side effect of *mismatching the other side*. Kept at MEDIUM because the fix is small and
makes criterion 3 hold unconditionally; the disagreement is about priority, not existence.

**Fix:** Let validation continue past the empty-`outcomes` issue (or hoist the branch-label
whitespace scan above that return) so the branch-edge side is checked regardless of the outcome list,
and add a document-boundary regression with `outcomes: []` plus a padded branch edge asserting the
whitespace-worded error.

---

### M3. The two new whitespace guards are verbatim duplicates (FIXED)
> **Fix:** `decide_padded_label_issue()` extracted at `src/model.rs:2917-2935`, called from both sites. Claude diffed the produced strings against round 0 and found them byte-identical, so catalog rows and test substring assertions still hold; it judged the deletion test passed (predicate plus five-field construction genuinely concentrate).

**Severity:** MEDIUM · **File:** `src/model.rs:2987-2998`, `src/model.rs:3056-3066` · **Source:** promoted in-diff duplication smell

The `x != x.trim()` predicate plus its identical five-field `ValidationIssue` push is now written
twice, differing only in the message text. Both cited sites lie inside the issue diff, so the
duplication-promotion rule applies mechanically: this enters the findings track rather than the
advisory smells track.

**Fix:** Extract a small helper (e.g. `padded_label_issue(node, kind, value) -> Option<ValidationIssue>`)
and call it from both sites, or otherwise remove the duplicated push. Gate the extraction on the
deletion test — if it merely relocates the shape without concentrating it, say so and leave both
sites, and this finding is dismissed on that evidence instead.

---

### L1. Preservation test asserts only that error-severity issues are empty (FIXED)
> **Fix:** The test now `assert_eq!`s the complete `(severity, node_id, message)` sequence against a literal, with no severity filter. Claude derived the two expected terminal-node warnings independently from the fixture and `src/model.rs:1916` before reading the literal; Codex separately confirmed the expected values are not routed back through validator logic.

**Severity:** LOW · **File:** `src/model.rs:6202-6217` · **Source:** Both (CLAUDE-4 = CODEX-2, merged) · **Cross-reference:** both AGREE

Acceptance criterion 4 is "validates exactly as it does today, **with the same issue set**".
`validates_unpadded_decide_labels_unchanged_at_document_boundary` filters `result.issues` to
`severity == "error"` and asserts only that subset is empty. The fixture's two `target_*` task nodes
are terminal and do produce warnings (`src/model.rs:1916-1922`), so a regression that added, removed,
or changed a warning on the unpadded path would pass unnoticed.

**Fix:** Assert the complete issue set for this fixture — an independently specified literal list of
the expected `(severity, node_id, message)` tuples including the existing terminal-node warnings —
rather than emptiness of a filtered subset.

---

### L2. Single-sided padding is not exercised at the seam (FIXED)
> **Fix:** Four tests added covering leading-only and trailing-only padding for outcome labels and for branch-edge labels. Each fixture pads only the side under test, so no assertion can be satisfied by the sibling check firing.

**Severity:** LOW · **File:** `src/model.rs:6150`, `src/model.rs:6162`, `src/model.rs:6184` · **Source:** Codex (CODEX-3) · **Cross-reference:** Claude AGREE

All three padded fixtures use `" yes "`, padded at both ends. An implementation that rejected a label
only when it carried **both** leading and trailing whitespace would pass every one of them, while the
acceptance criteria require rejection for leading **or** trailing whitespace on both sides of the
correspondence. The production checks use `!= trim()` and are correct; the document-boundary seam
simply does not pin that.

**Fix:** Add independent leading-only and trailing-only cases for outcome labels and for branch-edge
labels, asserting the whitespace-worded error at the same strict-deserialization seam.

---

### L3. Validation-catalog citation refresh is half-done (deferred)

**Severity:** LOW · **File:** `docs/workflow-schema.md:1691` · **Source:** Claude (CLAUDE-2) · **Cross-reference:** Codex DISAGREE

The diff refreshed the four existing `decide` rows' source ranges and added two new rows, but left
every following row untouched. The `parallel_batch` `itemsBinding` row still cites
`src/model.rs:3051-3060`, which now points inside the new decide branch-edge whitespace block — two
adjacent rows in the same table citing overlapping ranges for unrelated errors.

**Deferred, not fixed.** The brief's **Out of scope** names exactly this: "edits to the model module
shift line references in rows this issue did not touch. That fragility is pre-existing and owned by no
one here; add the new row and leave the rest." Codex dismissed the finding on that basis and it is
the correct reading — the residual inconsistency is the pre-existing fragility the brief declines to
own, not a defect this slice introduced. Recorded so the next owner of that catalog sees it.

---

### L4. New branch-edge whitespace error identifies the edge by label, not `edge.id` (dismissed: sibling uses `edge.id` only because it has no label to show)

**Severity:** LOW · **File:** `src/model.rs:3061` · **Source:** Claude (CLAUDE-3) · **Cross-reference:** Codex DISAGREE

Claimed divergence from "the message conventions of the sibling decide errors". Verified against the
source and dismissed: the sibling at `src/model.rs:3044-3051` fires precisely when the edge has **no
usable label**, so `edge.id` is the only identifying datum available to it. The new error always has a
label and prints it. Claude's residual concern — two edges whose labels differ only in padding render
identically — is real but self-limiting: both edges are reported in the same run, and the fix for
either is the same.

---

### L5. Criterion-1 test cannot distinguish which of the two new checks fired (dismissed: each side is pinned by its own independence test)

**Severity:** LOW · **File:** `src/model.rs:6140`, `src/model.rs:6149` · **Source:** Claude (CLAUDE-5) · **Cross-reference:** Codex DISAGREE

`has_decide_whitespace_validation_error` matches either new error, and both fire in the
identically-padded fixture, so that one test would stay green if either check were deleted. Dismissed
because coverage of each side is pinned separately by
`validates_decide_rejects_padded_outcome_labels_independently` (`src/model.rs:6161`) and
`validates_decide_rejects_padded_branch_edge_labels_independently` (`src/model.rs:6183`) — the latter
already asserts the branch-edge wording specifically. The reporting reviewer conceded overall coverage
survives; the identically-padded test's job is to prove the reproduction is refused, which it does.

---

### L6. Whitespace-only branch-edge label is refused without naming whitespace (dismissed: an all-whitespace label is an empty label under the pre-existing guard)

**Severity:** LOW · **File:** `src/model.rs:3039-3053` · **Source:** Claude (CLAUDE-6) · **Cross-reference:** Codex UNCERTAIN

Behaviour confirmed exactly as described: the pre-existing guard tests
`.is_none_or(|label| label.trim().is_empty())`, so `"   "` trips it and `continue`s before the new
check. Adjudicated by reading the source: that guard **already trims**, by the same `str::trim`
definition the brief adopts, and has always classified an all-whitespace label as a *missing* label
rather than a padded one. Reporting it as "requires a label matching an outcome" is the more specific
diagnosis, the document is refused either way, and no acceptance criterion covers this shape. No
change; the pre-existing classification stands.

---

### L7. Editor trims decide outcomes but not branch-edge labels (deferred)

**Severity:** LOW · **File:** `ui/src/features/editor/EdgeInspector.svelte:80` · **Source:** Claude (CLAUDE-7) · **Cross-reference:** Codex DISAGREE

`DecidePanel.svelte:52` trims outcomes on blur; `EdgeInspector.svelte:80` stores the raw edge-label
input. A user typing `"yes "` saves a document that fails at run start with no editor hint.

**Deferred, not fixed.** The frontend is untouched by this diff and this shape already errored before
the change (as the pre-existing mismatch error), so it is not a regression; the brief's **Key
interfaces** confine the work to the model module and its **Out of scope** keeps non-decide-validation
whitespace handling as-is. Worth a follow-up on the editor, not work for this slice.

---

### L8. Resumed runs are not re-validated (dismissed: the invariant is defined at the document boundary)

**Severity:** LOW · **File:** `src/runtime.rs:1443` · **Source:** Claude (CLAUDE-8) · **Cross-reference:** Codex DISAGREE

`validate_workflow` is called only from `src/api.rs:568` and `src/api.rs:752`; `Runtime::resume_run`
rehydrates a persisted workflow without re-validating, so a run started under the old code with
identically-padded labels can still reach the fallthrough arm. Dismissed: the documented claim is that
the arm is unreachable **from a validated document**, and a checkpoint accepted by the old validator is
not a document validated under the new invariant. Checkpoint migration is outside this brief, which
explicitly leaves routing unchanged. The reporting reviewer rated it 25% worth acting on.

---

### L9. New catalog row's source range includes the block's closing brace (dismissed: cosmetic, no documented range convention)

**Severity:** LOW · **File:** `docs/workflow-schema.md:1685` · **Source:** Claude (CLAUDE-9) · **Cross-reference:** Codex DISAGREE

Sibling rows stop before the closing brace; the new outcome row cites `src/model.rs:2987-2998` where
2998 is that brace. The row still cites the complete block that owns the diagnostic, the catalog is
hand-maintained Tier P with no machine-checked range rule, and the brief requires only that new rows
match the `| Severity | Condition | Source |` column shape, which they do.

---

### L10. Field-reference tables do not mention the new whitespace invariant (dismissed: criterion 6 names the validation catalog specifically)

**Severity:** LOW · **File:** `docs/workflow-schema.md:1123`, `docs/workflow-schema.md:1278` · **Source:** Claude (CLAUDE-10) · **Cross-reference:** Codex DISAGREE

The `decideConfig.outcomes` and edge `label` field rows do not state that surrounding whitespace is
refused. Acceptance criterion 6 requires a row in the **validation catalog**, which the diff adds; the
brief neither requires nor forbids expanding the field-reference prose. The reporting reviewer rated it
45% and flagged it as possibly deliberate minimalism. Left alone as documentation scope creep.

---

### L11. Fix round 1 left the validation catalog half-updated, with two rows citing the same range for different errors (FIXED)
> **Fix:** The four pre-existing decide rows reverted to their pre-change Source cells, the two whitespace rows pointed at their accurate post-redesign call sites. Both reviewers confirmed the document's baseline diff is now exactly two added lines and zero removed — the reverts land as unchanged context, `parallel_batch` is untouched, and no two rows cite overlapping ranges.

**Severity:** LOW · **File:** `docs/workflow-schema.md:1680-1696` · **Source:** Claude (CLAUDE-11, fix round 1) · **Cross-reference:** Codex reported no new findings; orchestrator verified against the source

The M2/M3 restructure shifted the decide region by roughly +20 lines. The two whitespace rows were
re-numbered to match; no pre-existing row was. Verified directly against the current tree — the
collision is visible in the table as it stands:

```
| error | `decide` outcome label has leading or trailing whitespace       | src/model.rs:3010-3018 |
| error | `decide` outcome label does not match an outgoing `branch` edge label | src/model.rs:3010-3019 |
```

Two different errors, essentially one range; the second points at the M1 comment and the padding gate
rather than the correspondence check. Nine further rows (empty prompt, input binding, duplicate
inputs, no outcomes, empty outcome label, duplicate outcome labels, branch count, branch edge missing
label, and the `parallel_batch` rows) are stale by the same shift.

**Adjudication.** The brief's **Out of scope** sanctions stale citations — "add the new row and leave
the rest" — so uniform staleness is permitted. What is not the brief's intent is the half-updated
state: round 0 refreshed the four pre-existing decide rows, and round 1 refreshed the two new ones,
which is how two rows ended up colliding. This is L3 recurring at larger magnitude, and unlike L3 it
now actively misdirects a reader. Fixed by returning the table to the state the brief asked for
rather than by taking on the citation-refresh burden it disclaims.

**Fix:** Revert the four pre-existing decide rows (duplicate outcome labels, correspondence, branch
count, branch edge missing label) to their pre-change citations so every pre-existing row is stale by
the same shift, and point the two new whitespace rows at their accurate current call-site ranges. Do
not refresh any other row.

---

### L12. The empty-outcomes regression does not pin the suppression invariant M2 put at risk (FIXED)
> **Fix:** Converted to a complete-issue-set `assert_eq!` through a new `decide_validation_issues` projection helper. Both reviewers confirmed it now pins absences — a hoisted branch-count check would break the assertion, since the fixture has one branch edge against zero outcomes.

**Severity:** LOW · **File:** `src/model.rs:6272-6295` · **Source:** Claude (CLAUDE-12, fix round 1) · **Cross-reference:** Codex reported no new findings; orchestrator verified against the source

`validates_decide_rejects_padded_branch_edge_when_outcomes_empty` asserts that the whitespace error is
present and that the `requires at least one outcome` error is present. It asserts nothing about what
must stay **absent**. Confirmed by reading the test: both assertions are `.any(...)` presence checks.

The entire risk M2's restructure introduced was that the `else` block might stop suppressing
branch-count, empty-label and correspondence for an empty outcome list. Both reviewers verified the
code is correct today, but with `outcomes: []` and one branch edge the branch-count check would fire
if it were ever hoisted out of the `else`, and this test would still pass.

**Fix:** Assert the complete issue set for this fixture — the empty-outcomes error, the branch-edge
whitespace error, and the `target_0` terminal-node warning — in the same shape L1 now uses.

---

### L13. Whitespace-only branch-edge labels are now reported twice (FIXED)
> **Fix:** Redesigned as specified — the two uncoordinated passes collapsed into one classifying loop with mutually exclusive arms, a label trimming to empty always taking the missing-label arm. Both reviewers returned PASS on all six checkable properties and confirmed the three witnesses are complete-issue-set assertions with independently derived expected values. The escalation is discharged: the mechanism is gone, not the symptom.

**Severity:** LOW · **File:** `src/model.rs:3054-3081` · **Source:** Claude (CLAUDE-13, fix round 1) · **Cross-reference:** Codex reported no new findings; orchestrator verified against the source

Round 0 carried a `continue` that suppressed the whitespace scan once the empty-label guard had fired.
M2's restructure split that single loop in two — the guard now lives inside the `else`
(`src/model.rs:3054-3070`), the whitespace scan outside it (`src/model.rs:3073-3081`) — with no
coordination between them. Verified against the source: the outer loop is an unconditional
`for edge in &branch_edges`, so a `label: Some("   ")` with a non-empty outcome list now yields both
`... branch edge "X" requires a label matching an outcome.` and
`... node branch edge label "   " has leading or trailing whitespace.` Round 0 and the pre-change
baseline both reported it once.

This is not an acceptance-criterion regression — criterion 4 covers labels carrying *no* surrounding
whitespace, and an all-whitespace label is not one. But it contradicts the adjudication recorded at
L6, where an all-whitespace label was ruled to be an *empty* label owned by the pre-existing guard,
and it is an unintended consequence of the restructure rather than a decision.

**Same-site escalation.** This finding's file and function are the ones fix round 1 just restructured
under M2 — the patch and the finding are the same code disagreeing with itself. Per the escalation
rule the cluster {M2, L13} is treated as **one** defect, and round 2 switches from patching to
redesigning the branch-edge region: the mechanism to remove is the *split* — two uncoordinated passes
over `branch_edges` answering questions about the same edge. A fix that leaves two passes standing is
rejected even if the double report is gone. The checkable properties the redesign must satisfy, and
the fixture corpus asserting the reason for each outcome, are stated in the round-2 brief.

**Fix:** One coordinated pass over `branch_edges` that decides, per edge: label missing or trimming to
empty → the existing `requires a label matching an outcome` error and nothing else (suppressed for an
empty outcome list, as today); otherwise padded → the whitespace error (never suppressed). Messages
byte-identical to today's for every shape that produces them.

---

### L14. The empty-outcomes suppression policy is now expressed in two places (FIXED)
> **Fix:** `let has_outcomes = !config.outcomes.is_empty();` bound once above the branch and used at both sites. Both reviewers confirmed control flow is otherwise identical and that the binding was correctly *not* applied to the branch-count check, which is a length comparison rather than the emptiness predicate. P1–P6 all re-verified PASS.

**Severity:** LOW · **File:** `src/model.rs:3062` (mirrors `src/model.rs:2983`) · **Source:** Claude (CLAUDE-14, fix round 2) · **Cross-reference:** Codex reported no new findings; orchestrator verified against the source

Hoisting the branch-edge loop out of the `if config.outcomes.is_empty()` / `else` structure — which is
what property P1 demanded — means the loop must re-consult the same predicate. Confirmed by reading the
source: `if !config.outcomes.is_empty()` at `src/model.rs:3062` restates, some 79 lines later, the
condition already branched on at `src/model.rs:2983`, with nothing in the code linking them.

This is the price of P1, not a defect — the alternative is the duplicated loop L13 removed — and both
halves are pinned by the two complete-set witnesses. Recorded and fixed in its cheapest form because
the coupling is invisible at the second site, and a future change to what an empty outcome list
suppresses needs coordinated edits at both.

**Fix:** Bind the predicate once above the branch and use that binding at both sites.

---

### L15. The new projection helper was introduced without updating the older test that inlines it (FIXED)
> **Fix:** The inlined mapping replaced with `decide_validation_issues(&result)`. Both reviewers checked the `expected_issues` literal specifically and found it byte-identical to the previous round — only the actual-value construction moved, so expectations were not retrofitted.

**Severity:** LOW · **File:** `src/model.rs:6369-6379` · **Source:** Claude (CLAUDE-15, fix round 2) · **Cross-reference:** Codex reported no new findings; orchestrator verified against the source

`decide_validation_issues` was added at `src/model.rs:6274-6286` and is used by the three round-2
witnesses, but `validates_unpadded_decide_labels_unchanged_at_document_boundary` still builds the
identical `Vec<(String, Option<String>, String)>` by hand. Verified byte-for-byte: the inlined block is
the exact mapping the helper performs. Both sites are inside the issue diff, so this is duplication
this change introduced rather than pre-existing shape.

**Fix:** Replace the inlined mapping with a call to `decide_validation_issues(&result)`.

---

### L16. L14's binding shifted the source by two lines and re-staled both whitespace catalog rows (FIXED)
> **Fix:** The two Source cells set to `src/model.rs:3012-3020` and `src/model.rs:3075-3083`, docs only. Both reviewers checked the range boundaries rather than the endpoints and confirmed each range is now fenced by its neighbouring checks and bleeds into neither, and both independently confirmed `src/model.rs` did not move this round — which is what keeps the citations accurate through commit. Claude withdrew its helper recommendation on the reasoning above.

**Severity:** LOW · **File:** `docs/workflow-schema.md:1685`, `docs/workflow-schema.md:1690` · **Source:** Claude (CLAUDE-16, fix round 3) · **Cross-reference:** Codex judged L11 NO REGRESSION — split verdict, adjudicated by the orchestrator against the source

`docs/workflow-schema.md` was untouched in round 3, exactly as required — the source moved under it.
Inserting `let has_outcomes = …` plus its blank line pushed the decide region down by two.

**Adjudication of the split verdict.** Codex reported the cited ranges still *contain* their call sites
and therefore no regression; Claude reported both ranges wrong at both ends. Read the source directly
and Claude is right — "contains the push statement" is too lenient a test for a citation that is
supposed to delimit the block that owns the error:

- Outcome row cites `3010-3018`. The block is `3012-3020` (comment 3012-3014, `if let` 3015-3020).
  Lines 3010-3011 are the `continue;` and closing brace of the **empty-outcome-label** guard — a
  different error — and the cited range truncates its own last two lines.
- Branch-edge row cites `3073-3081`. The arm is `3075-3083`. Lines 3073-3074 are the `});` and `}`
  closing the **`requires a label matching an outcome`** push — again a different error — and again the
  last two lines are cut.

So L11, closed one round earlier, has regressed: these two rows were made accurate on purpose and are
no longer accurate. This is not the inherited staleness the brief disclaims — it is this issue's own
rows.

**Recommendation considered and not taken.** Claude proposed pointing both rows at
`decide_padded_label_issue` (`src/model.rs:2917-2935`), where the message each row restates lives, as
stable against further edits inside `validate_decide_node_config`. Declined: every sibling row in this
table cites the block that *decides and emits* its error, not shared formatting infrastructure, and
the two rows would then carry an identical Source cell — the very shape that made the round-1 collision
hard to read. The stability argument also weakens at commit: this is the last fix round, so the source
stops moving, and any future shift is the fragility the brief explicitly declines to own for every
other row in the table.

**Fix:** Update the two Source cells to `src/model.rs:3012-3020` and `src/model.rs:3075-3083`. Docs
only — no source change, so the ranges stay accurate through commit.

---

## Verified Clean

- **Criterion 1 — identically-padded pair rejected** (Claude) — `src/model.rs:2987` fires on `outcome != outcome.trim()`; test at `src/model.rs:6149` runs serialize → strict `serde_json::from_value::<WorkflowV3>` → `validate_workflow`. Red-before confirmed by reading the pre-change code: `branch_labels` held the raw `" yes "` and the raw outcome matched, so no error was produced.
- **Criterion 2 — refusal names whitespace** (Both) — both new messages contain the literal word "whitespace"; `rg -n -i 'whitespace|padding' src/model.rs` shows no other validation message does, so the signal is unique to this defect, and the tests key on it rather than on error-presence.
- **Criterion 3 — both sides carry the invariant independently** (Claude; Codex qualified) — outcome side `src/model.rs:2987`, branch-edge side `src/model.rs:3056`; tests at `src/model.rs:6161` and `src/model.rs:6183`. The padded-outcome test additionally asserts the pre-existing mismatch error is *absent*, proving the refusal is not a side effect. Codex's empty-`outcomes` exception is M2.
- **Criterion 5 — routing unchanged** (Both) — no runtime or outcome-selection code changed; existing decide routing tests (`src/runtime.rs:11082`, `13660`, `14496-14640`) pass unchanged.
- **Criterion 6 — catalog observable** (Both) — the anchored `rg` returns the two new rows at `docs/workflow-schema.md:1685` and `:1690`, both in the neighbouring rows' column shape and among the decide rows.
- **Criterion 7 — suite** (Claude) — `cargo test --locked` green (477 tests) and `npm test` green (14 files, 131 tests); `tests/docs_catalog.rs` 6 passed / 1 ignored. The hand-written validation catalog sits outside every `<!-- BEGIN GENERATED -->` block, so the freshness assertion is unaffected.
- **Fallthrough arm genuinely closed** (Claude) — traced end to end: a matched selection carries a verbatim clone of a declared outcome (`src/runtime.rs:4344`); a failed one sets `success = false` and returns at `src/runtime.rs:4554` before `select_next_decision`. Routing's trim of `result.output` (`src/runtime.rs:6381`) is now a no-op, and run start rejects error-severity issues (`src/api.rs:754-767`).
- **Out-of-scope boundary respected** (Both) — the diff touches only `src/model.rs`, `docs/workflow-schema.md` and the issue record. No runtime change, no glossary or execution-model edit, and no prose anywhere describing the padded-label route as author-usable — the binding constraint holds.
- **Rejected alternative honoured** (Claude) — the duplicate-outcome `seen_outcomes` set still inserts raw labels and the membership test is still raw; nothing was trimmed at the comparison, exactly as the brief required.
- **Blast radius** (Claude) — all six bundled templates parsed programmatically: zero padded decide outcomes, zero padded branch-edge labels, so no shipped document is newly invalidated.
- **Whitespace definition consistent** (Claude) — both new checks use `str::trim`, matching the pre-existing empty-label guard and the runtime; no Unicode normalization or zero-width handling was added.
- **Security / performance** (Codex) — no injection, authorization bypass, data exposure, or insecure-default issue introduced; the new work is allocation-free trimming plus linear label scans.

## Smells (advisory)

- `src/model.rs:3055` — Speculative Generality: the `if let Some(label)` re-check can never be `None` — the `is_none_or` guard plus the new `continue` already returned for that case · fix: destructure once above the guard with a `let … else`.
- `src/model.rs:2969` — Duplicated Code (borderline, pre-existing): the `outgoing.iter().filter(|e| e.outcome == Branch)` walk is built twice, once into `branch_labels` and once into `branch_edges` · fix: collect `branch_edges` once and derive `branch_labels` from it. Second site is outside the diff, so this stays advisory.
- `src/model.rs:6140` — Mysterious Name (borderline): `has_decide_whitespace_validation_error` reads as "the" whitespace error but matches either of two distinct errors · fix: rename to `has_any_decide_whitespace_error`, or split per side.

The fourth reported smell — duplicated whitespace-guard bodies at `src/model.rs:2987` and `:3056` —
was promoted out of this track into finding **M3** by the in-diff duplication rule (both cited sites
inside the issue diff). Codex reported no smells.

## Outcome

**19 findings, all terminal: 11 FIXED, 2 deferred, 6 dismissed.**

- FIXED — M1, M2, M3, L1, L2, L11, L12, L13, L14, L15, L16
- deferred — L3 (catalog citation staleness the brief disclaims), L7 (editor edge-label trim, outside the brief's frontend boundary)
- dismissed — L4, L5, L6, L8, L9, L10

Provenance: 13 raised at first review (Claude 10, Codex 3), minus one merge (CLAUDE-4 = CODEX-2), plus one
promoted in-diff duplication smell (M3) gives the 13 round-0 headings; fix-round re-verification then added
L11-L13, L14-L15 and L16.

Four fix rounds were used, the full budget. Round 2 was a **redesign round** under the same-site escalation
rule: L13's file and function were the ones round 1's M2 patch had just restructured, so the cluster {M2, L13}
was treated as one defect and the round was dispatched to remove the mechanism — two uncoordinated passes over
`branch_edges` — rather than the symptom. Both reviewers returned PASS on all six checkable properties the
redesign had to satisfy, each pinned by a complete-issue-set fixture.

The remaining recurrence was structural rather than behavioural: `validate_decide_node_config` shifted line
numbers in three consecutive rounds, re-staling this issue's own two catalog citations twice (L11, then L16).
Both are accurate at commit; any future shift is the citation fragility the brief explicitly declines to own.

Full suite green outside any sandbox: `just test` — Rust 461 + 6 + 17 passed, 0 failed, 1 ignored; frontend 131
passed across 14 files. The two load-sensitive flakes the Claude reviewer hit under concurrent cargo runs
(`run_control_routes_return_typed_client_errors` in `tests/http_api.rs`, and
`runtime::tests::decide_abort_returns_promptly_and_kills_pane`) both passed in the authoritative run.

Recommended follow-up, outside this brief's boundary: the deferred L7 — the editor trims decide outcomes
(`DecidePanel.svelte`) but not branch-edge labels (`EdgeInspector.svelte`), so an author can still type a padded
branch label and meet the new refusal only at run start. It is the only residual finding with a user-visible
consequence.
