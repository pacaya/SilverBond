# Code review — ISSUE-260826-0637-03 (2026-08-30T03:06:25Z)

Scope: issue diff vs c6ee39d32977afa0308991d489bb8619df17331e · Reviewers: Claude + Codex (cross-verified, adjudicated inline)

Claude raised 14 findings, Codex 9; five pairs were independent confirmations of the same defect and are merged below. Both `=== SMELLS ===` blocks were empty.

**Round 1 outcome (2026-08-30).** Ten of eleven findings verified fixed by both reviewers. L3 came back STILL_BROKEN from Codex and was independently re-raised by Claude, and Claude's NEW-1 (admitted as L6) landed in the same paragraph the round-1 fix had just edited. Both surviving items sit inside the single **Flat-to-nested `kind` migration** paragraph, so round 2 is run under the **same-site escalation rule** as one defect rather than two: the paragraph is rewritten as one account derived end-to-end from `migrate_workflow_value_to_v4` and `migrate_v2_node_to_v3_kind`, not patched sentence-by-sentence. The shared mechanism is a paragraph asserting the *shape* of a migration step from its summary rather than from the construct that implements it — first as an unqualified "regardless of declared version", then as an over-general `*Config` glob.

### M1. Root `agentConfig` is documented as "not accepted" when it is silently ignored (FIXED)

> **Fix:** Rewritten to say the canonical v4 shape keeps variant config inside `kind`, and that on a document already carrying `kind` a root `agentConfig` is silently ignored and the override lost, cross-referencing **Unknown keys**; the migration sentence now qualifies on accepted declared versions. Both reviewers re-derived it against the migration guard and the absent `deny_unknown_fields`.

**Severity:** MEDIUM · **File:** `docs/workflow-schema.md:45-47,92-93` · **Source:** Both (CLAUDE-1 + CODEX-2)

The doc says "placing `agentConfig` on the node root is the legacy flat shape and is not accepted on a canonical v4 document". On a v4 node that already carries `kind`, `migrate_v2_nodes_to_v3_kind` skips the node entirely (it only fires when `kind` is absent and `type` is present), and `WorkflowNode` has no `deny_unknown_fields`. The document loads successfully and the root `agentConfig` is silently dropped. "Not accepted" reads as rejection — exactly the failure mode the doc's own **Unknown keys** paragraph warns against seventeen lines earlier.

Codex's second half is also correct: missing and unsupported versions return *before* `migrate_v2_nodes_to_v3_kind` runs, so "runs on every ingest, regardless of declared version" needs an accepted-version qualifier.

**Fix:** Say the canonical serialized v4 shape places `agentConfig` under `kind`; on a canonical v4 document a root `agentConfig` is silently ignored and the override is lost (cross-reference **Unknown keys**). Qualify the migration sentence as running on every *accepted* declared version.

---

### M2. The document asserts a save-time validation-issue channel that does not exist (FIXED)

> **Fix:** "save" removed from the channel claim; the channel is now attributed to run creation and the validation endpoint, with an explicit sentence that saving does not validate. Both reviewers confirmed `save_workflow` neither calls `validate_workflow` nor returns an issues array.

**Severity:** MEDIUM · **File:** `docs/workflow-schema.md:77-78` · **Source:** Codex (CODEX-3), confirmed by Claude

The doc routes the reader to "the validation-issue channel a run **or save** response exposes". `save_workflow` normalizes, persists, and returns `{success, name}` — it never calls `validate_workflow` and never returns `ValidationIssue` values. Only the validation endpoint and run creation expose that channel. This contradicts the Context Pack's `Validation Issue` term verbatim ("validation is enforced only at run start, saving never validates") and recreates the precise implication the glossary's *Avoid* clause forbids.

**Fix:** Keep the claim at the model boundary — version rejection happens before `validate_workflow` and therefore never enters its issues vector. If endpoint behaviour is mentioned at all, state separately that saving does not validate.

---

### M3. The regeneration contract is written as an operational command that does not exist (FIXED)

> **Fix:** All three sites rewritten in prospective voice naming ISSUE-260826-0637-04 as supplier; `just regen-docs` retained as the contract name and the unresolvable `tests/docs_catalog.rs` citation dropped — which also closes the one sub-point of CODEX-4 that was unambiguously valid.

**Severity:** MEDIUM · **File:** `docs/workflow-schema.md:104-106,251-259` · **Source:** Both (CODEX-6 + CLAUDE-2 + CLAUDE-3 + CLAUDE-13)

Three sites present the follow-on generator as working today: "The generator emits one readable fragment block…" (:104), "To refresh generated blocks after changing the model, run: `just regen-docs`" (:251-255), and "By default, `cargo test` regenerates in memory and asserts the committed markdown matches" (:257-259). None exists in the reviewed tree — `justfile` defines no `regen-docs` recipe, `tests/` holds only `http_api.rs`, and `SB_REGEN_DOCS` has zero code hits. The lone hedge, "(`tests/docs_catalog.rs`, added in a follow-on issue)", attaches grammatically to the recipe sentence only, and the path it cites is unresolvable, which also fails AC 9's one-jump criterion.

The brief permits naming the command as the contract; it does not permit imperative present tense.

**Fix:** Put all three sites in explicit contract/prospective voice naming ISSUE-260826-0637-04 as the supplier — the command name stays, the operational framing goes.

---

### M4. The repo-wide `deny_unknown_fields` negative carries no followable citation (FIXED)

> **Fix:** Discovery command `rg 'deny_unknown_fields' src/` attached to the negative. The Claude reviewer executed it: zero matches, exit 1 — the empty result is the proof the claim needs, and the form matches what the record's round-5 gate established for this class.

**Severity:** MEDIUM · **File:** `docs/workflow-schema.md:29-31` · **Source:** Codex (CODEX-4, narrowed on adjudication)

"No struct in `src/` uses `deny_unknown_fields`" is a repo-wide negative backed only by the token `src/`. A reader cannot follow that in one jump, and the issue record's own round-5 gate established a standalone discovery command as the required form for exactly this class of claim (a bare figure or unaddressable negative over decaying repo state).

Codex's broader premise — that every assertion in the file needs `path:line` because that is "the repository's established form" — was **rejected** on adjudication: a sweep of `docs/execution-model.md`, `docs/api-reference.md`, `docs/agent-drivers.md`, `docs/getting-started.md` and `ARCHITECTURE.md` finds zero source citations of any kind, `path:line` is a harness-record convention, and the brief explicitly says not to restate line numbers. Only the discovery-command half survives.

**Fix:** Attach a standalone read-only discovery command to the negative (a `rg` for `deny_unknown_fields` over `src/`), so the claim is re-checkable rather than asserted.

---

### M5. Version failure modes and their messages are conflated (FIXED)

> **Fix:** `version` now described as a JSON non-negative integer (`Value::as_u64`), with the two failure classes split — missing/non-`u64` yields `workflow version is required`, an unsupported non-negative integer yields the `anyhow::bail!` message — and both classified as ingest errors. Drift §1.10 item 74 now covered.

**Severity:** MEDIUM · **File:** `docs/workflow-schema.md:18,73-77` · **Source:** Both (CODEX-7 + CLAUDE-5 + CLAUDE-6)

`.and_then(Value::as_u64)` runs before the supported-version match. A string (`"4"`), a float (`4.0`) and a negative number therefore all fail with `workflow version is required` — even though the key is present — and never reach the `anyhow::bail!` carrying `Only workflow versions 2, 3, and 4 are supported`. Two claims are wrong as written: ":18 must be present and parseable as an integer" (invites the reader to think `"4"` works) and ":77 These rejections are `anyhow::bail!` ingest failures" (the missing/non-`u64` path is a `.context` on an `Option`, not a `bail!`). This is drift-audit §1.10 item 74, a section the brief assigns to this issue in full.

**Fix:** Describe `version` as a JSON non-negative integer, and split the two failure classes — missing/non-`u64` versus unsupported integer — naming each message and noting both are ingest errors rather than validation issues.

---

### M6. Startup snapshot re-normalization is stated as unconditional (FIXED)

> **Fix:** Now states initialization *attempts* re-normalization: valid supported legacy snapshots are rewritten, while invalid JSON and normalization failures are skipped with a `tracing::warn!` and left byte-unchanged. Both reviewers matched it to the two warn-and-`continue` branches preceding the update.

**Severity:** MEDIUM · **File:** `docs/workflow-schema.md:99-100` · **Source:** Both (CODEX-8 + CLAUDE-10)

"Stored run snapshots whose `workflow_json` is not version `4` are re-normalized on database init." The selecting query also picks up rows failing `json_valid`, and `upgrade_run_workflow_json` skips both invalid JSON and any normalization failure with a `tracing::warn!`, leaving those rows byte-unchanged. An operator reading this would assume damaged or unsupported (v1/v5) snapshots were repaired when they will keep failing on read.

**Fix:** State that initialization *attempts* normalization — valid supported legacy snapshots are rewritten; invalid or unsupported rows are skipped with a warning and left unchanged.

---

### L1. Unknown-key leniency is overstated (FIXED)

> **Fix:** Scoped to additional unknown keys and misspelled optional/defaulted fields, with the required-field set whose misspelling still fails deserialization named explicitly. The Claude reviewer independently re-derived that set and found the list exactly right, with no over- or under-inclusion.

**Severity:** LOW · **File:** `docs/workflow-schema.md:29-31` · **Source:** Codex (CODEX-1), confirmed by Claude

"A misspelled or obsolete key at any level is silently ignored on ingest" is true of a *stray* key but misleads about the document's fate when the typo replaces a required one. `WorkflowV3::version`/`entry_node_id`, `WorkflowNode::id`/`name`/`kind`, `WorkflowEdge::id`/`from`/`to`/`outcome` and the `NodeKind` tag carry no serde default, so misspelling any of them still fails deserialization. Kept at LOW rather than Codex's MEDIUM: the warning errs toward distrust, which is the safe direction.

**Fix:** Scope the sentence to additional unknown fields and misspelled optional/defaulted keys, then state that replacing a required key still fails ingest.

---

### L2. The catalog is described as already listing config shapes (FIXED)

> **Fix:** Now prospective ("will be listed … once the catalog is populated") with `NodeKind` named as the interim authority, matching the five placeholder sections. The "fourteen" count was deliberately kept as glossary-sanctioned vocabulary.

**Severity:** LOW · **File:** `docs/workflow-schema.md:64` · **Source:** Both (CODEX-9 + CLAUDE-9)

"The fourteen `type` values and their config shapes are listed in the generated node catalog below" is false at the committed state — all 28 spans are empty by design. The sentence routes a reader to a blank section with no fallback authority, unlike the five placeholder sections which each name `src/model.rs`.

Codex's secondary advice to drop the handwritten numeral was **rejected**: `CONTEXT.md`'s **Node Kind** entry fixes "the fourteen-variant tagged union" as glossary vocabulary, so the count is sanctioned terminology, not an unowned enumeration.

**Fix:** "…once the catalog is populated; until then `NodeKind` (`src/model.rs`) is the authority."

---

### L3. The flat→nested migration paragraph omits two silent-data-loss behaviours (FIXED)

> **Fix:** Closed in round 2 by the same-site redesign, not by another patch. The strip is now stated as a loop over a fixed array of eleven enumerated literal key names calling `node.remove` on each, so no reader can conclude an arbitrary `somethingConfig` is stripped; the silent-loss consequence is preserved and made more precise ("left at the node root because its key was not moved for the node's `type`"), and the unrecognized-`type` catch-all path is unchanged. Claude diffed the enumerated list against the source array — same names, same order, right count; Codex verified the same.

**Severity:** LOW · **File:** `docs/workflow-schema.md:91-98` · **Source:** Claude (CLAUDE-14), confirmed by Codex

Drift-audit §1.10 items 79 and 80 are unmentioned, and the brief assigns this issue all of §1.10: (a) after nesting the recognized keys, migration unconditionally strips *every* `*Config` key from the node root, so a config object that does not match the node's `type` is silently discarded; (b) an unrecognized `type` falls through the catch-all arm, producing a `kind` carrying only the tag, which then fails `NodeKind` deserialization. Both matter to a reader migrating a v2/v3 document; the word "recognized" only gestures at (a).

**Fix:** One sentence per behaviour with its `src/model.rs` symbol citation, or an explicit note that they are deferred to `## Node fields`.

**Round 1 outcome — still unfixed.** The unrecognized-`type` half landed correctly. The strip half did not: the replacement sentence says migration "removes every `*Config` key from the node root unconditionally", but the loop iterates a **fixed array of eleven literal names** (`agentConfig`, `decideConfig`, `batchConfig`, `parallelBatchConfig`, `subflowConfig`, `spawnConfig`, `sendConfig`, `waitConfig`, `captureConfig`, `killConfig`, `runAgentConfig`), not any key matching `*Config` — a `bogusConfig` at the node root survives that function. Codex returned STILL_BROKEN and Claude raised the same defect independently as NEW-2; the orchestrator confirmed the fixed array against the source. The operative takeaway (a config object not matching the node's `type` is silently discarded) remains true, so this is a precision defect, not a reversal.

---

### L4. "Execution and UI fields" — `WorkflowNode` carries no UI fields (FIXED)

> **Fix:** "and UI" dropped and `splitFailurePolicy` named in the parenthetical. Both reviewers confirmed every remaining named field is execution/config and that canvas geometry lives on `WorkflowV3::ui`.

**Severity:** LOW · **File:** `docs/workflow-schema.md:35-37` · **Source:** Claude (CLAUDE-8)

Every common field the sentence enumerates is execution/config; canvas geometry lives on `WorkflowV3::ui` → `WorkflowCanvasUi`, keyed by node id, not on the node. Codex DISAGREED, correctly noting that `WorkflowNode`'s own doc comment says "common execution/UI fields stay on the node object" — so the doc mirrors the source's phrasing. Adjudicated **kept** anyway: AC 9 asks that surviving claims be true of the source, not that they inherit the source comment's looseness, and a reader who goes looking for UI fields on the node finds none.

**Fix:** Drop "and UI". Optionally name `splitFailurePolicy` in the parenthetical — it is among the fields most likely to be mis-placed inside `kind` by a reader arriving from the flat shape.

---

### L5. The fourteen `### <wire-tag>` headings are hand-maintained outside every generated span (FIXED)

> **Fix:** The narrowed remedy landed in `## Regenerating this document`: the per-kind headings are stated to be hand-maintained and to require updating when the `NodeKind` variant set changes. The block-id interface ISSUE-260826-0637-04 inherits is untouched.

**Severity:** LOW · **File:** `docs/workflow-schema.md:108-218` · **Source:** Claude (CLAUDE-4, narrowed on adjudication)

Only the 28 spans between markers are generator-owned, so the heading sequence itself is hand-written. Adding, renaming or removing a `NodeKind` variant drifts the headings while the generator keeps rewriting the spans, and no check catches it.

Claude's primary remedy — move each heading inside its own `fragment` block — was **rejected**: the block-id scheme is the contract ISSUE-260826-0637-04 explicitly inherits, and unilaterally changing what the generator must emit would rewrite a downstream interface this slice is not granted. Codex DISAGREED with the finding outright; kept at LOW because the drift risk is real even though its cheap remedy is in scope.

**Fix:** Add one line to `## Regenerating this document` stating that the per-kind headings are hand-maintained and must be updated when the `NodeKind` variant set changes.

---

### L6. The accepted-version qualifier dropped the brief's salient point: migration fires on a v4-declared document (FIXED)

> **Fix:** The paragraph now states it outright — the call "is not gated on legacy versions — it runs for every accepted declared version, including a document that already declares version `4`, not only versions `2` or `3`" — and the contrast with the `version < 4`-gated `outputSchema` migration is made visible in the same subsection. Drift §1.10 item 78 is stated in full with no inference chain left to the reader.

**Severity:** LOW · **File:** `docs/workflow-schema.md:102-103` · **Source:** Claude (NEW-1, round 1), verified by the orchestrator

The M1 fix replaced "regardless of declared version" with "runs on every ingest whose declared `version` was accepted by `migrate_workflow_value_to_v4`". That is accurate — the version match and its `bail!` precede the migration call — but the reader must now chain to the accepted set to reach the point the brief states explicitly: the flat-to-nested `kind` migration "runs on any declared version, **not only the legacy ones**", i.e. it fires on a document declaring version `4`. That is drift §1.10 item 78, a section this issue owns in full, and it is the behaviour that explains why a v4-labelled flat document loads at all. The contrast is sharpened by the very next sentence, which correctly gates `outputSchema` rewriting on `version < 4` — the paragraph now states one version-gated and one ungated migration without making the ungated one's v4 case explicit.

Codex reported no new findings this round, so this entered on the orchestrator's own verification: `migrate_v2_nodes_to_v3_kind` is called immediately after the version match, unconditionally, while `migrate_legacy_output_schemas_in_workflow` sits behind the `version < WORKFLOW_SCHEMA_VERSION` guard.

**Fix:** Make the v4 case explicit — the migration runs on every accepted declared version, including a document that declares version `4`, not only the legacy ones.

---

## Round 2 outcome — same-site redesign, ACCEPTED

The **Flat-to-nested `kind` migration** paragraph was rewritten end-to-end from `migrate_workflow_value_to_v4` and `migrate_v2_node_to_v3_kind` in call order, against ten checkable properties given to the implementer as the bar (version acceptance precedes migration; the flat→nested migration ungated with the v4 case explicit; the `outputSchema` migration gated and the contrast visible; the per-node precondition; the strip as a fixed set rather than a pattern; the silent-loss consequence; the unrecognized-`type` path; subflow claims still true; no bare occurrence count; one-jump citations with no `path:line`).

Both reviewers returned **MECHANISM: GONE** on the judgement that mattered — not merely that L3 and L6 were closed, but that no place remains in the paragraph where a step's shape is asserted from its name rather than from the construct implementing it. Both prior failures were failures of summary vocabulary (the adverb "regardless", the wildcard "every `*Config`"); the rewrite quotes two constructs verbatim from source (`_ => {}` and `if version < WORKFLOW_SCHEMA_VERSION as u64`) and names a literal predicate for the per-node guard. All ten properties PASS from both reviewers, all ten round-1 fixes verified HELD (nine fixed phrases grepped for and absent), the structural contract intact, and neither reviewer raised a new finding.

**Rounds used: 2 of 4.** Every finding is terminal: 11 FIXED, 1 admitted mid-loop and FIXED (L6), 4 dismissed, 0 deferred.

## Advisory (recorded, not fixed — Claude, round 2)

Neither was raised as a finding; both are recorded so they are not lost, and both were judged not worth a third round.

- `docs/workflow-schema.md:118-120` — the relative clause "leaving `kind` with only the tag, which then fails `NodeKind` deserialization" could be misparsed as "a tag-only `kind` fails", which is false: `approval`, `split` and `collector` are unit variants whose correct `kind` is exactly tag-only. The sentence's subject is "An unrecognized `type` string", so the natural reading is true as written. If the paragraph is ever touched again, appending "because the tag matches no variant" removes the ambiguity.
- `docs/workflow-schema.md:110-117` — drift §1.10 item 80 (the `"capture"` and `"kill"` arms injecting an empty config object when none was moved) remains uncovered; the `match` description's "and so on" glosses the two arms that behave differently from the other nine. It was never part of L3 as adjudicated, so it is neither a regression nor a missed fix, and an injected empty config deserializes identically to a defaulted one — no reader's behaviour changes. It is the last §1.10 item without a home.

## Dismissed on adjudication

- **CODEX-5** (the `/api/capabilities` sentence violates the §1.12 deletion rule) — dismissed. §1.12 is titled "Narrative claims (D — delete **or fold into C prose**)", so folding is sanctioned; item 91 is an observation about the endpoint, not doc content flagged for removal, while item 90 (the `features.runAs` sentence) *was* deleted; and `CONTEXT.md`'s **Node Kind** entry — a term this issue's `terms:` names — states that exact fact, making the sentence mandated terminology rather than unowned API narrative. It enumerates nothing and cannot drift the way the deleted lists did.
- **CLAUDE-7** (subflow omitting `version` not covered) — dismissed. "Only a subflow whose `version` the ingest path does not accept at all" already covers omission, and the missing-version rule is stated in the same section; the recursion uses the same function.
- **CLAUDE-11** (`docs/getting-started.md` anchor now dangles) — dismissed as a defect in this slice. Correct behaviour: the brief explicitly withholds permission to repoint it and the follow-up is filed as `ISSUE-260826-1648-01`. Reported here so the two land together.
- **CLAUDE-12** (`docs/README.md` blurb now inaccurate) — dismissed. That file is explicitly out of scope and later epic issues repopulate the scaffold; transient, for the epic's closing sweep.

## Verified Clean

- **AC 1 — eight H2 headings, exact set and order** — both reviewers: exactly Document grammar, Node catalog, Node fields, Edges and conditions, Document-level fields, Templates and agent config, Validation catalog, Regenerating this document. No extras, no renames; matches every downstream anchor in -05/-06/-07.
- **AC 2 — marker pairs inside `## Node catalog`** — anchored pcre2 observable returns 1; 28 `BEGIN` and 28 `END`, each opener matched by block-id; scheme `node-catalog:<wire-tag>:{fragment,workflow}` covers all fourteen kinds and is what ISSUE-260826-0637-04 declares it inherits.
- **AC 3 — legacy `## Node Types` gone** — zero matches.
- **AC 4 — every node example nests its tag inside `kind`** — one example remains, structurally correct (`kind: {type, agentConfig}` with `agent`/`prompt` on the root), checked by reading rather than grepping the tag. No flat-shape example survives.
- **AC 5 — legacy `## Edges` section gone** — zero matches; the `source`/`target` field table and the edge-outcome table are both deleted.
- **AC 6 — `## Document grammar` states nested `kind`, the two required keys, silent unknown keys** — observable matches; independently verified that `version` and `entry_node_id` are the only `WorkflowV3` fields without a serde default, `NodeKind` is `#[serde(tag = "type")]`, and `deny_unknown_fields` appears nowhere in `src/` or `ui/`.
- **Fourteen-kind list and ordering** — the `### <tag>` sequence matches `WorkflowNodeType::as_str` in declaration order and matches `supportedNodeTypes` element-for-element.
- **Version/migration prose, load-bearing claims** — missing-version and unsupported-version errors, "never a `ValidationIssue`" (all production callers map to `ApiError::bad_request`), force-rewrite on every validation path (validation's first statement is `ensure_defaults`, which assigns the canonical version — structurally guaranteed, not a path enumeration), recursive subflow migration, and unconditional flat→nested migration all verified against the ingest path. No bare occurrence count survives.
- **Version framing / ADR forward reference** — the ADR link resolves, the id matches its front matter, and v5 is stated as decided-and-unlanded, never present tense.
- **Placeholder sections name an authority** — all five defer explicitly, and every symbol they name exists.
- **Deleted-tier content** — no survivor of the flat-shape examples, the `source`/`target` table, the edge-outcome table, the `loopCondition`-as-English-prompt reading, the prompt-template table, agentDefaults/orchestrator/access-mode tables, the UI-metadata block, the `runAs` narrative, or the validation prose.
- **Out-of-scope boundary respected** — the diff touches only `docs/workflow-schema.md` and the issue record; zero changes under `src/`, `ui/`, or the four excluded docs. No deferred table was written; generated spans left empty.
- **Seam checks** — every brief-provided `rg` observable passes; `just check-v4-docs` exits 0; `cargo test` green (449 library + 17 integration tests); filtered diff passes `git diff --check`.
- **Security / executable risk** — none. Documentation-only diff introducing no executable behaviour; the deletions remove no security control.

## Smells (advisory)

None. Both reviewers returned empty `=== SMELLS ===` blocks — Claude folded its one candidate (the repeated per-kind marker stanza) into L5 rather than double-counting, and Codex suppressed it under the "documented standard overrides" rule since the stanza is the specified downstream generator interface.
