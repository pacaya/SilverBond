# Code Review: feature/tmux-panes

**Branch:** `feature/tmux-panes` vs `development`
**Files changed:** 95 files (95 files changed, 38499 insertions(+), 6036 deletions(-))
**Date:** 2026-07-20
**Generated:** 20260720-032528 (UTC — matches the report filename)
**Reviewers:** Claude Opus, Codex (merged adversarial review)
**Adjudicator:** Google Gemini 3.1 Pro (High), third-family judge

---

## HIGH

### H1. Session continuation can reuse an incompatible privileged pane (FIXED)
> **Fix:** Pane adoption and editor validation now enforce effective agent, privilege-ranked access, and cwd compatibility while missing access metadata fails closed.
**Severity:** HIGH — fails silently while validation gives active false assurance; a read-only node executes in an unrestricted process and inherits the wrong driver's destructive-command blocklist.
**Files:** `src/tmux_exec.rs:406-426` (reuse arm), `:926`, `:1235-1240`, `:2056-2194` (`spawn_pane`/`build_agent_command`), `:2119-2130` (pane stamps), `src/model.rs:1806-1819` (continuation validation), `src/driver.rs:282-334`, `:295-299`, `src/runtime.rs:2078-2086` (`agent_name_for_node`)
**Description:** For a `RunAgent` node the effective agent resolves from `runAgentConfig.agent` (`src/tmux_exec.rs:926`, `:1235`; `src/runtime.rs:2078-2086`), but continuation validation compares raw `node.agent` on both sides (`src/model.rs:1807-1808`), so a Codex-effective node passes the "same agents" check against a Claude source pane; `cwd` and `timeout` are shadowed the same way at `:1237-1240`. `PaneGuard::acquire` returns `(pane_id, None, true)` in the reuse arm (`src/tmux_exec.rs:406-426`) and never calls `spawn_pane` — which, with `build_agent_command` (`:2056-2194`), is the only place `access_mode`/`access_profile_override` become argv. Control then goes straight to `wait_for_mode`/`send_text` with no access or agent assertion anywhere. A downstream `read_only` node therefore executes inside the source pane's `Execute`/`Unrestricted` process, and `run_agent_interactive` selects its driver — including the destructive-command blocklist (`:994-1000`) — from the wrong agent.
**Fix:** Enforce launch-configuration compatibility at the point of pane adoption, using metadata the system already stamps.
- Promote the implicit access ordering in `src/driver.rs:295-299` into an explicit privilege rank on the profile type (`read-only < workspace-write < full-access`).
- In `PaneGuard::acquire`'s reuse arm (`src/tmux_exec.rs:407-411`), call `names::read(&pane_id)` and hard-error — in the existing `resolve_reused_pane` error style — unless the pane's `KEY_AGENT` equals this node's effective agent and its `KEY_ACCESS` rank is no broader than the profile `build_agent_command` would produce for this node's `AgentConfig`.
- Define and implement an explicit policy for panes spawned with a `command` override, which carry no `KEY_ACCESS` (`:2074`, `:2122`); fail closed.
- Fold in the editor-time projection of the same rule: export `agent_name_for_node` (or a `model.rs` twin) and use it on both sides of `src/model.rs:1807-1808`, extending the comparison to effective access profile and `cwd`, so authors see the error before the run starts.
- Add regressions for a `runAgentConfig.agent` override mismatch, for a `read_only` node continuing from a broader-access source, and for a `command`-override pane with no recorded access.

---

### H2. Session registration lifecycle ignores its own success/failure signals (FIXED)
> **Fix:** Kill and cleanup outcomes now use post-action has-session probes; registrations are removed only after confirmed absence, with per-target terminal verdicts and contextual warnings.
**Severity:** HIGH — an unkilled session plus its agent process leaks permanently and silently, unrecoverable by any later reaper pass; the same path carries `run_as` panes running as another user.
**Files:** `src/tmux_exec.rs:1205-1207` (`kill_tmux_session`), `:1209-1222` (`cleanup_panes`), `:517` (dropped register result); `src/storage.rs:464-473` (`register_tmux_session`), `:479-506` (`remove_tmux_sessions`), `:525` (`list_reapable_tmux_sessions`); `src/runtime.rs:6352-6371`, `:6374-6381`, `:6479`, `:6507-6510`, `:6517-6538`
**Description:** `kill_tmux_session` is `tmux::run(&["kill-session", "-t", name]).is_ok()`, and in the pinned `tmux-tools-core` rev `69173d1` `run_with` returns `Ok(TmuxOutput { … exit_code })` with no status check — only `run_checked` routes through `check()`. A kill *rejected* by tmux therefore reports success. The reaper treats that `Ok(true)` as confirmation and calls `remove_tmux_sessions` (`src/runtime.rs:6517-6537`), deleting the only record that makes the session reapable; because the reaper attempts kills only on sessions it just confirmed live (`:6479`, `:6507-6510`), a nonzero exit there is almost certainly a genuine failure, so the delete is wrong in exactly the reachable case. `cleanup_panes` discards every result with `let _ =` while its caller computes `killed_sessions` from the target list *before* the kills run (`:6352-6355`), and `register_tmux_session`'s `false` return (runs row absent) is dropped at both `src/runtime.rs:6374-6381` and `src/tmux_exec.rs:517`. Net: a live tmux session survives permanently, invisible to boot reaping and to the run-observability surface, with zero diagnostics.

*Correction to the original write-up:* the `Ok(false)` warn arm at `src/runtime.rs:6538` is **not** dead code — `run` still returns `Err` on spawn failure and on the 5 s command timeout. It is unreachable only for tmux-level rejection.
**Fix:** Determine kill success by observing tmux state rather than trusting the kill's own result.
- After issuing the kill, probe `has-session -t <name>`; treat a nonzero exit as "session gone". Ignore the kill command's exit status entirely.
- Delete the DB registration only when the session is confirmed absent. Otherwise retain the registration and `warn!` with `run_id` and `session_name` so a later reaper pass can reconcile it.
- Change `cleanup_panes` to return per-target absent/present verdicts instead of `let _ =`, and compute `killed_sessions` (`src/runtime.rs:6352-6355`) from those verdicts rather than from the target list.
- Propagate `register_tmux_session`'s `bool` at `src/runtime.rs:6374-6381` and `src/tmux_exec.rs:517`, and `warn!` on `false` — a false there means the pane is unreapable from birth.
- Add reaper and terminal-cleanup regressions using tmux fakes that exit **nonzero**; the existing fakes at `src/tmux_exec.rs:3761` and `src/runtime.rs:8534` are `exit 0` scripts and cannot catch this.

---

### H3. Embedded `public/` bundle is stale — shipped binary serves pre-fix UI, and the branch's own new CI gate fails (FIXED)
> **Fix:** Rebuilt public/ via npm install plus just build so the embedded bundle carries this run's UI (decidePrompt and current-password both verified present), and added a just-wired pre-commit build guard; the frontend-freshness required-status-check setting remains a manual repo-owner step.
**Severity:** HIGH — every release binary ships a UI missing the last three commits' fixes, the divergence is invisible in dev, and this is the **third** recurrence of the same defect.
**Files:** `public/assets/index-cKTkf5yB.js`, `public/index.html`, `src/frontend.rs:10`, `.github/workflows/frontend-freshness.yml`, `justfile` (`build` recipe), `ui/vite.config.ts`
**Description:** `src/frontend.rs:10` embeds the bundle at compile time (`static PUBLIC_DIR: Dir<'_> = include_dir!("$CARGO_MANIFEST_DIR/public")`) and `serve()` reads only from that static — there is no filesystem or dev-mode fallback, so staleness is baked into every `cargo build` artifact while `just dev` (Vite on :5173) hides it locally. `public/` was last rebuilt in `da27009`; `ui/src` has since changed by **+275/−159 shipped lines** across `AppShell.svelte` and `workflowStore.svelte.ts` (commits `ba9d6e1`, `05eb37c`, `78d0570`), including the compound-prompt and run-lifecycle fixes. Proof: the literal `"decidePrompt"`, introduced by those commits, appears **0 times** in the committed `public/assets/index-cKTkf5yB.js`. `.github/workflows/frontend-freshness.yml` triggers on bare `push:`/`pull_request:` with no `continue-on-error` and fails on this branch as-is.

*Corrections to the original write-up:* "717 lines across `AppShell.svelte` and `workflowStore.svelte.ts`" is inaccurate — 717 is total insertions across **three** files, 442 of them in `workflowStore.test.ts`, which is never bundled. The gate is **not** flaky: build deps are exact-pinned (`vite 7.3.1`, `svelte 5.53.10`) with `package-lock.json` + `npm ci`, and `index-DcqmMkig.css` kept the same content hash across two independent rebuilds.

*Recurrence:* rebuild-and-commit has already been applied as the fix at `20260620-092605` M1 (which also *added* this very CI workflow), `20260705-074320` L13/M23, and was instructed again at `20260716-003531`. It went stale again each time — a fourth manual rebuild alone is not a fix.
**Fix:** Rebuild now to unblock, then make the existing gate un-ignorable.
- Run `just build` (→ `vite build --config ui/vite.config.ts`, `outDir: ../public`, `emptyOutDir: true`) and commit the regenerated `public/` as the final commit on the branch.
- Configure `frontend-freshness.yml` as a **required status check** in `main`'s branch-protection settings, so a stale bundle blocks merge rather than merely reporting red. This is the load-bearing part of the fix — the workflow already exists and has been ignored across three recurrences.
- Add a `just`-wired pre-commit hook that runs the frontend build and refuses any commit touching `ui/src` without a corresponding `public/` update, so drift is caught at authoring time.
- Note for whoever applies this: the root `node_modules` is present and `npm run build` runs from the repo root; `ui/node_modules` is an empty stub and is not the relevant one.

---

### H4. Opening/importing a legacy workflow crashes the graph editor (FIXED)
> **Fix:** Normalizer now backfills missing decide, parallel-batch, subflow, and call configs from editor defaults for both legacy and kind-shaped nodes, with regression coverage.
**Severity:** MEDIUM — *lowered from HIGH*: reachable only via browser file import, since the backend always emits these configs; but within that ingress it is a total, unrecoverable loss of the editor. ID retained as `H4` for stability.
**Files:** `ui/src/features/editor/flowNodes.ts:67,71`; `ui/src/lib/types/workflow.ts:226-229` (required decls), `:520` (`moveV2ConfigField`), `:543-611` (`normalizeWorkflowNode`), `:545-547` (early return), `:588-596` (capture/kill backfill pattern); `ui/src/lib/stores/workflowStore.svelte.ts:76,166`; `ui/src/lib/stores/nodeMetadata.ts:62-64` (`defaultNodeKind`); ingress `ui/src/features/editor/Sidebar.svelte:33`, `ui/src/app/AppShell.svelte:145`
**Description:** `NodeKind` declares `subflowConfig`/`decideConfig`/`batchConfig` as required (`ui/src/lib/types/workflow.ts:226-229`), but `normalizeWorkflowNode` backfills only `captureConfig` (`:588-590`) and `killConfig` (`:594-596`) — `moveV2ConfigField`'s `if (value != null)` guard at `:520` silently drops the rest. A node whose config is absent or `null` therefore survives as `kind = {type:"subflow"}` with no config, and a node already in v4 `kind` shape bypasses normalization entirely through the early return at `:545-547`. `buildFlowNodes` then throws `TypeError: Cannot read properties of undefined (reading 'workflowName')` at `flowNodes.ts:67` inside `GraphEditor`'s `$effect`; because there is **no `<svelte:boundary>` anywhere in `ui/src`**, the whole canvas blanks with no recovery. Two further unguarded dereferences exist at `workflowStore.svelte.ts:76` and `:166`.

*Correction to the original write-up:* the premise that the Rust serializer omits default-valued configs does **not** hold for these three — `src/model.rs:631-646` uses `#[serde(default, rename=…)]` with no `skip_serializing_if`, so the backend always emits them and backfills missing ones on deserialize. Every backend-sourced path (sidebar open, templates, runs) is safe; the sole crashing ingress is browser file import (`Sidebar.svelte:33` `JSON.parse` with arrays-only validation → `AppShell.svelte:145` `duplicateWorkflowForEditing`), which never round-trips through the backend. The required type declaration is therefore truthful for backend data.

*Prior history:* this is a **known-incomplete prior fix**. S19 (`docs/issues/feature-tmux-panes-code-review-20260705-074320.md:467-478`, FIXED, MEDIUM) named `flowNodes.ts:66-71` explicitly and its fix text promised the normalizer would synthesize kind "including the legacy `subflowConfig` reads" — it never backfilled the configs themselves. S19 also explicitly declined a per-node try/catch as the primary remedy.
**Fix:** Close the gap at the normalizer chokepoint, sourcing defaults from `defaultNodeKind()`.
- In the `decide` / `parallel_batch` / `subflow` / `call` cases of `normalizeWorkflowNode`, fill any missing config from `defaultNodeKind(nodeType)` (`ui/src/lib/stores/nodeMetadata.ts:62-64`), mirroring the existing capture/kill backfill pattern at `:588-596`.
- **Also apply the backfill to already-`kind`-shaped nodes** that currently bypass everything via the early return at `:545-547` — without this, a v4 node with `kind:{type:"subflow"}` and no config still crashes.
- Do **not** source `batchConfig` from `DEFAULT_BATCH_CONFIG` as the original fix text suggested: it is not exported, only a local const inside `InspectorPanel.svelte:225`. Use `defaultNodeKind()` for all three so editor and normalizer cannot drift.
- Verify no import cycle is introduced by `types/workflow.ts` depending on `stores/nodeMetadata.ts`.
- Add normalize regressions for config-less `subflow`, `decide`, and `parallel_batch` nodes; `workflow.normalize.test.ts:57-63` currently covers only capture/kill backfill.

---

### H5. Nested splits lose the outer family's failure policy — a failed branch finalizes the run as Completed (FIXED)
> **Fix:** Split-family liveness now derives from active cursor references; stale member IDs are removed, missing-family warnings added, and nested fail-fast cancellation regression-tested with checkpoint compatibility.
**Severity:** HIGH — silent success-on-failure with no backstop anywhere; an orchestration engine reporting a failed run as Completed is the worst defect class in this codebase.
**Files:** `src/runtime.rs:228` (`SplitFamilyState`), `:1927-1935` (`evict_idle_split_families`), `:5412`, `:5421-5431`, `:5428`, `:5525-5534` (`handle_split_node`), `:5941-5943` (`handle_terminal_cursor_status`), `:5957-5962` (sibling cancel), `:6254-6263` (`finalize_run`), `:5696-5705` (collector release); related `:5711`, `:5834-5852` (M9)
**Description:** `handle_split_node` removes the parent cursor from `active_cursors` (`src/runtime.rs:5412`) and inserts the new family with `member_cursor_ids: child_cursor_ids` only (`:5421-5431`), while children are pushed carrying `[…inherited, new]` family ids (`:5525-5534`) and are never added to the inherited families' member lists. Outer families thus reference only dead cursor ids, and `evict_idle_split_families` — whose retain predicate is "some `member_cursor_ids` entry is still in `active_cursors`", guarded only by `force_failed` (`:1927-1935`) — drops them while live descendants still hold their ids. On a later descendant failure, `handle_terminal_cursor_status` sets `fail_run = cursor.split_family_ids.is_empty()` → false (`:5941`), then the bare `if let Some(family) = …get_mut(family_id)` at `:5943` finds nothing and silently skips with no `else` and no warn; `finalize_run` (`:6254-6263`) sees no `force_failed` family and returns `RuntimeStatus::Completed`, and sibling cancellation (`:5957-5962`) never fires.

**No backstop exists:** the only run-failure paths for a failed node are `handle_terminal_cursor_status` (call sites `:4256`, `:4507`, `:4741`, `:5085`) and the abort path; collector release always constructs `NodeResult { success: true, … }` (`:5696-5705`) and merely counts failures into `parsed_output.summary`. The run genuinely finalizes Completed.

*Trigger, narrowed:* nested splits are legal — `src/model.rs:1710-1735` only forbids non-success edges out of a split and warns on fan-out < 2. The silent-success outcome requires the **outer** family's policy to be stricter than the inner one (`fail_fast_cancel`/`drain_then_fail` outer over `best_effort_continue` inner); a loop back to the same split produces the same stale ids but is benign, since the regenerated family carries an identical policy. Eviction is not rare — it also runs at the end of `release_collectors_if_ready` (`:5882`), invoked after every collector arrival (`:5646`) and every terminal cursor (`:5973`). Minimal repro: outer split `fail_fast_cancel` → A, B; A → inner split `best_effort_continue` → A1, A2; B → terminal. B's `finish_cursor` evicts the outer family; A1 then fails → Completed.

*Prior history:* this is a **correctness regression created by a prior fix**. `docs/issues/feature-tmux-panes-code-review-20260620-092605.md:181-189` M14 (FIXED) introduced eviction purely as a memory/write-amplification optimization, explicitly characterized as "never a correctness fault", proposing exactly the rule that causes this. Never previously reported or dismissed. **Shares a root cause with M9** — family membership is denormalized onto cursor ids that are destroyed and re-minted at control-flow transitions.
**Fix:** Make split-family liveness derived from cursors rather than denormalized onto the family, and fix M9 in the same change.
- Rewrite `evict_idle_split_families` (`:1927-1935`) to build the referenced set from `active_cursors.iter().flat_map(|c| &c.split_family_ids)` and retain any family present in it, keeping the existing `force_failed` guard.
- Remove `member_cursor_ids` from `SplitFamilyState` (`:228`) and its population at `:5428`. The eviction predicate at `:1932` is its only reader, so once liveness is derived it is pure duplication — and the two-sided invariant is what allowed the desync.
- Handle checkpoints persisted by the current build: `#[serde(default)]` on the removed field or a checkpoint-version bump, so in-flight runs deserialize.
- Add an `else` branch at `:5943` emitting `tracing::warn!` with the missing `family_id` and `cursor_id`, as a permanent invariant tripwire.
- Fix M9's collector-release fallback (`:5711`, `:5834-5852`) in the same change so it stops emitting a cursor with empty `split_family_ids`, which detaches a live cursor from its policy by the same mechanism.
- Add nested-split regressions: `fail_fast_cancel` outer over `best_effort_continue` inner with an inner-branch failure must finalize Failed and cancel siblings. No existing test covers nested splits — `split_spawns_collects_and_continues` (`:9697`), `split_and_collector_maps_evict_after_completion_without_restart` (`:9772`), `drain_then_fail_waits_for_siblings_before_failing_run` (`:11182`) and `fail_fast_cancel_stops_slow_siblings` (`:11237`) are all single-level.

---

### H6. Pane-stream process setup deadline is not cancellation-safe when descendants inherit pipes (FIXED)
> **Fix:** Enable and teardown now run under an absolute deadline in a dedicated process group with null stdio, closing the indefinite hang; upstream traversal-probe orphan leak remains unpatched (no upstream publish access).
**Severity:** HIGH — carried by the unguarded enable path, which hangs a pane-stream task indefinitely; every timeout on any path also permanently leaks a blocking-pool thread and an orphan process. The prepare path alone would be MEDIUM.
**Files:** upstream `tmux-tools` `core/src/tmux.rs:194`, `:226-229`, `:242-248`; `src/api.rs:61` (`PANE_STREAM_SETUP_TIMEOUT`), `:1391-1409` (guarded prepare), `:1424-1432` (**unguarded** enable), `:1481`, `:1529-1532`; `src/tmux_exec.rs:66-77` (`run_as.command`), `:123-126` (`resolve_tmux_bin_with_timeout`); `Cargo.toml:28` (rev pin)
**Description:** `command_output_with_timeout` cannot honor its deadline when a descendant inherits stdout/stderr: on deadline it runs `child.kill(); child.wait(); join_reader(stdout); join_reader(stderr)` (`core/src/tmux.rs:226-229`), where `read_pipe` is `read_to_end` (`:242-246`) and `join_reader` is an unbounded `handle.join()` (`:248`). `Child::kill` is `libc::kill(pid, SIGKILL)` against a single pid, so any surviving descendant holding the inherited write end withholds EOF forever. SilverBond hits this on three paths: the traversal probe `ensure_run_as_can_traverse_root` (`src/api.rs:1532`), the `pipe-pane` enable (`:1481` via `:1424-1432`), and `resolve_tmux_bin_with_timeout` (`src/tmux_exec.rs:126`). Reachability is ordinary, not pathological: `run_as.command` is operator-arbitrary (`src/tmux_exec.rs:66-77`), `resolve_tmux_bin` runs `<prefix> zsh -lic …` where an interactive rc that backgrounds anything holds the pipe, and `sudo` forks-and-waits whenever PAM session/timestamp cleanup applies, leaving the exec'd `tmux` alive after the parent is killed.

*Correction to the original write-up — the two consequences differ and must not be conflated:* the **prepare** path *is* wrapped in `tokio::time::timeout_at` (`src/api.rs:1395-1409`, with a comment at `:1391-1394` acknowledging detachment), so the caller returns within 6 s and the cost is a permanently leaked blocking-pool thread plus an orphan process. The **enable** path at `:1424-1432` is `.await`ed with **no** outer timeout — only the budget handed to the broken primitive — so a stuck drain hangs that pane-stream task forever. `PANE_STREAM_SETUP_TIMEOUT` is a real bound for prepare and no bound at all for enable. Mitigating: with `use_pty` (sudo ≥ 1.9.14 default) the command's stdout is a pty rather than the pipe; and `start_pane_stream` is per-websocket-connection (`:1951`), so leakage marches toward the 512-thread blocking pool only across many failures.

*Scoping correction:* `tmux-tools-core` is `git+https://github.com/pacaya/tmux-tools.git?rev=69173d1` (`Cargo.toml:28`) — **the maintainer's own repository**, so the "external dependency, not editable in this repo" precedent recorded at `20260716` H3 / R2-1 no longer binds. Upstream change also has precedent: `20260620` M15/M7 landed the timeout helper itself upstream. Related: `20260716` M18 fixed an earlier uncancellable-setup hang by splitting prep from enable — that split is what left the enable path unguarded.
**Fix:** Repair the primitive upstream so the deadline holds regardless of process topology, then bump the pin.
- In `command_output_with_timeout`, set `command.process_group(0)` at spawn and, on deadline, `libc::kill(-pgid, SIGKILL)` to kill the entire group rather than the single child. **Trap to avoid:** `std::process::Command` does not create a new process group by default, so issuing a group kill without `process_group(0)` would signal SilverBond itself.
- Additionally, on deadline `drop` the reader handles instead of `join`ing them, so the helper returns by its deadline even if a descendant escaped the group via its own `setsid`. Group-kill alone leaves the unbounded join; non-joining drain alone leaves the orphan process and its thread.
- Bump the `tmux-tools` rev pin in `Cargo.toml:28` to the fixed revision; this is a coordinated two-repo change.
- Add a regression using a run-as wrapper that forks a child inheriting stdout/stderr and delays the tmux command; assert the helper returns within its deadline and that no reader thread remains. Note that nine tmux/socket tests are EPERM-inconclusive under sandbox, so the test must not depend on real socket operations.
- Consider also wrapping the enable path at `src/api.rs:1424-1430` in `tokio::time::timeout_at(setup_deadline, …)` mirroring `:1395`, as defense in depth against any future primitive regression.

---

## MEDIUM

### M1. Compound extraction silently widens inherited agent access (FIXED)
> **Fix:** Extracted subflows now inherit useOrchestrator and a cloned agentDefaults so an explicit read_only survives extraction; parent goal deliberately left empty per scope semantics.
**Severity:** MEDIUM (top of band) — a pure editor gesture silently revokes a restriction the author explicitly set, with no prompt, diff, or warning; mechanically certain rather than conditional, and it fails toward *wider* access.
**Files:** `ui/src/lib/stores/workflowStore.svelte.ts:891-911` (`saveSelectionAsCompound`); `ui/src/lib/types/workflow.ts:278`; `ui/src/features/editor/GraphEditor.svelte:232-234` (trigger); `ui/src/features/editor/InspectorPanel.svelte:509`; `src/runtime.rs:2763`, `:2774`, `:4831`; `src/model.rs:~264` (`merge!`); `src/driver.rs:405-412`, `:615-617`, `:840-842`
**Description:** The `subflowDoc` object literal at `ui/src/lib/stores/workflowStore.svelte.ts:891-911` copies `cwd: active.cwd` but has no `agentDefaults` key and hardcodes `useOrchestrator: false`; `WorkflowDocument.agentDefaults` is optional (`ui/src/lib/types/workflow.ts:278`), so nothing type-flags the omission. Subflow scope is authoritative at dispatch — `agent_defaults: active_workflow.agent_defaults.clone()` (`src/runtime.rs:2774`, batch twin `:4831`) — so with no subflow defaults and no node override, `merge!(access_mode).unwrap_or_default()` (`src/model.rs:~264`) yields `AccessMode`'s `#[default] Execute` (`src/driver.rs:405-412`), producing skip-permissions for Claude (`src/driver.rs:615-617`) and `--full-auto` for Codex (`:840-842`). The loss is not limited to `accessMode`: `model`, `maxTurns`, `maxBudgetUsd`, `autoApprove`, `systemPrompt`, and `orchestrator` all merge through the same macro, and `useOrchestrator: false` is a second silent semantic change for orchestrator-enabled workflows. The trigger is a bare `prompt()` at `GraphEditor.svelte:234` with no preview or warning, and the store leaves selection on the compound node rather than drilling in, so the user never sees the new scope's config.

*Governing precedent:* `docs/issues/feature-tmux-panes-code-review-20260620-092605.md:92-94` M5 (FIXED) deliberately made the backend source `cwd`/`use_orchestrator`/`agent_defaults` from `workflow_for_cursor` rather than the root, ruling explicitly: "For `cwd`, inherit root when the subflow's `cwd` is empty … **keep strict per-scope values for `agent_defaults` and `use_orchestrator`**." That ruling is why `cwd` is copied here and defaults are not, is re-ratified at `...20260716-003531.md:204`, and is guarded by the passing test `subflow_dispatch_uses_subflow_scoped_cwd_defaults_and_orchestrator` (`src/runtime.rs:10453-10530`). The fix therefore belongs in the UI extraction path, not in the runtime. No prior finding covers compound extraction's failure to populate the new scope; the ~25 existing compound tests (`ui/src/lib/stores/workflowStore.test.ts:259-967`) assert only graph/prompt/variable rewiring.
**Fix:** Populate the generated subflow with the active workflow's effective configuration at extraction time.
- In the `subflowDoc` literal, add `agentDefaults: structuredClone($state.snapshot(active.agentDefaults))` when present, and set `useOrchestrator: active.useOrchestrator` rather than hardcoding `false`.
- **Do not copy `goal`**, contrary to the original fix text: `goal` is a per-scope prompt-context string (`workflow_goal: active_workflow.goal`, `src/runtime.rs:2763`), not a safety control, and copying the parent's goal into a reusable subgraph is semantically wrong. Leave `goal: ""`.
- Add a store-level regression that extracts a node under `agentDefaults.claude.accessMode = "read_only"` and asserts the generated child retains it; add a companion assertion for `useOrchestrator: true`.
- Accept the snapshot semantics: later parent edits will not propagate to the extracted child. That divergence is the model M5 already established for subflow scope and is not a defect of this fix.

---

### M2. Default Send nodes crash compound extraction and fail typechecking (FIXED)
> **Fix:** Compound extraction tolerates omitted Send config and initializes it before rewrites while preserving target and enter; typecheck gate now reports zero errors.
**Severity:** MEDIUM (top of band) — sole cause of a red typecheck gate, so it blocks merge outright, and the runtime crash is reachable from ordinary backend data; offset by a trivial repair and the need for one save/reload cycle to hit it.
**Files:** `ui/src/lib/stores/workflowStore.svelte.ts:175`, `:197` (compare correct sibling handling at `:171`, `:190-193`); `ui/src/lib/stores/workflowStore.test.ts:576-577`; `ui/src/lib/types/workflow.ts:231`, `:581`; `src/model.rs:430-443`, `:652`
**Description:** `ui/src/lib/stores/workflowStore.svelte.ts:175` reads `node.kind.sendConfig.text` and `:197` writes `node.kind.sendConfig.text = text`, both unconditionally, while `NodeKind` declares `sendConfig?: SendConfig` (`ui/src/lib/types/workflow.ts:231`). Two lines above, the sibling `run_agent` branch handles this correctly — `node.kind.runAgentConfig?.prompt ?? ""` (`:171`) with a `?? { killAfter: true }` default before writing (`:190-193`). Verified by running the gate: `npm run typecheck` reports **exactly 4 errors and no others** — `workflowStore.svelte.ts:175:43`, `:197:38`, `workflowStore.test.ts:576:12`, `:577:12`, all `'…sendConfig' is possibly 'undefined'` — so this finding alone is why `just typecheck` / CI is red on this branch. At runtime, a Send node whose text is still empty round-trips from the backend without `sendConfig`, and "Save as compound" over it throws `TypeError` in `compoundNodePromptTexts`, blanking the canvas since `ui/src` contains no `<svelte:boundary>`.

*Premise verified — and note it differs from H4:* the optional declaration here is **truthful**. `src/model.rs:652` carries `#[serde(default, rename = "sendConfig", skip_serializing_if = "is_default")]` over `SendConfig::default() = {target: None, text: "", enter: true}` (`:430-443`), so the backend genuinely omits a default-valued config. This is unlike `subflowConfig`/`decideConfig`/`batchConfig` in H4, which are always emitted — hence the two findings correctly take different fixes. `normalizeWorkflowNode` does not backfill send (`ui/src/lib/types/workflow.ts:581`), unlike capture/kill at `:588-596`.

*Introduced on this branch:* both deref sites and the two test assertions came from `ba9d6e1` ("Harden pane-stream lifecycle and compound extraction boundaries") as the fix for `20260716` M12, were restructured into `compoundNodePromptTexts`/`applyCompoundPromptField` by the S9 fix, and were touched again in `05eb37c`. `sendConfig`'s optionality is long-standing (`12ac692`) — the fix author introduced the mismatch, not the type.
**Fix:** Guard both sites with optional chaining, matching the sibling `runAgentConfig` handling in the same function.
- At `:175`, read `node.kind.sendConfig?.text ?? ""`.
- At `:197`, write `node.kind.sendConfig = { ...(node.kind.sendConfig ?? { text: "", enter: true }), text }` so the spread preserves `target` and `enter` rather than discarding them.
- Narrow or initialize the optional config in the two assertions at `workflowStore.test.ts:576-577`.
- Confirm `npm run typecheck` reports 0 errors afterwards.
- Do **not** make `sendConfig` required and backfill it in the normalizer (the H4 approach): because `skip_serializing_if` means the field really is absent on the wire, a required type would be a lie for any ingress that bypasses `normalizeWorkflowNode` — the exact flaw H4 exists to correct.
- Follow-up worth tracking separately: `spawnConfig` and `waitConfig` carry the same `skip_serializing_if` and are latent twins of this bug.

---

### M3. Duplicated pane spawn/registration path between the oneshot and interactive routes (dismissed: premise is factually false — `src/tmux_exec.rs:1167-1190` contains no spawn or registration code, it installs a `TmuxInvocation` and delegates to the same `run_agent_interactive` (`:915`) the interactive route uses; spawn/registration exists only at `PaneGuard::acquire:397` → `spawn_pane:2056`, which the oneshot also reaches. The cited "full path" `:2149-2192` is `build_agent_command`'s argv assembly, shared by both. The only real difference that is a defect is `timeout_secs: None` at `:1176`, which is M8 and independently fixable; the missing `register_tmux_session` (`:517`) belongs with M4. The "promoted from the advisory smells track" provenance is unsupported — no `SMELLS-LEDGER.md` row covers these lines — and "CLAUDE-12" appears nowhere in the repo; the abort asymmetry it alludes to is already fixed and regression-tested at `src/runtime.rs:9926-9967`. The thin-adapter shape is deliberate, extended by parameter four times across prior reviews (`20260608_part_2:48` "single choke-point", `20260610:98`, `20260620-092605:225`, `20260716-003531:44`).)

---

### M4. Stale session registrations accumulate and can target a newer run (FIXED)
> **Fix:** The reaper reconciles absent registrations only after successful listings across each invocation group, preserves uncertain rows, and excludes names owned by nonterminal runs.
**Severity:** MEDIUM (upper band) — under per-run sockets every orphaned row adds a `spawn_blocking` + `tmux list-sessions` subprocess to **every boot**, so growth is O(runs) process spawns at startup rather than a cosmetic row count; the cross-run kill destroys a live user session.
**Files:** `src/tmux_exec.rs:450-463` (`PaneGuard::drop`), `:550-556` (`clear_key`/`block_on`), `:678-681` (spawn node), `:2083-2087`, `:2482-2506` (`stable_session_name`); `src/runtime.rs:1155-1159`, `:1592` (`dismiss_run`), `:6344-6355`, `:6371`, `:6383-6389`, `:6418`, `:6507-6510`, `:6517`, `:6523-6537`, `:6554-6569` (`kill_active_run_panes`); `src/storage.rs:547-561` (`list_reapable_tmux_sessions_for_run_id`)
**Description:** `run_tmux_sessions` rows are deleted on only two paths — reaper-after-confirmed-kill (`src/runtime.rs:6523-6537`) and terminal cleanup of still-registered active panes (`:6371`). Guard teardown and the abort path `kill_active_run_panes` (`:6554-6569`) both kill without deregistering: `PaneGuard::drop` (`src/tmux_exec.rs:450-463`) calls only `active_pane.clear_key()`, which removes the entry from `active_panes` (`src/runtime.rs:1155-1159`) — and since terminal cleanup derives its `killed_sessions` *from* `active_panes` (`:6344-6355` → `:6371`), the guard has already erased the only record that would have triggered `remove_tmux_sessions`. The reaper cannot compensate, because `:6507-6510` iterates *live* sessions and `continue`s on any name not present, so a row whose session is gone is never visited. There is no TTL, no `created_at`, no prune, and no `LIMIT` on the reapable query. Separately, `list_reapable_tmux_sessions_for_run_id` (`src/storage.rs:547-561`) filters only on `runs.status IN ('completed','failed','aborted','restarted')` with no exclusion of names registered to nonterminal runs, and `:6418` skips rows by `run_id` only — so `dismiss_run` (`:1592`) on an old terminal run can reach `:6517` and kill a live run's identically-named session.

*Corrections to the original write-up:* UUID suffixing does **not** refute the cross-run half, but the colliding names do not come from the path cited. `spawn_pane` (`src/tmux_exec.rs:2083-2087`) uniquifies only when no explicit name is given; `PaneGuard` builds its `SpawnConfig` with `session_name: None` (`:1002-1009`, `:1244-1251`), so `run_agent` sessions are always UUID-suffixed and can never collide. The colliding names originate from the **`spawn` node** (`:678-681`), whose explicit `SpawnConfig.session_name` goes through `stable_session_name` → `normalized_session_name(name, None)` (`:2482-2506`) with no UUID. The cross-run kill therefore requires an explicit `spawn`-node `sessionName` reused across runs, a shared invocation, and a reaper pass during the live window — narrower than stated, and impossible for `run_agent`. Also, the "`Drop` cannot reach async DB code" concern does not hold: `ActivePaneRegistration` carries a `tokio::runtime::Handle` and `clear_key` already does `handle.block_on` (`:550-556`).

*Relationship to H2:* **complementary — implement together, H2 first.** H2's finalized fix supplies the `has-session` absent/present probe that this reconciliation consumes. H2 makes accumulation marginally worse (rows are now retained on an unconfirmed kill), which is correct in isolation and tolerable only because this reconcile pass sweeps them on the next reap.

*Prior history:* not a regression. `20260705-074320` M3 created the table with a "DELETE at cleanup" contract, and S2 introduced `PaneGuard` with `Drop` explicitly scoped to "kill + `clear_key`" — DB deregistration was never in its contract. `20260716-003531` M7 recorded that registration is removed only after a successful kill and that the table has no TTL, but did not file it. No prior finding covers the cross-run name collision.
**Fix:** Make the reaper authoritative by reconciling the database against observed tmux state — the same discipline H2 establishes for kills.
- In the per-invocation reaper loop, after `list_silverbond_tmux_sessions` succeeds, delete every terminal registration in that invocation group whose session name is **absent** from `live_sessions`, replacing the bare `continue` at `src/runtime.rs:6507-6510`. Gate the deletion on a successful listing so a transient tmux failure cannot wipe live registrations.
- Add a `NOT EXISTS` anti-join to `src/storage.rs:547-561` excluding any session name registered to a nonterminal run, so a stale terminal row can never target a live run's session.
- Do **not** add a DB write to `PaneGuard::drop`: `Drop` does not run on SIGKILL or `abort()` (recorded `20260610`), so it can never be the guarantee, and adding it creates a second source of truth for no additional correctness.
- Tests: zero residual rows after an ephemeral agent run followed by a reap; a terminal stale row sharing a name with a live nonterminal run is not killed; a failing `list-sessions` deletes nothing. Note nine tmux/socket tests are EPERM-inconclusive under sandbox, so prefer the fake-tmux harness.
- Side benefit to verify: the pass should also clean rows orphaned by the abort path and by SIGKILL, and heal databases already carrying accumulated rows.

---

### M5. A new run can wait for the pane selected in the previous run (FIXED)
> **Fix:** beginRunStream clears selectedPane to the "active" sentinel and drops runObservability through a shared helper that resetRun now delegates to, ending cross-run pane staleness.
**Severity:** MEDIUM (lower band) — *lowered*: a dead terminal with a visible workaround, not data loss; the trigger needs a resume/restart or manual pane pick rather than every run, and it self-heals when the same node runs again.
**Files:** `ui/src/lib/stores/workflowStore.svelte.ts:994-1001` (`beginRunStream`), `:1007-1018` (`resetRun`), `:1035-1044` (`setRunObservability`), `:1061` (`applyRunEvent`); `ui/src/app/AppShell.svelte:147`, `:156`, `:202`, `:211`, `:220`; `ui/src/features/runtime/PaneTerminal.svelte:149`, `:184-194`; `src/api.rs:60`, `:449-451`, `:1251-1265`, `:1281-1295`; `src/runtime.rs:1171-1194`, `:1385`
**Description:** `AppShell.svelte:202` starts every run, resume, and restart via `store.beginRunStream()` (`workflowStore.svelte.ts:994-1001`), which only aborts the SSE controller, bumps `runEpoch`, and installs a new controller — it touches neither `selectedPane` nor `runObservability`. Only `resetRun()` (`:1007-1018`) clears them, and it is called solely from `openWorkflow` (`AppShell.svelte:147`) and `handleCreate` (`:156`). `setRunObservability` (`:1035-1044`) has exactly two branches — `panes?.length === 1` and `panes && panes.length > 1` — with no empty/absent case, and `src/api.rs:449-451` omits the `panes` field entirely when the list is empty while `src/runtime.rs:1385` spawns workflow execution *after* `start_run` returns, so a fresh run's kickoff response never carries panes. A stale exact pane key therefore survives into the new run and reaches `streamPane(activeRunId, pane, …)` at `PaneTerminal.svelte:149`, leaving the terminal blank while the backend holds the request pending.

*Corrections to the original write-up:* the backend **does** re-resolve on every pane-registration notify (`src/api.rs:1281-1295`, `src/runtime.rs:1171-1194`) — but only for the requested key, so the failure needs the new run never to register a pane under that key, and re-running the *same* workflow node typically self-heals. The hold is also not a guaranteed 120 s: `run_still_awaiting_pane` (`src/api.rs:1251-1265`) returns `PaneUnavailable` immediately once the run reaches a terminal status, bounding it at min(run duration, 120 s). Reachability is narrower than "the common case": `selectedPane` can only *become* a stale exact key via a resume/restart kickoff that did return panes (`AppShell.svelte:211` is the only `setRunObservability` call site, and observability is never refreshed mid-run) or via a manual dropdown pick (`PaneTerminal.svelte:188`) — a plain Run→Run sequence never auto-populates it. Finally, the claim that this supersedes a "separately-reported stale-attach-command symptom" is unsupported: no such report exists anywhere in `docs/`.

*Prior history:* this is exactly the hole a prior fix knowingly left. `docs/issues/code-review-20260608_part_4_of_4.md:127` R3-6 (FIXED) added the reset to `resetRun()`/`setWorkflow()`; `docs/issues/feature-tmux-panes-code-review-20260620-092605.md:95` M6 (FIXED) covered the same class but its fix note at `:99-103` **explicitly dropped the "reset at run start/resume" half**, keeping only the membership check. `...20260716-003531.md:212` M9 then introduced `beginRunStream` as cancellation-only by design, and S8 (`:645`) routed all three run entry points through it instead of `resetRun`. The 120 s pending hold is itself a deliberate choice over error-closing (`...20260705-074320.md:90`, M4 FIXED).
**Fix:** Clear run-scoped selection state where the run lifecycle begins.
- In `beginRunStream` (`ui/src/lib/stores/workflowStore.svelte.ts:994-1001`), set `selectedPane = "active"` and `runObservability = null`, so all three lifecycle entries through `AppShell.svelte:202` start from the sentinel. Have `resetRun` delegate to that shared clearing rather than duplicating it.
- This is effective because `"active"` re-resolves dynamically as panes register (`src/runtime.rs:1180-1184` single-pane, `src/api.rs:2008-2021` candidate fallback), unlike a stale exact key.
- Accept the tradeoff: resuming or restarting the *same* run now discards a deliberate user pane choice. The dropdown at `PaneTerminal.svelte:184-194` remains available to re-select.
- Add the missing cross-run regression: kickoff payload with no `panes`, first registered pane under a different key, asserting the socket opens against `"active"`. `workflowStore.test.ts:974-1024` currently covers only the 1-pane, >1-pane, `resetRun`, and `setWorkflow` paths.
- Worthwhile separate follow-up (not part of this fix): `runObservability` is never refreshed after kickoff, so the pane dropdown and attach command also go stale mid-run. Refreshing it from `applyRunEvent` as panes register would address that independently.

---

### M6. `extraArgs` are appended after access-mode flags, defeating the documented access ceiling (FIXED)
> **Fix:** Added normalized, abbreviation-aware extra-argument validation for Spawn and RunAgent including Codex config overrides; argv ordering unchanged, errors surfaced in the inspector issue list.
**Severity:** MEDIUM (lower band) — *lowered*: author-only configuration footgun with no third-party injection path and no shipped template exercising it; still a silent, real downgrade of a declared access ceiling.
**Files:** `src/tmux_exec.rs:2141-2183` (`build_agent_command`, `:2146`, `:2158-2162`, `:2161`, `:2171`, `:2178`), test `:3332-3335`; `src/driver.rs:687-688`, `:823-848`; `src/model.rs:419`, `:520`; `ui/src/features/editor/InspectorPanel.svelte:840`, `:1320`
**Description:** `build_agent_command` assembles argv as `once(binary).chain(session_args.args).chain(extra_args.iter().cloned())` (`src/tmux_exec.rs:2158-2162`), appending `cfg.extra_args` verbatim after the driver's session args on all three branches (`:2161`, `:2171`, `:2178`) with no filtering anywhere. Access args are emitted inside the driver — claude at `src/driver.rs:688`, codex `--sandbox <mode> -a <policy>` / `--full-auto` at `:823-848`. An author who declares `accessMode: read_only` and also types a skip-permissions entry or `--sandbox danger-full-access` into the free-text `extraArgs` textarea (`InspectorPanel.svelte:840`, `:1320`) gets the later flag: claude-code's skip-permissions flag is a boolean that simply turns on, and codex is clap v4 `ArgAction::Set`, so a second `--sandbox`/`-a` overwrites rather than erroring. `extra_args` flows verbatim from workflow JSON (`src/model.rs:419`, `:520`) through `src/tmux_exec.rs:1006`, `:1246` to `:2146` with no validation at any point.

*Correction to the original write-up:* the comment cited as the broken invariant — `// Access mode is the ceiling; explicit tool lists only refine it.` at `src/driver.rs:687-688` — sits **inside** `ClaudeDriver::build_session_args` and governs `allowed_tools`/`disallowed_tools` ordering relative to access args. It says nothing about `extra_args` and is not false as written; the review reads it wider than its scope. Mitigating: `extra_args` has **no variable interpolation** anywhere in `src/model.rs`/`src/runtime.rs`/`src/tmux_exec.rs` — it is verbatim author JSON with no third-party injection channel — and no shipped template uses `extraArgs`. One combination may fail closed rather than escalate: `--full-auto` (emitted for `Execute`) plausibly carries a clap `conflicts_with` against `--sandbox`, so that pairing fails to launch; the `read_only` → unrestricted path, which is the one that matters, does escalate.

*Prior history — "extra args last" is a deliberate decision, made twice:* `docs/issues/code-review-20260608_part_2_of_4.md:73` (F5, HIGH, FIXED) specifies exactly this order, so that "`build_session_args` [is] the single source of all dynamic config/safety flags"; the test at `src/tmux_exec.rs:3332-3335` ("driver-rendered session args should precede caller extra args") encodes it, added in `05eb37c` alongside the `driver.rs:687` comment. `docs/issues/feature-tmux-panes-code-review-20260620-092605.md:176` then adjudicated: "`command`/`extra_args` stay free-form (intended); their blast radius is now bounded by the enforced identity." Reordering argv would reverse F5; the chosen fix deliberately does not.
**Fix:** Reject access-affecting flags in `extra_args` at workflow-validation time, preserving the existing argv ordering.
- Add a validator in `src/model.rs` alongside the existing `validate_run_as_config`, applied to both `SpawnConfig.extra_args` (`:419`) and `RunAgentConfig.extra_args` (`:520`).
- Implement it as **normalize-then-match**, not naive string equality: split each entry on `=` to catch `--flag=value`, prefix-match against a per-agent flag set to catch clap's unambiguous long-flag abbreviations (e.g. `--sand=…`), and reject codex `-c` config-override keys touching `sandbox`/`approval` — an equivalent escalation channel the original fix list omits entirely.
- Cover at minimum: the skip-permissions flag, `--permission-mode`, `--allowedTools`, `--disallowedTools`, `--sandbox`, `-a`, `--ask-for-approval`, `--full-auto`, and the bypass-approvals-and-sandbox variant.
- Surface the rejection at authoring time in the UI so the author sees why the workflow is invalid, rather than discovering it at spawn.
- Do **not** reorder argv to place access args after `extra_args`: that reverses F5's finalized "single source of all safety flags" design, breaks the `:3332-3335` test contract, leaves contradictory duplicate flags in argv, and would break any `extra_args` that legitimately ends in a positional or `--`.
- Add regressions for each blocked form (`--flag value`, `--flag=value`, abbreviated, and `-c sandbox_mode=…`), plus a positive case confirming benign flags like `--model` still pass.

---

### M7. `run_decide_node` discards the agent's failure/timeout/abort status (FIXED)
> **Fix:** Decide now carries the agent NodeResult through the outcome overlay, normalizes the timeout sentinel to -2, and maps Timeout via exit code or error_type, preserving metadata.
**Severity:** MEDIUM (upper band) — a timed-out Decide can silently route down a real branch on a truncated answer, the same silent-wrong-answer class as the already-fixed R1-2/F9 pair; metadata reset additionally destroys telemetry on every decide, including successful ones.
**Files:** `src/runtime.rs:3843`, `:3860-3877`, `:4081-4112` (`select_decide_outcome`), `:4189-4195`, `:4221`, `:4237-4245`, `:4251` (`apply_join_result`), `:5886`; `src/tmux_exec.rs:1101-1116` (interactive timeout), `:1104` (`-1` sentinel), `:1138-1144`, `:2452-2469` (`aborted_result`), `:763`, `:1055`, `:2437` (`-2` sentinel sites)
**Description:** `run_decide_node` discards the entire agent `NodeResult` except `output` (`src/runtime.rs:3843`) and reconstructs a fresh one at `:3860-3877` with `..Default::default()`. The interactive-poll timeout path builds `failed_result("Timeout waiting for agent response", -1, …)` and then sets `result.output = response.output` — the **partial capture** — plus `error_type = "timeout"` (`src/tmux_exec.rs:1101-1116`). That partial capture flows straight into `select_decide_outcome`, whose word-boundary regex matching (`:4081-4112`) can succeed on it, yielding `success = true` and `exit_code = 0` (`:3861-3864`). The user therefore sees one of two wrong outcomes: the run routes down a real branch as if the agent had answered — no error, no timeout event — or outcome selection fails and the node reports the misleading `"LLM response did not match any decide outcome: …"` classified as `Failure`, so `on_timeout` collector/split semantics (`:5886`) receive the wrong status. `aborted_result` (`src/tmux_exec.rs:2452-2469`) likewise carries a partial capture, so a matching abort capture can advance the run one node past the abort before the loop's next `is_aborted` check.

*Two defects the original write-up missed:* (1) the `..Default::default()` reconstruction resets `metadata` on **every** decide, including successful ones, losing `agent_session_id` and all cost/token telemetry that `node_done` events (`:4189-4195`) and `NodeExecutionLog.metadata` (`:4221`) depend on; (2) the interactive timeout sentinel is **`-1`** (`src/tmux_exec.rs:1104`), not `-2` — `-2` appears only at `:763`, `:1055`, `:2437` — so even a perfect pass-through of the agent result would still map to `Failure` rather than `Timeout`. Any fix must normalize the sentinel or key off `metadata.error_type`.

*Scope corrections:* the abort half is **already covered** — `apply_join_result:4237-4245` consults `ctx.registry.is_aborted(run_id)` before `metadata.error_type`, which is why `decide_abort_returns_promptly_and_kills_pane` (`:9922`) passes for reasons independent of this finding. And the reviewer's early-return concern is unfounded in the other direction: there is no path where the oneshot reports `success: false` while legitimately carrying a complete answer, since normal completion always returns `success: true, exit_code: 0` (`src/tmux_exec.rs:1138-1144`) and the pane kill happens after capture.

*Coupling:* **M7 and M8 must be fixed as a single change.** Today `run_tmux_oneshot` passes `timeout_secs: None`, so the timeout path is largely dormant for Decide; M8 makes it reachable, and fixing M8 alone strictly increases this finding's blast radius.

*Prior history:* no prior finding covers status/metadata discard. Adjacent fixes: `code-review-20260608_part_1_of_4.md:66` R1-2 (FIXED, decide substring misrouting → word-boundary + tie→`None`), `..._part_2_of_4.md:75` F9 (FIXED, prompt echo → `PromptCaptureMarkers`, consistent with this review's adjudication note), `feature-tmux-panes-code-review-20260620-092605.md:218` M17 (FIXED, decide abort latency → the `select!`/abort-token plumbing at `:3819-3841`), `...20260716-003531.md:277` L4. Open sibling in this review: L9.
**Fix:** Carry the agent's `NodeResult` forward and overlay only the outcome, rather than rebuilding it.
- Keep `result` as the base instead of constructing a fresh `NodeResult` at `src/runtime.rs:3860-3877`. On `!result.success`, return it with `resolved_prompt` and `parsed_output` filled in so the UI still shows the captured text.
- On success, mutate `output`/`stderr`/`success`/`exit_code` from `select_decide_outcome` while preserving `metadata`, `raw_output`, and the cost/token fields — this ends the telemetry loss on the success path.
- Normalize the timeout sentinel: either change `src/tmux_exec.rs:1104` from `-1` to `-2`, or make `apply_join_result` (`:4251`) test `metadata.error_type == "timeout"` rather than the numeric sentinel. Without this, `CursorTerminalStatus::Timeout` remains unreachable for Decide no matter how the result is propagated. If changing the sentinel, audit the three other `-2` sites (`src/tmux_exec.rs:763`, `:1055`, `:2437`) for consistency.
- Land this together with **M8** in one change.
- Add regressions for: a decide timeout whose partial capture *does* match an outcome (must not route down that branch), a decide timeout that reaches `CursorTerminalStatus::Timeout`, and a successful decide that retains `agent_session_id` and cost metadata.

---

### M8. Decide nodes ignore their configured timeout entirely (FIXED)
> **Fix:** Threaded timeout_secs through run_tmux_oneshot; decide and preview sites pass timeout_for_node while the two orchestrator sites keep None.
**Severity:** MEDIUM — loss of the configured deadline with silent fallback to a much longer generic cap, compounded by a UI that renders the setting as if it worked.
**Files:** `src/runtime.rs:3810` (decide call site), `:1748` (preview call site), `:6971`, `:7004` (orchestrator call sites), `:2110-2128` (`timeout_for_node`, Decide arm at `:2120`), contrast `:3658`/`:3706` (`run_agent_node` pattern); `src/tmux_exec.rs:1167-1174` (signature), `:1180`, `:948-951` (300 s default); `ui/src/features/editor/InspectorPanel.svelte:972`, `:978-987`
**Description:** `run_decide_node` never computes `timeout_for_node(&node)` before calling `run_tmux_oneshot` at `src/runtime.rs:3810`, and `run_tmux_oneshot` has no timeout parameter to receive one — its signature (`src/tmux_exec.rs:1167-1174`) omits it entirely and it forwards a literal `None` as `run_agent_interactive`'s 4th argument at `:1180`. `node.timeout` is therefore parsed and validated for `NodeKind::Decide` (`timeout_for_node`'s catch-all arm, `src/runtime.rs:2120`) but never consumed, so every Decide node silently falls back to the generic 300 s cap in `run_agent_interactive` (`src/tmux_exec.rs:948-951`) regardless of what the author configured.

*User-visible broken promise not noted in the original write-up:* `InspectorPanel.svelte:978-987` renders the generic "Timeout" input bound to `selectedNode.timeout` inside the "Guards & retry" section (`:972`), and that section is **not** gated by node kind — so an author can add it to a Decide node, set a timeout, and observe no effect whatsoever.

*Scope corrections:* the original fix text ("compute `timeout_for_node` in `run_decide_node` and pass it into `run_tmux_oneshot`") is under-specified, since there is no parameter to pass it into. The preview call site (`src/runtime.rs:1748`) has a real `node` in scope and ignores its timeout for the same reason — the same defect, unreported. The two orchestrator call sites (`:6971` `run_orchestrator_refinement`, `:7004` `run_orchestrator_branch`) are meta-calls that rewrite a prompt or choose a branch rather than executing the node's own agent run, and both already pass `config: None`, so `None` is correct for them. The cross-reference to M3 does not hold — M3 was dismissed as factually unfounded; this is an independent missing-parameter defect. No prior review deliberately omitted decide's timeout, and no test asserts decide timeout behaviour.

*Coupling:* **land as one change with M7.** M8 makes the timeout path reachable for Decide; without M7 the newly-firing timeout is laundered into a wrong branch or a bogus outcome-selection failure.
**Fix:** Thread an explicit timeout through the oneshot seam and honour it wherever a node's own agent run is being executed.
- Add `timeout_secs: Option<u64>` to `run_tmux_oneshot`'s signature (`src/tmux_exec.rs:1167`) and forward it at `:1180` in place of the literal `None`.
- Pass `timeout_for_node(&node)` from the decide site (`src/runtime.rs:3810`) and from the preview site (`:1748`).
- Pass explicit `None` from the two orchestrator sites (`:6971`, `:7004`).
- Add **no** new default: rely on `run_agent_interactive`'s existing 300 s fallback (`src/tmux_exec.rs:951`), exactly mirroring `run_agent_node`'s `Option<u64>` pass-through at `src/runtime.rs:3658`/`:3706`. Introducing a decide-specific default would create a second fallback location diverging from every other node type.
- Implementation note: at the decide site the timeout must be captured by value alongside the cloned `agent_config`, since `node` is moved into the `spawn_blocking` closure — compute it before the closure.
- Add the missing regression: a Decide node with a short configured `timeout` must time out at that deadline rather than at 300 s.

---

### M9. Collector release with no live waiter fabricates a phantom cursor (FIXED)
> **Fix:** Collector barriers snapshot the first terminal arrival, materialize it before release, and preserve policy, variables, call frames, scoped results, and all-failure continuation.
**Severity:** MEDIUM (HIGH-leaning) — *raised*: in subflow scope it silently aborts the entire run and discards the subflow's results, and the trigger ("both branches of a 2-way split fail") is an ordinary flaky-agent outcome rather than an exotic authored shape.
**Files:** `src/runtime.rs:425-432` (`CollectorBarrierState`), `:1907-1919` (`reset_released_collector_barrier`), `:1998-2008` (`collector_barrier_scope`), `:2966`, `:4355-4357` (`complete_subflow_if_at_exit`), `:5632-5634` (sole `waiting_cursor_ids` append), `:5655-5664` (readiness), `:5684-5698` (summary), `:5706-5711`, `:5742-5748`, `:5834-5851` (phantom construction), `:5859`, `:5863-5864` (normal path), `:5910`, `:5911-5939`, `:5941`, `:5963`
**Description:** `waiting_cursor_ids` is appended in exactly one place — `src/runtime.rs:5632-5634`, inside `handle_collector_entry`'s *live*-arrival path. `handle_terminal_cursor_status` (`:5911-5939`) records the arrival via `insert_collector_arrival`, never touches `waiting_cursor_ids`, and then deletes the cursor at `:5963`. Since barrier readiness at `:5655-5664` checks only `required_inputs ⊆ arrivals`, a barrier whose arrivals all came through the terminal path releases with an empty waiting list: `representative_cursor_id` falls back to `new_cursor_id()` (`:5706-5711`), `representative_state` is `None` (`:5742-5748`), and `:5834-5851` fabricates a `CursorState` with `split_family_ids: Vec::new()`, `call_stack: Vec::new()`, and `var_map: checkpoint.var_map.clone()` — the run-global map rather than the cursor-scoped map the normal path carries at `:5863-5864`. In subflow scope the barrier is keyed to the call frame (`collector_barrier_scope`, `:1998-2008`), so the release targets a subflow-local node id on a cursor with no `call_stack`; resolution runs against the root workflow, misses, and `:2966` emits `Node "…" not found — aborting`, setting `RuntimeStatus::Aborted` for the whole run. `complete_subflow_if_at_exit` returns `false` immediately (`:4355-4357`, cursor absent from `active_cursors`), so the frame is never popped and the parent `call` node receives nothing — regressing the already-fixed R1-6. In root scope the empty `split_family_ids` makes `:5941` compute `fail_run = true` on any later downstream failure, failing the run against a `BestEffortContinue` policy the normal path (`:5859`) would have honoured.

*Reachability, raised:* the minimal repro is the shape of the existing test `best_effort_continue_keeps_run_completable_after_branch_failure` (`:11128`) with `branch-a` also scripted to fail — split → two tasks → collector → after. `DrainThenFail` reaches it as well (`:5946` only sets `force_failed`; the run is not marked failed until `finalize_run`); `FailFastCancel` is largely shielded because `terminal_reason` is set and the main loop breaks. No existing test covers zero surviving branches — `:11128`, `:11182`, and `:11237` all keep one branch alive.

*Semantics, settled:* the collector builds a summary carrying `failed`/`timedOut`/`cancelled` counts and sets `success: true` (`:5684-5698`), and `BestEffortContinue` means continue — so downstream work **must** run even with zero survivors. Suppressing the cursor is therefore not a neutral half-measure.

*Relationship to H5:* **same edit site, distinct defect — partial overlap only.** H5's finalized fix covers the empty `split_family_ids`; it does **not** address the empty `call_stack` (subflow abort and lost frame) or the wrong `var_map`, and the root-scope half is not moot after H5 either. Land both together.

*Prior history:* no prior finding covers `waiting_cursor_ids` or the representative fallback. Adjacent FIXED items: R1-5 (barrier scoping by `frame_id`), R1-6 ("thread the correct node/`NodeResult` into the collector representative-cursor case" — which this bug defeats), H4/M14 (barrier reset and eviction), M19 (merge-key collisions). Nothing here was dismissed or deferred.
**Fix:** Carry a real cursor snapshot on the barrier and seed the representative from it instead of fabricating one.
- Add `representative_snapshot: Option<CursorState>` to `CollectorBarrierState` (`src/runtime.rs:425-432`), populated in `handle_terminal_cursor_status` from the `cursor` clone already taken at `:5910` before removal, and cleared by `reset_released_collector_barrier` (`:1907-1919`).
- At `:5834-5851`, build the released cursor from that snapshot — restoring `call_stack`, `var_map`, and `split_family_ids` — retaining `new_cursor_id()` only for the cursor id itself.
- Store a single snapshot rather than one per arrival to bound checkpoint growth, and define a deterministic selection rule when branches carry divergent `var_map`s (e.g. first terminal arrival wins); document that a snapshot may be stale if its branch mutated globals after capture.
- Land together with **H5**: both edit `:5834-5851` and share the `#[serde(default)]` / checkpoint-version bump, so M9 rides H5's migration at no extra cost.
- Do **not** simply skip the `active_cursors.push` when the waiting list is empty: that silently drops all post-collector work, contradicting both `BestEffortContinue` and the collector's own `success: true` contract, and leaves the parent `call` stalled.
- Add regressions: a 2-way split under `BestEffortContinue` where **both** branches fail must continue past the collector with the correct downstream node; the same shape inside a subflow must complete the frame and return results to the parent `call` rather than aborting; and a later downstream failure must be judged against the configured policy rather than failing the run.

---

### M10. `O(call_nodes × subflow_size)` validation blowup blocks a tokio worker (FIXED)
> **Fix:** Precomputed SubflowFacts threaded through validation to remove the per-call-node graph rebuild, plus post-hydration 50k node / 100k edge ingress caps.
**Severity:** MEDIUM — mostly self-inflicted (a legitimate large workflow already hangs the UI for seconds on every validate) plus a local-process DoS; bounded by the loopback bind and the multi-worker runtime, but reachable with no unlock gate.
**Files:** `src/model.rs:944-960` (`WorkflowV3::graph`), `:1326-1363` (`validate_workflow_input_bounds`), `:1365` (`validate_graph_body`), `:1484` (per-node dispatch), `:2000-2021` (`validate_subflow_catalog`), `:2061`, `:2078-2083`, `:2149-2153`; `src/api.rs:168` (`require_same_origin_mutation`), `:546-548`, `:723-732`; `src/main.rs:21`
**Description:** `validate_graph_body` loops every node and dispatches each `Subflow`/`Call` to `validate_subflow_node_config` (`src/model.rs:1484`), which rebuilds the callee's entire graph via `subflow.graph()` (`:2061`) — three `HashMap`s over all its nodes and edges (`:944-960`) — then scans every callee node for terminals with `outgoing_for` (`:2078-2083`) and rebuilds a `BTreeSet` of its variables (`:2149-2153`). All of this is per call node, giving `Σ(call nodes) × |callee|`; `validate_subflow_catalog` (`:2000-2021`) re-runs `validate_graph_body` for each subflow, so calls nested inside subflows amplify further. Measured on a release build: 200 calls / 500-node subflow (97 KB JSON) = 132 ms; 400/1000 (196 KB) = 874 ms; 800/2000 (398 KB) = **5.09 s**. Both HTTP entry points run it inline on the async task with no `spawn_blocking` (`src/api.rs:548`, `:732` — the latter after a deep `clone()`), `hydrate_saved_subflows` runs *before* `enforce_workflow_input_bounds` (`:546`/`:547` and `:723`/`:731`), and the existing bounds count only subflows and call edges, never nodes (`src/model.rs:1326-1363`). No `DefaultBodyLimit` exists anywhere in `src/`; axum 0.8's `Json` default is 2 MB.

*Worse than the original write-up:* hydration is an **amplifier**. A ~400 KB payload of 4096 trivial call nodes — exactly the `MAX_SUBFLOW_CALL_EDGES` ceiling — pointing at a single **saved** 2000-node subflow pulls that body from disk and extrapolates to roughly **26 s of CPU per request**.

*Reachability, refined:* single-user desktop app bound to `127.0.0.1:3333` (`src/main.rs:21`) with no authentication anywhere; `require_same_origin_mutation` (`src/api.rs:168`) rejects header-less clients, blocking cross-site browser pages and naive `curl`. But that middleware is CSRF protection, not authn — any local process can set `Origin: http://localhost:3333` and pass — and `/api/validate-workflow` carries **no unlock gate**, unlike `create_run` (`:724`). The runtime is `#[tokio::main]` default (num_cpus workers), so a single request degrades rather than kills; a loop of them saturates every worker including the health endpoint.

*Prior history:* direct precedent in the same file. `docs/issues/feature-tmux-panes-code-review-20260705-074320.md:160-169` M10 (FIXED) addressed exponential subflow-cycle detection under the same threat framing, and its fix introduced `MAX_WORKFLOW_SUBFLOWS`/`MAX_SUBFLOW_CALL_EDGES` explicitly as "defense-in-depth … bounds V regardless of algorithm" — sized for that algorithm's V, with a node-count cap never considered. That review also observed the missing `spawn_blocking` and **deliberately declined it**, choosing to fix the algorithm instead. `...20260716-003531.md:307-316` S2 (FIXED) later closed a bounds bypass via nested catalogs. This review's own smells list files the same code as Feature Envy, proposing a `validate_call_against` method on the subflow type.
**Fix:** Make validation linear by precomputing per-subflow facts once, and bound total input after hydration.
- In `validate_workflow`, build a single `BTreeMap<&str, SubflowFacts>` where `SubflowFacts` holds the subflow's node-id set, terminal-node id list, and variable set.
- Thread that map through `validate_graph_body` and `validate_subflow_node_config` so each call node performs only string comparisons against precomputed sets, never rebuilding a graph. All borrows are immutable from one `&WorkflowV3`, so lifetimes stay trivial; prefer `SubflowFacts` over carrying whole `WorkflowGraph`s.
- Add a total node+edge cap to `validate_workflow_input_bounds` (`src/model.rs:1326-1363`) — suggested 50k nodes / 100k edges, roughly 25× the largest plausible authored workflow — and count it **after** hydration so the saved-subflow amplification path is covered.
- Do **not** wrap `validate_workflow` in `spawn_blocking`: it relocates the CPU burn onto the pool shared with every `src/storage.rs` DB call, so a flood would stall persistence instead of HTTP. The `20260705` review reached the same conclusion.
- This also resolves the Feature Envy smell filed against `validate_subflow_node_config`; consider expressing `SubflowFacts` as a method on the subflow type.
- Add a validation-cost regression (e.g. 800 calls × 2000-node subflow completing well under a fixed budget) and a test asserting the post-hydration cap rejects an over-large hydrated catalog.

---

### M11. `spawn` nodes select an arbitrary access profile with no ceiling check (FIXED)
> **Fix:** Threaded the resolved AgentConfig through registered Spawn launches, validated profile capabilities, and added capability-backed selects; command overrides still bypass ceilings.
**Severity:** MEDIUM (lower band) — an inheritance/validation defect, not an authorization bypass: `access` is author-controlled JSON with no third-party injection channel and omission fails *safe* for three of four builtins; held at MEDIUM because a user-facing safety control ("inherit") does not do what it says and one path stamps a pane with an access profile that was never applied.
**Files:** `src/tmux_exec.rs:656-694` (`execute_spawn`, `:678` dropped config), `:286`, `:317-319` (config in scope one frame up), `:2166-2182` (`build_agent_command` registry branch), `:2172-2179` (swallowed profile error), `:2182`→`:2122-2123` (`KEY_ACCESS` stamp), `:2069-2076` (command-override bypass); `src/driver.rs:281-322` (`resolve_registry_access_profile`), `:295-305` (widening guard), `:268-279` (`agent_access_profile_names`); `src/model.rs:411-425` (`SpawnConfig.access`), `:1486-1512` (validation that omits it); `ui/src/features/editor/InspectorPanel.svelte:1301-1307` (spawn Access field), `:782-788` (RunAgent twin); `src/agents/mod.rs:216-218`, `:222-240`
**Description:** `execute_spawn` discards the run's resolved `AgentConfig` at `src/tmux_exec.rs:678` even though it is in scope one frame up (`:286`, dispatch `:317-319`), forcing `build_agent_command` down the registry branch at `:2166-2182`. That branch calls `registry.launch_argv(agent, explicit_access)` directly, so the widening guard in `resolve_registry_access_profile` (`src/driver.rs:295-305`) — reachable only from the `Some(agent_config)` branch — never runs for Spawn, and an omitted `access` inherits the *registry* default (`src/agents/mod.rs:222-240`) rather than `agentDefaults`. `SpawnConfig.access` is an unvalidated `Option<String>` (`src/model.rs:411-425`; `:1486-1512` checks only agent/command/cwd), and for an **unregistered** agent `:2172-2179` swallows the unknown-profile error and launches bare argv while `:2182`→`:2122-2123` still stamps `KEY_ACCESS` — a pane labelled with a profile that was never applied. User-visible: the free-text Access field at `InspectorPanel.svelte:1301-1307` reads `placeholder="inherit"` and inherits nothing, and its RunAgent twin at `:782-788` advertises `read_only`/`execute`, neither of which is a valid registry key (`read-only` is).
**Fix:** Make `resolve_registry_access_profile` the single owner of every access decision by threading the resolved `AgentConfig` into the spawn path.
- Pass `config` from `run_tmux_node` (`src/tmux_exec.rs:286`) through the `:317-319` dispatch into `execute_spawn`, and forward it at `:678` instead of `None`.
- Have `build_agent_command` take the `Some(agent_config)` branch when a Spawn node launches an **agent**, with `SpawnConfig.access` populating `AgentConfig.access_profile_override` exactly as `RunAgentConfig.access` already does. Command-override spawns keep bypassing this via `:2069-2076`.
- **Sequence after M16.** M16 is an open `default`-widening hole inside `resolve_registry_access_profile` (`src/driver.rs:314-321`); routing Spawn into that function before M16 lands inherits the hole.
- Fold the authoring-time half into M6's validator: reject any `SpawnConfig.access` that is not a key of `agent_access_profile_names(agent)` (`src/driver.rs:268-279`) when the agent resolves to a registry entry, warn when it does not, and surface the rejection in the UI.
- Fix the two misleading UI controls: replace the free-text inputs at `InspectorPanel.svelte:1301-1307` and `:782-788` with a select populated from `/api/capabilities` (`accessProfiles`), with an explicit "registry default" option — this also kills the `read_only`-vs-`read-only` footgun.
- Accept the behavioural change: Spawn's argv gains `build_session_args`' model/tool/turn flags. This is a deliberate reversal of the `20260716-003531.md:88-96` M1 ruling ("Leave Spawn's else-branch behavior intact"), which preserved this path as the working reference RunAgent was fixed against; note the reversal in the commit message.
- Add regressions: a Spawn node under `agentDefaults.accessMode: read_only` must not launch with a broader profile; an omitted `access` must inherit `agentDefaults` rather than the registry default; an unregistered agent with an unknown profile must not stamp `KEY_ACCESS`; a command-override spawn must still bypass the ceiling unchanged.

---

### M12. A stale pane-stream task's teardown disables a freshly established `pipe-pane` (FIXED)
> **Fix:** Pane-stream registry gained a single-owner active/draining/terminating lifecycle with explicit drain signaling, reconnect revival, and owner-scoped teardown, ending stale-generation pipe-pane clobber.
**Severity:** MEDIUM — a visibly dead terminal that self-heals on the next reconnect, with no data loss and no security impact; held below HIGH because trigger 2 is narrow, but the window for trigger 1 is wide until H6 lands.
**Files:** `src/api.rs:1550-1564` (`stop_pane_stream`, unscoped `pipe-pane`), `:1963` (teardown call), `:1448` (setup-error teardown call), `:1553` (`timeout` over `spawn_blocking`), `:1805-1832` (`subscribe_pane_stream`, `drop(streams)` at `:1832`), `:1834` (`spawn_pane_stream_task`), `:1868-1875` (`sender.closed()` arm), `:1952-1972` (task lifetime), `:1416` (lifecycle marker), `:1704-1716`, `:1756-1758`, `:1886`, `:1162`, `:1169-1173` (`stream_chunks_seen` gate), `:60` (`PANE_STREAM_PENDING_TIMEOUT`); `src/app.rs:223-240` (`unsubscribe`), `:232`, `:251`, `:258-271` (`same_channel` / `remove_terminal_sender`); tests `src/api.rs:3815`, `:4440`
**Description:** `stop_pane_stream` runs `tmux::run(&["pipe-pane", "-t", &pane_target])` with no command and no owner check (`src/api.rs:1550-1564`); tmux has one pipe per pane and no way to query its command, so this closes whatever pipe is installed. Nothing serializes setup per `PaneStreamKey` — `subscribe_pane_stream` holds the registry mutex only across the lookup/insert and explicitly `drop(streams)` at `:1832` before spawning the pump at `:1834` — while the registry entry is removed at refcount 0 (`src/app.rs:235-236`) even though the task owning the tmux pipe lives on to `:1963`. A reconnect therefore creates a **second** owner for the same key: task #1's `sender.closed()` arm (`:1868-1875`) fires immediately, and its unconditional teardown at `:1963` (or the setup-error path at `:1448`) disables task #2's pipe. The writer then hits EOF, emits its `.` marker (`:1416`) → `terminated` (`:1704-1716`) → `Ok(0)` (`:1756-1758`) → `Terminal` (`:1886`) → `RecvError::Closed` (`:1162`); the user sees `"pane stream unavailable"` when no bytes had yet flowed, and a silent socket close when they had (the frame at `:1173` is gated on `!stream_chunks_seen` at `:1169`). A second, narrower trigger: `tokio::time::timeout` at `:1553` wraps `spawn_blocking`, and dropping the `JoinHandle` **detaches** rather than cancels, so the closure can still fire its unscoped `pipe-pane` after a reconnect installed a replacement. Teardown is the one place in the codebase that skips the `same_channel` ownership discriminator already used at `src/app.rs:232`, `:251`, `:266`. No test spawns two generations for one key (`:3815` covers a single generation; `:4440` is registry-level only).

*Prior history:* `docs/issues/code-review-20260608_part_3_of_4.md:83-85` filed this exact hazard against the upstream `PaneStream::drop` ("bare `pipe-pane -t <pane>` toggle-off, no owner guard") and fixed it with the broadcast registry + refcount — that removed *concurrent viewers* as a trigger but never made teardown owner-scoped. M12 is the surviving remainder. `...20260716-003531.md:536-546` M18 (FIXED) did **not** create this: its own description at `:540` already records the reconnect-clobber and left it in place.

*Relationship to H6:* **disjoint at line level; sequence H6 first.** H6 targets `:1391-1409`, `:1424-1432` and upstream `core/src/tmux.rs`; M12 targets `:1550-1564`, `:1963`, `:1448`, `:1805-1842`. Neither subsumes the other and there is no hard dependency, but H6's unguarded enable lets task #1 sit in setup indefinitely, which massively widens trigger 1 — and H6's fix caps that window at 6 s and makes trigger 2's late fire far less likely. M12 does nothing for H6's leaked thread and orphan process.
**Fix:** Make pipe ownership structural by giving the registry a single-owner lifecycle, so teardown can never target another generation's pipe.
- Stop removing the entry at refcount 0. Have `unsubscribe` (`src/app.rs:223-240`) mark the entry `draining` instead, and let only the owning task remove it via `remove_terminal_sender` (`:258-271`).
- On reconnect, a `subscribe_pane_stream` that finds a draining entry **revives** it — cancelling the drain and reusing the live sender and pipe — rather than inserting a second entry. Where revival is not possible, await the owner's exit before inserting.
- Gate revival so a genuinely terminal stream (lifecycle marker observed, `terminated` set at `:1704-1716`) is never resurrected; those must fall through to a fresh setup.
- Give the pump loop an explicit cancel/drain signal rather than relying solely on `sender.closed()` (`:1868-1875`), and add the `draining` state to `PaneStreamEntry`.
- With a single owner per key by construction, `stop_pane_stream` (`:1550-1564`) is only ever reached by the pipe's owner; no generation token and no second key-indexed map are needed.
- Land **after H6**, which edits adjacent lines and shrinks this bug's window.
- Add the missing two-generation regression: disconnect during setup, reconnect, and assert the first task's exit does not issue a `pipe-pane` against the second generation's pane, and that a fast reconnect reuses the existing pipe rather than re-running the 6 s setup. Note that tmux/socket tests are EPERM-inconclusive under sandbox, so prefer the fake-tmux harness.

---

### M13. Unhandled promise rejections — abort/approve/respond fail silently (FIXED)
> **Fix:** Added a guard() helper wrapping abort/approve/respond/dismiss so rejections surface via store.setError with a per-action label; validateMutation gained an onError; regression tests cover the 404 approve and 409 respond paths.
**Severity:** MEDIUM (upper edge) — `respondToInteraction` has a genuinely reachable 409 whose failure mode is a permanently blocked run with zero feedback; held below HIGH because `abortRun` is effectively infallible server-side, the fix yields only a 5-second transient pill, and the pattern is pre-existing on `development` rather than a branch regression.
**Files:** `ui/src/app/AppShell.svelte:446` (`abortRun`), `:449` (`approveRun`), `:452` (`respondToInteraction`), `:461` (`dismissRun` — `.then` with no `.catch`), `:72-75` (`validateMutation`, no `onError`), `:220-223` (`runLifecycle` try/catch — the convention), `:360-361` (error pill render), `:57`, `:68` (mutation `onError` handlers); `ui/src/lib/api/client.ts:22-35` (`ApiError`), `:38-52`, `:51` (throw); `ui/src/lib/stores/workflowStore.svelte.ts:1051-1058` (`setError`, 5000 ms auto-clear); backend reachable failures `src/api.rs:2416`, `:2411`, `src/runtime.rs:1061`, `:1583-1586`
**Description:** Four API calls in `AppShell.svelte` are invoked as bare statements — `:446`, `:449`, `:452`, and `:461`, the last chaining `.then` with no `.catch` so both the call and the chained `invalidateQueries` float. All four sit inside arrow callbacks passed as props to `RunPanel`/`HistoryPanel`, so no parent can await them either. `apiFetch` throws `ApiError` on any non-2xx (`ui/src/lib/api/client.ts:51`) and nothing catches it: there is no `unhandledrejection` listener and no `<svelte:boundary>` anywhere under `ui/`. The non-2xx paths are real — `respond_interaction` returns `StaleInteractionSession` → **409** (`src/api.rs:2416`, `src/runtime.rs:1061`) on a double-submitted or stale interaction card, and `approve_run` → `resolve_approval` 404s when no approval is pending (`src/api.rs:2411`). The user clicks Approve or answers a prompt, nothing changes, and the run stays blocked, while every comparable action in the same file reports through the header pill at `:360-361` via `store.setError`.

*Correction to the original write-up:* "every other call path in the file routes errors through `store.setError`" is overstated. The direct-call paths do (`runLifecycle:220-223` covering `startRun`/`resumeRun`/`restartFromNode`; mutation `onError` at `:57`, `:68`), but `validateMutation` (`:72-75`) has no `onError` and the three `createQuery` calls at `:38-40` surface nothing — TanStack swallows those, making them silent-but-not-unhandled. Real ratio: 4 unguarded floating promises vs 5 guarded, plus 4 TanStack-managed calls that never reach the user.

*Prior history:* no prior review flagged these four sites, so no dismissal is being reversed. `docs/issues/feature-tmux-panes-code-review-20260610.md:140-142` rewrote what is now `:452` to pass `sessionId` without adding error handling (out of scope then), and `...20260716-003531.md:351-352`'s per-card `sessionId` work is precisely what makes the 409 reachable. The repo-wide convention across multiple FIXED findings (e.g. `...20260620-092605.md:337-342`, L8) is to surface failures loudly rather than drop them silently; this is the frontend instance of that rule. No `.test.ts` under `ui/src` references `abortRun`, `approveRun`, `respondToInteraction`, `dismissRun`, `setError`, or `errorMessage`.
**Fix:** Introduce one shared error-wrapping helper in `AppShell.svelte` and route every floating call through it with a per-action label.
- Define a local helper: `const guard = (label: string, p: Promise<unknown>) => void p.catch((e) => store.setError(\`${label} failed: ${e instanceof Error ? e.message : "Unknown error"}\`));` — reusing the `instanceof Error` narrowing already present at `:223` so the message shape stays identical to the rest of the file.
- Wrap all four sites with their own action names (`"Abort"`, `"Approve"`, `"Respond"`, `"Dismiss"`); at `:461` attach the guard around the whole `.then` chain so the chained `invalidateQueries` rejection is covered too.
- Close the adjacent silent gap in the same change: give `validateMutation` (`:72-75`) an `onError` that routes to `store.setError`, matching `:57` and `:68`.
- Do **not** add a global `unhandledrejection` listener as the fix: it leaves each call site unhandled and degrades the user-visible result to a generic unlabelled pill with no action name. It is acceptable only as a separate dev-time diagnostic.
- Add the missing regressions — no test currently touches any of these actions. Assert that a rejected `respondToInteraction` (409) sets `errorMessage` naming the action, and the same for `approveRun` (404).

---

### M14. `get_run` is a read that can fail because of a write (FIXED)
> **Fix:** The workflow_json CAS upgrade moved into Database::init and upsert now normalizes on INSERT, so get_run is a pure read with no write-back.
**Severity:** MEDIUM — the filed failure mode alone is LOW (both cited triggers are near-unreachable under WAL + a write-requiring startup), but two comments on a data-integrity path assert a startup upgrade that does not exist, and this write-back is what converts L11 from a transient in-memory bug into permanent on-disk workflow corruption.
**Files:** `src/storage.rs:294` (`get_run`), `:328-332` (false comment), `:333` (in-memory normalize), `:335-343` (read-path CAS), `:342` (`?`), `:180-190` (`upsert_run`), `:186` (INSERT), `:189-192` (duplicate false comment), `:985` (`ensure_dir`), `:992-994` (`configure_connection`: WAL, `synchronous=NORMAL`, 5 s `busy_timeout`), `:134`ff (`init`), tests `:1639-1692`, `:1745-1789`; callers `src/runtime.rs:1394-1397`, `:1437-1440`, `:1452-1455`, `src/api.rs:472`, `:1029`, `:1214-1216`, `:1259-1261`; normalization `src/model.rs:973`, `:325-352`
**Description:** `get_run` performs a write on the read path — a genuine CAS at `src/storage.rs:335-343` guarded by `WHERE run_id = ?2 AND workflow_json = ?3` — whose `?` at `:342` propagates through the `with_connection` closure into the returned `anyhow::Result`, so a write-side failure turns a successful read into `Err`. That maps to `RunControlError::Internal` in `resume_run` (`src/runtime.rs:1394-1397`) and `restart_from` (`:1437-1455`), `PaneContextError::Internal` at `src/api.rs:1214-1216` and `:1259-1261`, and a 500 at `src/api.rs:472` and `:1029`. The persistence is pure optimization: the in-memory `workflow` is already normalized at `:333` *before* the UPDATE, so if the write never lands, normalization simply re-runs on each read — nothing reads persisted-canonical-ness.

*Corrections to the original write-up:* both cited triggers are overstated to the point of being unreachable. `configure_connection` sets `journal_mode=WAL` with a 5 s `busy_timeout` (`:992-994`), so under WAL readers never block the writer and only writer-vs-writer contends in what is a single process with a pool — a >5 s busy is effectively impossible. The read-only-DB-file scenario is hypothetical: `Database::init()` unconditionally runs `CREATE TABLE`/migrations (`:134`ff), `with_connection` calls `ensure_dir` (`:985`), and WAL requires writable `-wal`/`-shm`, so a read-only file kills startup long before any `get_run`.

*Two defects the original write-up missed:* (1) the comment is not merely misleading — `:328-332` points to "H3's one-time startup upgrade" as the durable repair, and **no startup upgrade exists anywhere**; `workflow_json` is written only at `:186` (INSERT) and `:340` (this CAS), and the same false claim is duplicated at `:189-192` in `upsert_run`. (2) **This write-back is what makes L11 durable.** `migrate_output_schema` (`src/model.rs:325-352`) runs inside `normalize_workflow_value` (`:973`), i.e. inside this read path at `:333`, so the first `get_run` of a run whose node carries a real JSON Schema rewrites `workflow_json` in the DB with the corrupted form, destroying the original. Without the write-back, L11 is transient and in-memory only.

*Prior history — two prior fixes resolved into contradictory artifacts:* `docs/issues/feature-tmux-panes-code-review-20260705-074320.md:33-42` H3 (FIXED, HIGH) *introduced* this write-back ("`get_run` durably self-heals `workflow_json` via idempotent compare-and-swap so it stops re-migrating on every read"), but its fix text asked for a one-time **startup** upgrade and the implementation delivered on-load-only. `:288-294` L6 (FIXED, LOW) had explicitly *rejected* a read-path write-back, and its entire fix was documentation with "No logic change" — those are the two comments the shipped H3 code now contradicts.
**Fix:** Make reads pure by moving the upgrade to startup and canonicalizing on write — the design H3 specified and L6 documented.
- Add a migration step in `Database::init()` that scans `runs`, normalizes each `workflow_json`, and CAS-updates rows that differ. Startup is already a write context, so a failure there is honest and loud rather than surfacing as a failed read.
- Have `upsert_run` (`src/storage.rs:180-190`) store the normalized value at `:186`, so new rows are born canonical and nothing non-canonical is ever persisted.
- Remove the write from `get_run` entirely (`:335-343`), keeping the in-memory normalization at `:333` as insurance for rows written by an older binary.
- Rewrite both comment blocks (`:328-332`, `:189-192`) to describe what the code actually does. After this change L6's original invariant is true again, so the text can state it plainly.
- **Sequence L11 before or with this change.** While the read-path write-back exists, every `get_run` risks persisting L11's corruption; once reads are pure, the exposure drops to the startup pass and the write path, both of which L11's fix must be correct for anyway.
- Reconcile with the immutable-snapshot invariant covered by `stores_run_records` before changing `upsert_run`.
- Existing tests `get_run_persists_structurally_migrated_v3_workflow_as_v4` (`:1639-1692`) and `get_run_leaves_canonical_v4_workflow_unchanged` (`:1745-1789`) assert the read-path write-back specifically and must be rewritten to target the startup migration instead. Add a regression that a legacy v3 row is canonical after `init()` with no `get_run` call in between.

---

### M15. ~4 full-checkpoint DB writes per second for the whole duration of a human approval wait (FIXED)
> **Fix:** persist_checkpoint hashes the serialized checkpoint with updated_at cleared and skips the DB write when unchanged, ending the ~4 writes/sec approval-wait churn.
**Severity:** MEDIUM (low end) — pure write churn with no correctness or security impact and no executor blocking; held above LOW because the wait is unbounded by construction, every write is genuinely byte-different so nothing dedups, and this is the known-incomplete remainder of a chartered prior fix.
**Files:** `src/runtime.rs:2795-2798` (ungated persist), `:2789-2790` (`changed`), `:2830`, `:2847-2850`, `:2852`, `:2862-2865` (`wait_changed` gating), `:3518-3535` (`wait_for_approval`), `:3526-3530` (decision arm), `:3531` (250 ms sleep), `:6577-6594` (`persist_checkpoint`), `:6582` (`updated_at` stamp), `:6299`, `:551-589` (checkpoint contents); `src/storage.rs:255-287` (`update_run_checkpoint`), `:284` (`state_json`), `:186-203` (frozen `workflow_json`), `:976-980` (pool), `:991-995` (WAL, `synchronous=NORMAL`, 5 s busy_timeout)
**Description:** While a run is parked on a human approval and no agent tasks are in flight, the loop at `src/runtime.rs:2793-2799` spins a 250 ms poll and unconditionally re-serializes and re-writes the full `RuntimeCheckpoint` at `:2797` — `changed` is false at `:2789` while parked, so `:2790` is skipped and `:2797` is the sole write per iteration. That is ~4 writes/s, ~14,400/hour, ~115,000 overnight. The blob is `state_json` (`src/storage.rs:284`), carrying `all_results` (every node's full agent output), `batch_item_results`, `execution_log`, and `output_hashes` (`src/runtime.rs:551-589`) — realistically 50 KB–2 MB on a moderate workflow — and `persist_checkpoint` stamps `updated_at = now_iso()` (`:6582`) before writing, so every blob is byte-different and nothing dedups. Cost is full JSON serialization per tick plus roughly 800 KB/s of WAL traffic on a 200 KB blob, with the periodic auto-checkpoint fsync stealing the pool's writer slot from `append_event`. The wait is unbounded: approval resolves only via the oneshot `wait.receiver`, and there is no timeout arm in `wait_for_approval` or `restore_pending_approval`.

*Corrections to the original write-up:* (1) the cited line range is the **caller** — `wait_for_approval` itself is at `:3518-3535` with its 250 ms arm at `:3531`, and the ungated persist is `:2796-2797`. (2) "the entire checkpoint" is right but the implied cost is inflated: this is **one** `UPDATE runs` writing 10 columns, and `workflow_json` is *not* rewritten (frozen at INSERT, `src/storage.rs:186-203`). (3) "pre-existing; identical on `development`" is misleading — `wait_changed` does not exist on `development` at all, where the *whole* loop persisted unconditionally; this branch fixed two of three sites and `:2795-2797` is the leftover. Mitigating: WAL is on with SQLite's default 1000-page auto-checkpoint (`src/storage.rs:991-995`), so the WAL does not grow unbounded, and writes run on `spawn_blocking` against a pool, so the executor is never blocked.

*Prior history — this is the residual of a chartered prior fix, never a dismissal.* `docs/issues/feature-tmux-panes-code-review-20260620-092605.md:125-130` M9 (FIXED) specified exactly this remedy — "gate the 250ms tick persist with a dirty flag set where the loop does dispatch/apply work" — and the implementation introduced `wait_changed` at `:2830`/`:2852` but missed the `wait_for_approval` early-`continue` path. `docs/issues/feature-tmux-panes-code-review-20260610.md:186` had flagged the same ~4×/s pattern earlier as an efficiency smell. **Downstream dependency worth knowing:** `...20260620-092605.md:183` lowered its M14 to LOW *explicitly because* "M9's dirty-flag persist gate removed the per-250ms re-serialization premise" — an assumption that is false on the approval path until this lands. No overlap with M4, M7, or M8 in this review.
**Fix:** Make "don't write what didn't change" an invariant of the persistence boundary rather than a rule each caller must remember.
- In `persist_checkpoint` (`src/runtime.rs:6577-6594`), serialize the checkpoint once and hash the resulting JSON **before** stamping `updated_at`, so the stamp cannot defeat the comparison.
- Hold a `last_persisted_hash` on `RuntimeContext`; when the hash is unchanged, skip the DB write entirely and leave `updated_at` un-advanced. Stamp `updated_at` and update the stored hash only on an actual write.
- Audit readers of `updated_at` before landing: it will stop advancing while a run is idle, and any UI or reaper logic that infers staleness or liveness from it must be adjusted to use run status instead.
- This closes the whole class at one choke-point, including `:6299` and any unaudited caller — not just the `wait_for_approval` path.
- Add a regression asserting that N consecutive idle ticks during an approval wait produce exactly one (or zero) `update_run_checkpoint` calls; no test currently covers write frequency, only the correctness round-trips at `src/storage.rs:1373`/`:1410`.
- Complementary trivial follow-up, not part of this fix: returning `bool` from `wait_for_approval` and gating `:2796-2797` on it would make all three loop exits structurally symmetric and finish M9 item (3) as literally chartered. It is redundant once the hash gate lands, but harmless and clarifying.

---

### M16. `default` access-profile fallback can silently widen `Edit` (FIXED)
> **Fix:** Default fallback now requires a non-widening declared rank, using exact local profile-shape validation because an upstream rev bump was not authorized.
**Severity:** LOW — *lowered from MEDIUM*: latent-only. The branch is dead code against every shipped builtin and the operator's own `agents.toml`; reaching it requires a hand-authored registry entry that both omits `workspace-write` and gives `default` broad argv — an author configuring their own escalation, with no third-party channel. ID retained as `M16` for stability.
**Files:** `src/driver.rs:282-324` (`resolve_registry_access_profile`), `:290` (`mapped`), `:291` (override arm), `:295-305` (widening guard), `:307-308` (else arm), `:310-311` (early return), `:314-315` (false comment), `:316-318` (`ReadOnly` fail-closed), `:319-321` (`default` fallback), `:323` (bail); `core/src/agents/mod.rs:28-30` (`AccessProfile`), `:180-186` (`Registry::load` merge), `:342-348` (unknown-name insert); `core/src/agents/builtin.rs:29-45`, `:76-93`, `:117-129`, `:149-156`, `:269`; `Cargo.toml:28` (upstream rev pin); neighbouring tests `src/driver.rs:1819`, `:1830`, `:1841-1887`
**Description:** `resolve_registry_access_profile` resolves a profile name and, when that name is absent from `spec.access_profiles`, substitutes the literal `"default"` at `src/driver.rs:319-321` for every mode except `ReadOnly` (`:316-318`). The comment at `:314-315` asserts that a default "may safely reduce or preserve privileges" — a property the code never checks and *cannot* check, because `AccessProfile` carries only raw argv (`core/src/agents/mod.rs:28-30`) and exposes no privilege metadata. The widening guard at `:295-305` never runs on this path: it lives inside the `access_profile_override` arm (`:291`) and compares the override *name* against the mode, while the `default` fallback is a post-hoc substitution in the `else` arm (`:307-308`) reached only after the early return at `:310`. No test covers `:319-321`.

*Reachability correction to the original write-up:* the branch is **unreachable with the shipped registry**. All four builtins define both `workspace-write` and `full-access` (`builtin.rs:29-45` codex, `:76-93` claude, `:117-129` cursor, `:149-156` agy), as does the operator's `~/.config/tmux-tools/agents.toml`. The only mapped key missing anywhere is agy's `read-only` (asserted at `:269`), which lands on the fail-closed branch at `:316-318`. Custom entries are possible — `Registry::load` merges `~/.config/tmux-tools/agents.toml` (`mod.rs:180-186`) and inserts unknown names as new agents (`:342-348`) — so a user-authored agent declaring only a broad `[x.access.default]` is the sole reaching path.

*Prior history — this reverses a recorded ruling.* `docs/issues/feature-tmux-panes-code-review-20260705-074320.md:514-519` (L7, FIXED) created this asymmetry deliberately and ruled the Edit/Execute side safe: "Read-only is the only mode whose default-fallback can escalate privilege — edit/execute/unrestricted falling to `default` keep or drop privilege (fail-safe)... add a comment explaining it." The comment M16 attacks is the one that ruling mandated. The reversal is correct in principle: the ruling rested on an assumption about `default` that nothing enforces. `...20260716-003531.md:88-92` (M1) later added the `:291-305` override arm and its guard.

*Relationship to H1 — H1 does **not** subsume this.* H1's finalized fix promotes the implicit ordering at `:295-299` into a privilege rank over `read-only < workspace-write < full-access`; `"default"` is not in that lattice and `:319-321` never consults it. H1 supplies the mechanism, not the check. **Sequence M16 after H1.**

*Relationship to M11 — no dependency in either direction.* M11's finalized text sequences itself behind M16 to avoid "inheriting the hole"; verification shows that is unnecessary. Today Spawn's omitted `access` passes `None` into **core's** own fallback (`core/src/agents/mod.rs:222-240`), which is reachable for every builtin, unguarded, and stamps `KEY_ACCESS` regardless — so routing Spawn through `resolve_registry_access_profile` is a strict improvement even with `:319-321` intact. M11 needs nothing from M16, and this fix needs no Spawn-specific addition. *(M11's own sequencing note has been left as written; correcting it was raised but not decided.)*
**Fix:** Extend H1's privilege rank over the fallback branch so `default` is accepted only when it is provably no broader than the profile it replaces.
- Land **after H1**, and reuse the rank type H1 introduces rather than defining a second ordering.
- Give `AccessProfile` a declared privilege rank and accept the `default` substitution at `:319-321` only when `rank(default) <= rank(mapped)`. A `default` that is unranked or undeclared fails closed via `:323`.
- `AccessProfile` lives in the pinned upstream crate (`Cargo.toml:28`), so this is a coordinated two-repo change: add the rank upstream, then bump the rev. H6 already establishes that upstream edits are in scope for this branch — coordinate the two rev bumps.
- Do **not** implement the rank as a driver-side map keyed on profile *name*: it cannot rank `default` at all, which is the entire question, and collapses into simply deleting the fallback.
- Do **not** infer rank by parsing `default`'s argv for known-broad flags. A denylist over free-form argv is unsound across agents and versions, and the adjudication note explicitly rules it out.
- Delete the false comment at `:314-315` and replace it with text describing the rank check that now exists.
- Add the missing regression: an `Edit` run against a custom agent that declares only a broad `default` must fail closed rather than launch; add a companion positive case where a narrow `default` is accepted.

---

### M17. Non-atomic workflow writes plus fail-fast listing (FIXED)
> **Fix:** WorkflowStore::save now writes <name>.json.tmp + sync_all + rename; list() warns-and-skips per bad file and unlinks stale temps; added lists_workflows_and_skips_invalid_files regression.
**Severity:** MEDIUM — real data loss with an all-or-nothing blast radius on the list endpoint and no recovery path, offset by a millisecond-scale crash window on a single-user local app writing small JSON.
**Files:** `src/storage.rs:880-887` (`save`), `:841-861` (`list`), `:844` (extension filter), `:882` (`ensure_dir`), `:889-894` (`delete`), `:919-947` (`TemplateStore::list` convention), `:1247` (happy-path test), `:1267` (template skip test); `src/api.rs:494-495` (`GET /api/workflows`), `:2430-2435` (error mapping) *(pre-existing, untouched by this branch)*
**Description:** `WorkflowStore::save` writes with `tokio::fs::write` — `O_TRUNC` with no fsync — so a crash mid-save leaves an empty or half-written `.json` (`src/storage.rs:880-887`). `WorkflowStore::list` then propagates the first read/parse/normalize error through three bare `?` at `:849-851`, so a single damaged file makes `GET /api/workflows` return HTTP 500 (`src/api.rs:494-495`, `:2430-2435`) and the sidebar shows *no* workflows rather than just omitting the broken one. There is no backup, no `.bak`, and no in-app repair: the only remedy is `DELETE /api/workflows/:name` for a name the UI can no longer display, or hand-editing the filesystem. `TemplateStore::list` (`:919-947`) already establishes the warn-and-skip convention in the same file with a regression test at `:1267`; `WorkflowStore` has none.

*Correction to the original write-up:* `hydrate_saved_subflows` does **not** call `list` — it calls `get(&name)` per referenced subflow (`src/api.rs:581`), so a corrupt file breaks hydration only for workflows that reference that specific subflow by name. The "fails wholesale" claim holds for the list endpoint only.
**Fix:** Make the write durable and atomic, and make the read resilient, mirroring the `TemplateStore` convention already in this file.
- In `save`, serialize to `<dir>/<safe>.json.tmp`, `sync_all()` the file handle, then `tokio::fs::rename` over `<safe>.json`. `ensure_dir` at `:882` already guarantees the directory exists and the temp file lands in that same directory, so the rename is a same-filesystem atomic replace.
- Keep the `.json.tmp` suffix specifically: `list` filters on `ends_with(".json")` (`:844`), so an orphaned temp file is invisible to the scan. A `.tmp.json` suffix would *not* be — do not reorder it.
- Convert `list`'s three `?` at `:849-851` into `match` / `tracing::warn!` / `continue`, mirroring `:919-947`, so one bad file is skipped rather than failing the endpoint.
- Have the `list` scan unlink stale `*.json.tmp` entries it encounters, so a crash between write and rename cannot accumulate debris. Accept the mild layering smell of a read path performing cleanup.
- `save` is the only writer needing this treatment — `delete` (`:889-894`) is `remove_file`, which is already atomic.
- Add a `lists_workflows_and_skips_invalid_files` regression parallel to `:1267`; the existing `stores_workflow_files` (`:1247`) is a happy-path round-trip only.
- Optional follow-on, not required by this fix: surface skipped filenames to the UI so the user can identify and repair the damaged file rather than silently losing it from the list.

---

### M18. `runAs.socket` is unvalidated and the agent-user case is excluded from the privileged-run gate (FIXED)
> **Fix:** Removed author-controlled runAs.socket across Rust, UI, types and docs; legacy field deserializes and is discarded; runtime and reaper always derive the per-run silverbond-<run_id> socket.
**Severity:** MEDIUM (lower edge) — author-controlled, ungated, and defeats a deliberate isolation boundary, but kills stay `silverbond-`-name-scoped, so it degrades isolation and leaks sessions rather than destroying arbitrary user panes.
**Files:** `src/tmux_exec.rs:89-92` (socket passthrough), `:1200` (`list_silverbond_tmux_sessions` prefix filter), `:1209-1222` (`cleanup_panes`), `:2482-2506` (`stable_session_name`/`normalized_session_name`), `:2508-2521` (`sanitize_tmux_name`), `:2979-2988` (socket tests); `src/model.rs:220-224` (`RunAsConfig`), `:874`, `:1875-1919` (`validate_run_as_config`); `src/api.rs:767-784` (unlock), `:831-846` (`resolved_run_as_is_privileged`); `src/runtime.rs:6449-6460` (reaper reconstruction), `:6505-6519`; `ui/src/features/editor/InspectorPanel.svelte:69-91`, `InspectorPanel.test.ts:281-302`; `docs/workflow-schema.md:67-97`
**Description:** `build_tmux_invocation_without_resolving` does `run_as.socket.clone().unwrap_or_else(|| format!("silverbond-{run_id}"))` (`src/tmux_exec.rs:89-92`), passing the author's string verbatim to `tmux -L <socket>`; neither `validate_run_as_config` (`src/model.rs:1875-1919`) nor `resolved_run_as_is_privileged` (`src/api.rs:831-846`) inspects `socket`, and `runAs.user == SILVERBOND_AGENT_USER` requires no unlock (`src/api.rs:767-784`). `socket` is not operator-only — it is a documented schema field (`docs/workflow-schema.md:67-97`) editable in the UI inspector (`ui/src/features/editor/InspectorPanel.svelte:69-91`), so any saved workflow can opt out of per-run socket isolation with zero friction. Consequences: SilverBond panes land on a live interactive tmux server and are cleaned up there; `kill-session -t` uses tmux prefix/fnmatch target matching, so deterministic continuation names from `stable_session_name` (`:2482-2506`, e.g. `silverbond-Reviewer-A`) collide across runs and users sharing a socket; and the per-run-socket isolation boundary is silently defeated.

*Corrections to the original write-up:* tmux-tools emits `-L`, not `-S`, so the socket is a label under the per-uid socket dir and tmux itself rejects `/` — there is no path escape. "Killing the user's own panes" does **not** follow: every kill is `silverbond-`-name-scoped (`src/tmux_exec.rs:1200`, `:2491-2506`), the boot reaper additionally requires a DB-registered terminal session (`src/runtime.rs:6505-6519`), `cleanup_panes` only kills targets from SilverBond's own registry, and tmux never reuses pane ids within a server, so a stale `kill-pane` is a no-op.

*Governing precedent:* `20260620` M12 (FIXED) chose per-run sockets **as** the isolation boundary and M10 (FIXED) requires kills to stay invocation-scoped; both are re-ratified at `docs/issues/feature-tmux-panes-code-review-20260716-003531.md:191`, `:266`. No prior finding rules on socket validation or on what the privileged gate is meant to cover.
**Fix:** Remove `runAs.socket` from the schema so the socket is always `silverbond-<run_id>` — eliminating the defect class rather than constraining it, and aligning the code with the M12 ruling that the socket is the per-run isolation boundary and therefore not the author's to choose.
- Delete the `socket` field from `RunAsConfig` (`src/model.rs:220-224`) and drop the override branch at `src/tmux_exec.rs:89-92`, leaving the `silverbond-{run_id}` construction unconditional.
- Remove the socket control from the UI inspector (`ui/src/features/editor/InspectorPanel.svelte:69-91`) and its assertions at `InspectorPanel.test.ts:281-302`.
- Remove the field from `docs/workflow-schema.md:67-97`.
- Migrate saved workflows carrying `runAs.socket`: accept and discard the field on deserialize (e.g. `#[serde(default)]` on a deprecated alias, or drop it in `normalize`) so existing documents load rather than erroring, and confirm the reaper's reconstruction path (`src/runtime.rs:6449-6460`) still derives the same socket name for in-flight runs.
- Update the socket tests at `src/tmux_exec.rs:2979-2988`, and add a regression asserting a workflow document containing `runAs.socket` loads successfully and still runs on `silverbond-<run_id>`.
- Accept the tradeoff: this removes the operator escape hatch for attaching to a pre-provisioned tmux server. If that need resurfaces, it should return as an explicitly privileged, gate-tripping field rather than an ungated one.

---

## LOW

### L1. `create_dir_all` syscall on every database operation (FIXED)
> **Fix:** Added ManagedSqliteConnectionManager wrapping SqliteConnectionManager, running ensure_dir(parent) in connect(); with_connection dropped its path parameter and its per-call mkdir; ~28 call sites updated.
**Severity:** LOW (borderline dismiss) — real, newly-introduced redundancy, but ~2 syscalls (~1–2 µs) on a path running at single-digit ops/sec and already guaranteed at `src/app.rs:284`; worth doing only as cleanup bundled with the `path`-parameter removal.
**Files:** `src/storage.rs:976-989` (`with_connection`), `:983-986` (the `ensure_dir` block), `:985`, `:987` (pool checkout), `:99` (`with_init(configure_connection)`), `:119`–`:820` (19 non-test call sites), `:415` (`append_event`); `src/app.rs:34` (`AppPaths::from_root`), `:284-289`, `:296-297`; `src/runtime.rs:6571-6572` (`emit_event`); `src/api.rs:2498-2509`, `:4224-4234` (no-init test constructors)
**Description:** `with_connection` calls `ensure_dir(parent)` at `src/storage.rs:985` on every invocation, before `connection.get()` at `:987`, across 19 non-test call sites including `append_event` (`:415`). In production this is pure duplication — `Application::boot` already runs `ensure_dir` on the database parent at `src/app.rs:284-289`, before `Database::new` (`:296`) and `db.init()` (`:297`). Introduced by commit `dac8d59` as collateral of the `Arc<Mutex<Option<Connection>>>` → r2d2 `Pool` migration, which hoisted the call out of the `if guard.is_none()` first-open branch; there is no runtime path that removes the directory, so the per-call repetition buys nothing.

*Corrections to the original write-up:* the real cost is ~1–2 µs (one `mkdir` returning `EEXIST` plus one `is_dir` stat — not per path component) against a `spawn_blocking` + pool checkout + WAL write, at a realistic peak of single-digit DB ops/sec, so "hot path" overstates it. The per-call `ensure_dir` is **not** load-bearing: `remove_dir_all` appears only at `src/runtime.rs:11831`/`:11856` against agent temp dirs, and the DB path is fixed per-root (`src/app.rs:34`), not per-run. The two test states that build a `Database` without `ensure_dir` or `init` (`src/api.rs:2498-2509`, `:4224-4234`) drive pane-stream tests that never touch the DB.

*Prior history:* not a regression of a deliberate fix. `20260620-092605` M9 introduced `with_connection` with `ensure_dir` deliberately scoped to first-open only, so relocating it restores that intent rather than reverting a prior decision.
**Fix:** Move the invariant to the point where it is actually true — connection open — rather than to a lifecycle method that callers can bypass.
- Introduce a thin `ManageConnection` wrapper around `SqliteConnectionManager` whose `connect()` runs `ensure_dir(parent)` and then delegates, replacing the `with_init(configure_connection)` wiring at `src/storage.rs:99`. The pool (`min_idle(0)`, `max_size(8)`) is the only thing that opens connections, so the directory is guaranteed for every connection at most 8 times per process instead of once per call.
- Delete the `ensure_dir` block at `:983-986` and drop the now-unused `path: &Path` parameter from `with_connection`, updating all 28 call sites (19 non-test at `:119`–`:820`, ~9 in tests). `path` is used for nothing else in the function.
- Prefer this over hoisting into `Database::init`: a newly-minted connection still recreates the directory if it is removed externally, and the guarantee does not depend on a caller remembering to call `init` — which the constructors at `src/api.rs:2498-2509` and `:4224-4234` already skip.
- Accept the ~20-line wrapper type as the cost of stating the invariant where it holds.

---

### L2. `send_socket_error_and_close` is never called (FIXED)
> **Fix:** Deleted dead send_socket_error_and_close; pane WebSocket errors stay on send_error_frame + close, preserving backpressure and seq handling.
**Severity:** LOW — compiler-visible dead code with zero runtime or UX impact; the error paths it was written for are all already covered by `send_error_frame`.
**Files:** `src/api.rs:2144-2155` (the dead function), `:2032-2040` (`PaneWsFrame`), `:2111-2126` (`send_error_frame`), `:2128-2142` (`send_frame`), `:1075-1076`, `:1085-1086` (the two abort sites that replaced it), `:914-916`, `:1170-1176`, `:1107`, `:1317-1327`; `ui/src/lib/api/client.ts:458-460`
**Description:** `send_socket_error_and_close` builds a `PaneWsFrame { type: "error", seq, error }`, sends it as text, then sends `Message::Close(None)` — and takes the un-split `WebSocket`, which is why the current handler cannot call it. Commit `dac8d59` refactored `pane_stream_socket` to `socket.split()` and deleted both of its call sites, replacing them with `send_error_frame(&mut sender, …)` + `sender.close()` at `src/api.rs:1075-1076` and `:1085-1086`. `cargo check --all-targets` confirms the resulting warning verbatim: `function 'send_socket_error_and_close' is never used --> src/api.rs:2144:10`; there is no `#[allow(dead_code)]` and no test caller.

*Reframing:* the defect is an orphaned duplicate, **not** a missing feature or silently-closing error paths. Every abort path is already structured — auth fails before upgrade (`:914-916`), pane-context failure sends `client_message()` (`:1075`), subscribe failure sends `to_string()` (`:1085`), pane-EOF sends `"pane stream unavailable"` (`:1170-1176`) — and the only silent drops (`:1107`, `:1317-1327`) are client disconnects, which is correct. The frontend already handles the frame shape at `ui/src/lib/api/client.ts:458-460`, but `send_error_frame` is what produces it, so wiring the dead function in would change nothing observable.

*Provenance:* added live in `12ac692` with callers and cited as live in three prior reviews (`docs/issues/code-review-20260608_part_4_of_4.md:56`, `...20260620-092605.md:120`, `...20260705-074320.md:96`). No prior review requested it; it is refactor residue.

*Separate live item, not covered by this fix:* `20260620-092605.md:120` flagged a raw `to_string()` leak, and the successor at `src/api.rs:1085` still passes `&error.to_string()` while `:1075` uses `client_message()`. That inconsistency remains open and should be tracked on its own.
**Fix:** Delete `src/api.rs:2144-2155` outright. The function is a strictly inferior duplicate of `send_error_frame` (`:2111-2126`) + `send_frame` (`:2128-2142`) — it bypasses the `PANE_STREAM_SEND_TIMEOUT` backpressure guard, never advances `next_seq`, and takes an unsplit socket the handler no longer holds. Removal clears the warning with zero behavior change and keeps `send_error_frame` the single error-frame choke point, matching the existing `send_snapshot` / `send_data_frame` / `send_heartbeat` family. Do not wire it into the abort sites: that would manufacture a caller for a helper the `dac8d59` refactor deliberately superseded, and would either regress backpressure/seq handling or simply duplicate `send_error_frame`.

---

### L3. `--exclude-dir` misuse in the new CI guard script (FIXED)
> **Fix:** Replaced the divergent rg/grep fork in scripts/check-canonical-v4-docs.sh with a single git ls-files pathspec pipeline, so historical issue and task docs are genuinely excluded on any userland without a ripgrep dependency.
**Severity:** MEDIUM — *raised from LOW*: the branch's own new CI gate is failing on every push right now, for a reason unrelated to the code it guards. ID retained as `L3` for stability.
**Files:** `scripts/check-canonical-v4-docs.sh:11-15` (rg branch), `:17-21` (grep fallback), `:18-19` (the bad globs); `.github/workflows/canonical-v4-docs.yml:9`, `:16`; tripping content at `docs/issues/feature-tmux-panes-code-review-20260610.md:121-122` and `docs/issues/feature-tmux-panes-code-review-20260705-074320.md:593`, `:594`, `:611`
**Description:** `scripts/check-canonical-v4-docs.sh:17-21` passes path-shaped globs to `grep --exclude-dir`, which matches directory **basenames** only, so `docs/issues` and `docs/tasks` are never excluded. Verified empirically on the same tree with both BSD grep 2.6.0 and a from-source GNU grep 3.11: `--exclude-dir=docs/issues` prints matches (exit 0), `--exclude-dir=issues` finds none (exit 1) — BSD grep does support the flag and treats a path-shaped glob identically. The `rg` branch at `:11-15` is correct, since `--glob '!docs/issues/**'` is path-anchored, so the two branches are not equivalent implementations. `.github/workflows/canonical-v4-docs.yml:9`, `:16` runs the script on `ubuntu-latest` with no install step, and the Ubuntu 24.04 runner image ships no ripgrep, so CI always takes the broken fallback and the job is **red on every push and PR today** — the review documents themselves contain `canonical v3` / `v3-only workflow schema` and trip the guard.

*Corrections to the original write-up:* "fails spuriously on any machine without ripgrep" understates it — the fallback is the default CI path, not an edge case. The `node_modules`/`target` half is overstated for CI: on a fresh checkout neither directory exists, and `-I` suppresses `.git` binary matches. Locally, though, the multi-GB `target/` makes the fallback pathologically slow.

*Provenance:* added in `ab8eee3` as the fix for L12 of `docs/issues/feature-tmux-panes-code-review-20260705-074320.md:591`, then widened in `3790df1`.
**Fix:** Collapse the rg/grep fork into one portable path built on git pathspecs, removing the divergent-implementation root cause rather than one instance of it.
- Replace both branches (`:11-21`) with a single pipeline: `git ls-files -z -- ':!docs/issues/**' ':!docs/tasks/**' ':!scripts/check-canonical-v4-docs.sh' | xargs -0 -r grep -InE "$PATTERN"`.
- Git pathspecs are genuinely path-scoped and behave identically under BSD and GNU userlands, so the exclusion works everywhere without a ripgrep dependency or an apt install step in the workflow.
- Tracked-file enumeration inherently skips `node_modules`, `target`, and `.git`, which also removes the local slowness.
- Keep `xargs -r` so an empty file list cannot leave `grep` reading stdin and hanging the job.
- The guard's pattern is plain ERE, so nothing is lost by dropping `--pcre2` along with the rg branch.
- Scanning only tracked files is correct here: the guard exists to police committed docs, and `public/` is tracked.
- Verify by running the script on a clean `git archive HEAD` checkout with ripgrep absent from `PATH`; it must exit 0.

---

### L4. `retry_count` is unclamped — debug-build panic, and an unbounded retry runaway in the reachable mid-range (FIXED)
> **Fix:** retry_count is clamped in the runtime, bounded at ingress validation, and the Inspector input now carries min 0 / max 10, closing the runaway at every layer.
**Severity:** LOW — the claimed release-mode "zero attempts" failure does not exist and the debug panic degrades to a clean `Failed` run; what keeps it on the list is the reachable mid-range runaway (`retryCount: 1000`) through a UI input with no `min`/`max`.
**Files:** `src/runtime.rs:3696` (the `+ 1`), `:3697-3716` (retry loop), `:2110` (`timeout_for_node`), `:2778`, `:2832`, `:2854` (`JoinSet`), `:3729` (`retry_delay` → `Duration::from_secs`), `:4137-4146` (`apply_join_result`), `:6096`; `src/model.rs:813` (`retry_count`), `:815` (`retry_delay`), `:821` (`loop_max_iterations`), `:1326-1363` (`validate_workflow_input_bounds`); `src/api.rs:547`, `:731` (`enforce_workflow_input_bounds`); `ui/src/features/editor/InspectorPanel.svelte:991-998`
**Description:** `src/runtime.rs:3696` computes `node.retry_count.unwrap_or(0) + 1` on a `u32` with no clamp, and `validate_workflow_input_bounds` (`src/model.rs:1326-1363`) bounds only `MAX_WORKFLOW_SUBFLOWS` and `MAX_SUBFLOW_CALL_EDGES` — no per-node numeric limits, and `enforce_workflow_input_bounds` at `src/api.rs:547`, `:731` adds none. The practically important consequence is not the overflow but the mid-range: `retryCount: 1000` produces 1000 sequential agent invocations with no cap anywhere, since `timeout_for_node` (`:2110`) is per-attempt and there is no global run timeout. Ingress is the ordinary UI — `InspectorPanel.svelte:991-998` is a bare `type="number"` with no `min`/`max` and no clamp — so a typo or paste reaches the backend directly, not only a hand-edited file.

*Corrections to the original write-up:* the release-mode claim is **wrong**. The loop at `src/runtime.rs:3697-3716` is a do-while — `attempts += 1` precedes the `attempts >= max_attempts` guard — so `max_attempts = 0` executes the node exactly once and reports its real success/failure, behaviorally identical to `max_attempts = 1`. There is no silent skip and no false success. The debug panic is real (overflow checks on; no `[profile]` overrides in `Cargo.toml`) but benign: it occurs inside `run_cursor_task` spawned at `:2778`, is joined at `:2832`/`:2854`, and lands in `apply_join_result`'s `Err` arm (`:4137-4146`), emitting a `workflow_error` event with the panic message and setting `status = Failed` — an attributable failure with no unwind escape or checkpoint corruption.

*Sibling fields sharing this shape, one of them worse:* `retry_delay: Option<u64>` (`src/model.rs:815`) feeds `Duration::from_secs` directly at `src/runtime.rs:3729`, so a large value parks the run indefinitely; `loop_max_iterations` (`src/model.rs:821` → `src/runtime.rs:6096`) and `node.timeout` are likewise unbounded.

*Governing precedent:* `docs/issues/feature-tmux-panes-code-review-20260620-092605.md:63-67` resolved an identical unbounded-`u32` `maxConcurrent` finding with a runtime `clamp(1, MAX)` at the authoritative choke point, a named `MAX_PARALLEL_BATCH_CONCURRENT = 32` constant, **and** a UI clamp — reasoning explicitly that the runtime clamp is the decisive root-cause fix because it holds regardless of input source.
**Fix:** Apply the settled `maxConcurrent` remedy to `retry_count`: clamp in the runtime, bound at ingress, constrain the UI.
- Add `const MAX_NODE_RETRY_COUNT: u32 = 10;` in `src/model.rs` alongside the existing `MAX_*` constants. Pick the ceiling generously — a hard cap that is too tight will annoy authors who legitimately want ~20 retries.
- At `src/runtime.rs:3696`, use `node.retry_count.unwrap_or(0).min(MAX_NODE_RETRY_COUNT) + 1`. This is the load-bearing half: it holds for imported and legacy workflows that never pass through the API.
- Add a matching per-node check in `validate_workflow_input_bounds`, reusing the existing `ValidationIssue` error style so authors get a real message rather than silent coercion.
- Add `min="0" max="10"` to the retry-count input at `ui/src/features/editor/InspectorPanel.svelte:991-998`.
- Do **not** use `saturating_add(1)` alone: it converts the panic into `max_attempts = u32::MAX`, an unbounded retry loop that is strictly worse than current release behavior.
- Add regressions for a huge `retryCount` (clamped, run completes normally) and for the debug-overflow input specifically.
- Track as follow-up, not part of this fix: a general per-node numeric sweep covering `retry_delay`, `timeout`, and `loop_max_iterations`. `retry_delay`'s indefinite park is the worst of the family. The M-tier finding at `:330` already opens `validate_workflow_input_bounds` for node/edge caps and is the natural home for it.

---

### L5. Prompt/argv exposure — re-scoped to dead env plumbing and world-readable database (FIXED)
> **Fix:** Removed dead CommandArgs.env plumbing and the -e loop; SQLite db plus -wal/-shm sidecars now chmod 0o600 inside the pool manager's connect(); Unix regression added; argv window recorded as accepted risk.
**Severity:** LOW — the finding as filed is dismissible (dead env code plus a millisecond argv window), but the investigation surfaced a real permanent leak in the same data: the SQLite database holding every prompt is created with default permissions.
**Files:** `src/tmux_exec.rs:2106-2109` (dead `-e` loop), `:2073`, `:2299-2301` (`send_text`), `:3140` (`--` regression test); `src/driver.rs:517` (`CommandArgs.env`), `:698-700`, `:857-859`, `:985-987` (all four `env: Vec::new()` sites), `:665-667` (`--append-system-prompt`); `src/storage.rs:99` (`SqliteConnectionManager::file`), `:133` (`state_json`), `:134` (`workflow_json`), `:143` (`event_json`); `src/model.rs:801` (`Node.prompt`)
**Description:** Both cited mechanisms are real — `send_text` passes prompt text as an argv element (`src/tmux_exec.rs:2299-2301`) and `spawn_pane` appends `-e KEY=VALUE` pairs to the `tmux new-session` argv (`:2106-2109`) — but neither is meaningfully exploitable. `CommandArgs.env` is constructed at exactly four sites (`src/driver.rs:698-700`, `:857-859`, `:985-987`, `src/tmux_exec.rs:2073`) and **every one hardcodes `env: Vec::new()`**, so the `-e` loop is provably dead code iterating an always-empty vector. The prompt window is single-digit milliseconds (the tmux client is short-lived), while the agent's own argv — including `--append-system-prompt <text>` at `src/driver.rs:665-667` — is `ps`-visible for the agent's entire multi-minute lifetime and is untouched by the proposed fix. The genuine leak is elsewhere and permanent: `Node.prompt` (`src/model.rs:801`) is stored verbatim in `workflow_json` (`src/storage.rs:134`) alongside interpolated `state_json` (`:133`) and `event_json` (`:143`), and the database is opened via `SqliteConnectionManager::file` (`:99`) with **no `set_permissions` call anywhere in `src/`** — default umask, typically world-readable 0644. The same local adversary can therefore `cat` the prompts off disk indefinitely.

*Correction to the original write-up:* "`CommandArgs.env` is explicitly designed to carry environment values" is true of the type but false of the system — nothing populates it on any path, so the env half is not "no secrets yet" but no values at all.

*Fix viability note:* the reviewer's `load-buffer -` / `paste-buffer` route is not currently available. The pinned `tmux-tools` rev has no stdin plumbing (`core/src/tmux.rs:191-196` pipes only stdout/stderr and inherits stdin), so it would require an upstream change as H6 does. `paste-buffer` is also not equivalent to `send-keys -l`: it needs `-p`/`-d` handling, interacts with the target TUI's bracketed-paste mode (agents treat pasted and typed input differently), and buffer names collide across concurrent panes unless `-b`-namespaced. The `--` guard at `:2300` is deliberate and regression-tested at `:3140`.

*Prior history:* never previously raised — argv/`ps`/secrets-in-process-arguments appears nowhere in `docs/issues/`. `send_text` was reviewed twice (`code-review-20260608_part_*`) on Enter semantics only. Roughly ten prior severity rationales lean on "single-user loopback desktop" as a bounding factor.
**Fix:** Remove the dead mechanism, close the real leak, and record the argv window as accepted risk.
- Delete the `-e` loop at `src/tmux_exec.rs:2106-2109` and the `env` field at `src/driver.rs:517` together with its four `Vec::new()` initializers. It exists only to someday leak; removing it now is zero-risk.
- Set the SQLite file mode to `0o600` after creation at `src/storage.rs:99`, so prompts, interpolated state, and event payloads are not world-readable. Handle the non-Unix path with `#[cfg(unix)]` rather than failing the build.
- Verify the mode applies to the WAL and shared-memory sidecars (`-wal`, `-shm`) as well, not just the main database file — they carry the same content.
- Do **not** pursue the `load-buffer`/`paste-buffer` rewrite: it buys milliseconds against an adversary who can read the same prompts off disk permanently, requires a coordinated upstream change, and risks bracketed-paste regressions on the highest-traffic input path.
- Record explicitly that millisecond argv exposure of prompts is accepted risk for a loopback single-user desktop application, so a future review does not re-litigate it.
- Add a regression asserting the created database file is not group- or world-readable.

---

### L6. SSE `stream_run` replay/subscribe boundary has no sequence, so events can be both lost and duplicated (FIXED)
> **Fix:** Added per-run monotonic event sequence; stream_run now subscribes then replays then dedupes, emits SSE id:, and signals stream_resync on broadcast lag so the client refetches the journal.
**Severity:** LOW — the loss window is microseconds at run start where only cosmetic events exist and no hang is reachable; it stays on the list because the same boundary already duplicates events today and the naive one-line reorder would make that worse.
**Files:** `src/api.rs:873` (`list_events`), `:874` (`subscribe`), `:~898` (`Lagged` arm); `src/runtime.rs:46-51` (`RuntimeEvent`), `:928-937` (`subscribe`), `:~905` (`broadcast::channel(512)`), `:1070` (registry removal), `:6571-6574` (`emit_event`); `src/storage.rs:433-450` (`list_events`); `ui/src/lib/api/client.ts:141-205`, `:170-180`; `ui/src/app/AppShell.svelte:213` *(pre-existing, unchanged from `development`)*
**Description:** `stream_run` snapshots the persisted event journal at `src/api.rs:873` before subscribing to the live broadcast at `:874`, so an event emitted between those two awaits reaches neither path. `RuntimeEvent` carries no id and no sequence (`src/runtime.rs:46-51`), the SSE frames emit no `id:` field, and the client parses only `data:` lines (`ui/src/lib/api/client.ts:170-180`) — no Last-Event-ID, no dedupe, no gap detection, no resync. The window is one mutex acquisition after a completed `spawn_blocking` DB read, and the stream opens exactly once immediately after `createRun`, so only the earliest cosmetic events (`run_start`, first `node_start`) can fall in it; state-bearing events (`approval_required`, `agent_interaction_required` — the only ones the client cannot reconstruct, as `ui/src` has no polling fallback) arrive far later. No terminal hang is reachable: losing `done` does not wedge the loop, since teardown is driven by `RecvError::Closed` when the registry entry is removed (`src/runtime.rs:1070`), and `AppShell.svelte`'s `finally` clears `running`/`approval` regardless.

*Corrections to the original write-up:* the endpoint is **not** legacy — it remains the primary run-event channel (`ui/src/lib/api/client.ts:141-205` → `AppShell.svelte:213` → `store.applyRunEvent`); the WebSocket path added by this branch carries pane bytes only. More importantly, the **converse defect already exists**: because `emit_event` appends before broadcasting (`src/runtime.rs:6571-6574`), an event appended before `list_events` but broadcast after `subscribe` is delivered **twice** today. Duplicates are a property of the current ordering, not a new cost introduced by fixing it.

*Larger sibling in the same function:* `Err(Lagged(_)) => continue` (`src/api.rs:~898`) silently discards an unbounded number of events across a 512-event backlog with no client signal and no resync. The pane WebSocket path handles `Lagged` correctly by forcing a snapshot resync; the SSE path does not.

*Prior history:* `docs/issues/feature-tmux-panes-code-review-20260620-092605.md:359` gave `stream_run` framing an explicit clean bill ("replay + `done` short-circuit + keep-alive correct"), and structurally identical replay/subscribe boundary findings on the pane path were lowered to LOW as cosmetic (`...20260716-003531.md:148-157`). The subscribe ordering itself was never raised.
**Fix:** Give events a monotonic sequence so the replay/subscribe boundary becomes well-posed, then subscribe → replay → dedupe.
- Attach a per-run monotonic sequence to `RuntimeEvent` (`src/runtime.rs:46-51`) — either by surfacing the persisted `run_events` rowid or by stamping an `AtomicU64` at emit time in `emit_event` (`:6571-6574`).
- Reorder `src/api.rs:873-874` to subscribe **first**, then replay the journal, then drain the already-buffered receiver discarding any event whose seq is ≤ the maximum replayed seq. Reordering alone is not acceptable: with no dedupe key, a duplicated `approval_required` would re-set `store.approval` after the user resolved it.
- Emit the sequence as the SSE `id:` field so the client can detect gaps independently of server-side correctness.
- Use the new sequence to fix the `Lagged` arm at `src/api.rs:~898`: instead of `continue`, signal the client to refetch `/api/runs/{id}/events` — an endpoint that already exists and is currently unused — mirroring the pane path's snapshot-resync discipline.
- Add regressions for an event emitted inside the replay/subscribe window (delivered exactly once) and for a lagged receiver (client receives a resync signal rather than silent loss).

---

### L7. Password entered through an unmasked `window.prompt` (FIXED)
> **Fix:** Added PasswordDialog.svelte (masked native <dialog> mirroring ConfirmDialog) plus a passwordPrompt.svelte.ts deferred resolver; both window.prompt unlock sites now await it; InspectorPanel tests rewritten and component tests added.
**Severity:** LOW — real cleartext exposure, but the secret is an app-local unlock token rather than a system credential, it is correctly transported and never persisted client-side, and the backend gate is throttled and fails closed.
**Files:** `ui/src/app/AppShell.svelte:193`, `:194`, `:469`; `ui/src/features/editor/InspectorPanel.svelte:195`; `ui/src/lib/components/ConfirmDialog.svelte` (pattern to copy); `ui/src/lib/api/client.ts:80-101`; `ui/src/features/editor/InspectorPanel.test.ts:117-151`; `src/api.rs:636`, `:714`, `:759-796`; `src/app.rs:69-86`, `:100-104`, `:105-116`, `:128-170`
**Description:** The privileged-run unlock secret is collected with `window.prompt("Unlock password")` at `ui/src/app/AppShell.svelte:193` and `ui/src/features/editor/InspectorPanel.svelte:195`, rendering it in cleartext on screen and in some browsers' prompt autofill history. Everything downstream is already correct: the value is a function-local `const` passed to `api.createRun` / `api.testNode` (`ui/src/lib/api/client.ts:80-101`), serialized into a **POST JSON body** rather than a URL query string, read at `src/api.rs:636`, `:714`, never written to `localStorage`/`sessionStorage` (neither appears anywhere in `ui/src`), never held in `$state` or a store, never logged, and re-prompted on every privileged action. Secondary defect not in the original write-up: `prompt()` is suppressible by the browser's "prevent this page from creating additional dialogs" control, after which it returns `null` and every subsequent unlock silently becomes "Run cancelled" (`AppShell.svelte:194`) with no recovery path.

*Scoping — what the credential actually is:* an operator-chosen, **app-local** token compared against `SILVERBOND_UNLOCK_PASSWORD_HASH` (a `sha256:<64-hex>` value read only from the server process env, `src/app.rs:69-86`, `:105-116`) via constant-time compare behind global exponential throttling (`:128-170`). It is never a sudo or system credential and is never forwarded to sudo — the threat-model comment at `src/app.rs:100-104` records that contained launches use `sudo -u <user> -H --` without `-E`. No system-credential severity multiplier applies. Unlocking permits a `run_as`/process-spawning workflow to proceed under the operator's own uid instead of being 403'd (`src/api.rs:759-796`).

*Not to be conflated:* the third `prompt(` in `ui/src` is `GraphEditor.svelte:232`, the compound-node name prompt belonging to M1 — not a password, and out of scope here.

*Prior history:* `docs/issues/feature-tmux-panes-code-review-20260716-003531.md:165` cites `AppShell.svelte:193` only as evidence the secret is operator-chosen (low entropy), and its M6 explicitly **rejected** entropy enforcement as unenforceable. Masking was never raised.
**Fix:** Replace both `window.prompt` calls with a reusable masked dialog modeled on the repo's one established dialog pattern.
- Add `ui/src/lib/components/PasswordDialog.svelte` mirroring `ConfirmDialog.svelte`: a native `<dialog>` driven by `showModal()` from an `open` prop inside a `$effect`, `onsubmit`/`oncancel` callback props, `::backdrop` styling, and Escape handling.
- Expose `<input type="password" autocomplete="current-password">` and resolve a promise on submit or cancel, so both call sites can `await` it in place of `window.prompt`. A small deferred-resolver helper is needed to bridge the declarative modal into the existing imperative flow.
- Update `ui/src/app/AppShell.svelte:193` and `ui/src/features/editor/InspectorPanel.svelte:195` to use it; a shared component is warranted because both sites need identical UX.
- Rewrite `ui/src/features/editor/InspectorPanel.test.ts:117-151`, which currently does `vi.spyOn(window, "prompt")` and asserts `toHaveBeenCalledWith("Unlock password")`. No Playwright change is required — e2e never drives this prompt (`ui/e2e/run-agent-pane-stream.spec.ts:28`, `:37` POST `unlockSecret` directly and no spec registers a `page.on("dialog")` handler).
- Add vitest coverage for the new component, including that the input is `type="password"` and that cancel resolves without submitting.
- Note this also removes the dialog-suppression dead end: unlock currently becomes permanently impossible in a tab where the user has blocked additional dialogs, and today that path has zero e2e coverage.

---

### L8. `build_log_id` collides at one-second resolution (FIXED)
> **Fix:** build_log_id now derives log identity from run_id (slug plus UUID) instead of one-second timestamps, structurally eliminating the collision class; regression asserts two same-second runs produce distinct log rows.
**Severity:** LOW — real, unrecoverable loss of a user-visible history entry, but it requires two same-slug top-level runs terminating in the same wall-clock second, and no fan-out path multiplies logs per run.
**Files:** `src/runtime.rs:6803-6806` (`build_log_id`), `:6247`, `:6280` (sole call site in `finalize_run`), `:1354`, `:1365` (`run_{Uuid::now_v7()}`, unconstrained concurrency), `:2493`, `:3250` (inline subflow/batch execution); `src/storage.rs:152-153` (`logs.id TEXT PRIMARY KEY`), `:649-690` (`save_execution_log` upsert), `:702` (history ordering); `src/util.rs:41` (`slugify_filename`); `src/api.rs:148-149`, `:2282-2292`, `:2256`; `ui/src/lib/api/client.ts:124-127`; `ui/src/features/history/HistoryPanel.svelte:20`, `:55`, `:201` *(pre-existing)*
**Description:** `build_log_id` is `format!("{}_{}", slugify_filename(workflow_name), Utc::now().format("%Y-%m-%dT%H-%M-%S"))` — a workflow-name slug plus one-second resolution, discarding the already-unique `run_id` available on the checkpoint at the sole call site (`src/runtime.rs:6280`). `save_execution_log` upserts with `ON CONFLICT(id) DO UPDATE SET filename=…, run_id=excluded.run_id, data_json=excluded.data_json` against `logs.id TEXT PRIMARY KEY` (`src/storage.rs:152-153`, `:649-690`), so a colliding run destroys the earlier row outright — `run_id` and full `data_json` included. The loss is user-visible: these rows are exactly what `GET /api/logs` (`src/api.rs:148-149`, `:2282-2292`) feeds to `HistoryPanel.svelte:201`, so a completed run silently disappears from run history. Concurrency is unconstrained — `registry.register` is keyed by the unique `run_id` (`src/runtime.rs:1354`, `:1365`), so `AlreadyActive` can never fire for a second run of the same workflow. `slugify_filename` (`src/util.rs:41`) replaces only non-`[A-Za-z0-9_- ]` characters, so distinct names can collapse to one slug as well (`My Flow!` and `My Flow?` → `My Flow_`).

*Reachability, narrowed:* fan-out does **not** multiply logs. `build_log_id` has exactly one call site, in `finalize_run` (`src/runtime.rs:6247`, `:6280`); subflow and `Call` nodes execute inline via frames (`:3250`, results into `frame.subflow_results` at `:2493`) and `parallel_batch` items likewise — neither spawns a nested run nor calls `finalize_run`. The trigger is therefore two *separate* top-level runs of the same workflow whose terminal moments land in the same second: plausible for short workflows fired concurrently or a fast run plus `restart_run` (`src/api.rs:2256`), not for typical single-run use. Tests do not trip it — the `finalize_run` tests at `src/runtime.rs:9516`, `:10433` each use their own `TempDir` DB. No file is written; `filename` is a cosmetic `"{id}.json"` DB column.

*Sortability is not a constraint:* history is `ORDER BY start_time DESC` (`src/storage.rs:702`), not by id, so an id-format change breaks no reader ordering.

*Prior history:* no prior review covers `build_log_id` or log-id collisions. `docs/issues/feature-tmux-panes-code-review-20260620-092605.md` M19 concerns an unrelated merge-key collision.
**Fix:** Derive log identity from the run rather than from the clock.
- Change the signature to `build_log_id(workflow_name: &str, run_id: &str)` and emit `format!("{slug}_{}", run_id.trim_start_matches("run_"))`, keeping the slug as a readable prefix. Pass the `run_id` already present at `src/runtime.rs:6280`.
- This eliminates the collision class structurally: `run_id` is a UUIDv7 minted at `src/runtime.rs:1354`, so uniqueness is guaranteed rather than probabilistic, and UUIDv7 keeps the id time-sortable. It also makes the log row joinable to `runs` and to events by inspection.
- Do **not** merely raise timestamp resolution: sub-second formats narrow the window without closing it, and `Utc::now()` is not monotonic, so a backward clock step can re-collide. Do not append a random suffix either — it mints an unrelated value when a unique id is already in hand.
- Confirm the resulting id passes `safe_name` unchanged (UUID hex is alnum plus `-`, already within the allowed charset).
- Leave existing rows in the old format; no migration is needed since nothing parses the id and ordering is by `start_time`.
- Add a regression: two runs of the same workflow finalized within the same second produce two distinct log rows, both retrievable via `GET /api/logs`.

---

### L9. Non-string decide outcome fails with an imprecise diagnostic (FIXED)
> **Fix:** select_decide_outcome returns a typed selection distinguishing non-scalar and type failures, coerces numbers to label text, and keeps the no-prose-fallback contract intact.
**Severity:** LOW — *lowered*: the "abandoned fallbacks" premise is refuted, no behavior differs from the already-correct non-matching-string path, and what remains is a diagnostics polish item. ID retained as `L9` for stability.
**Files:** `src/runtime.rs:4056-4113` (`select_decide_outcome`), `:4061-4069` (structured branch), `:4063` (`as_str()?`), `:4064-4067` (exact match + `return`), `:4071-4079` (trimmed exact), `:4081-4112` (regex scan), `:4026-4041` (`decide_output_schema`), `:4007-4010` (prompt instruction), `:4043-4054` (`parse_decide_structured_response`), `:6741-6761` (`parse_structured_output`), `:3847-3858` (`run_decide_node` failure mapping), `:3860-3877` (block M7 rewrites), `:4237-4266` (`apply_join_result`), `:12166-12177` (pinning test)
**Description:** When a structured `outcome` field is a non-string, `as_str()?` at `src/runtime.rs:4063` returns `None` from the whole function. This is **behaviorally identical** to the string path: a string `outcome` that matches no label also returns `None` at `:4064-4067` without reaching stages 2–3. The consequence is a clean, correctly classified failure — `run_decide_node` (`:3847-3858`) maps `None` to `success=false`, `exit_code=1`, `stderr = "LLM response did not match any decide outcome: {labels}"`, and `apply_join_result` (`:4237-4266`) routes it to `CursorTerminalStatus::Failure`. The only real defect is diagnostic: the operator cannot tell that the agent emitted the wrong *type* rather than an unrecognized label.

*Correction to the original write-up — the central premise is false.* The fallbacks would recover nothing legitimate for a non-string, and skipping them is deliberate, tested behavior. `decide_routing_invalid_json_artifact_outcome_does_not_fall_back_to_prose` (`src/runtime.rs:12166-12177`) requires `{"outcome":"maybe","reason":"approve"}` to return `None` precisely so the prose scan cannot harvest a label from the `reason` field. Since stage 3 regexes over the raw JSON text, falling through would make `{"outcome": 1, "reason": "I would approve"}` route to `approve` — reintroducing the silent-misroute class fixed by R1-2 (`docs/issues/code-review-20260608_part_1_of_4.md:66`) and F9 (`...part_2_of_4.md:75`). **The reviewer's "fall through instead of returning" variant is a regression and must not be taken.**

*Reachability:* `outcome_value` comes from LLM text via `parse_decide_structured_response` (`:4043-4054`) → `parse_structured_output` (`:6741-6761`), a bare `serde_json::from_str` with no schema enforcement; `decide_output_schema` (`:4026-4041`) and the prompt instruction (`:4007-4010`) are prompt-only advice. So `{"outcome": true}` is possible but rare, and coercion rarely helps — `true` → `"true"` matches no realistic label (`yes`/`no`/`approve`). The one genuine case is a scalar `{"outcome": 1}` against a label literally named `"1"`.

*Prior decisions:* no prior review covers outcome type coercion; R1-2, F9, and the `:12166` test all push toward strictness.
**Fix:** Keep the strict contract, make the failure legible, and fold the change into M7 rather than landing it separately.
- Have the structured branch report *why* it failed instead of collapsing to a bare `None` — change the return type from `Option<String>` to a small result-ish enum distinguishing "no structured outcome", "outcome was not a string", and "outcome matched no label".
- Use that at `src/runtime.rs:3853` so the stderr reads e.g. `"decide agent emitted a non-string outcome (got number); expected one of: …"` rather than the current generic message.
- Fold in scalar coercion at `:4063`: map `String`/`Number`/`Bool` to their text form before exact-matching, so a legitimately numeric label still resolves. Keep arrays and objects non-matching.
- Preserve the no-prose-fallback contract exactly — the structured branch must still `return` rather than fall through, and `:12166-12177` must continue to pass unchanged.
- **Sequence with M7:** M7 rebuilds `run_decide_node`'s result construction at `:3860-3877` and already names L9 as its open sibling (`:265`). The improved stderr lands in the same `:3847-3858` block, so implement both together to avoid two edits to one path.
- Update the ~20 test call sites the return-type change touches, and add cases for a numeric outcome matching a `"1"` label, a boolean outcome (fails with the type-specific message), and an object outcome (fails, no prose fallback).

---

### L10. Unbounded hot loop with no sleep in the scheduler (FIXED)
> **Fix:** Orphan WaitingApproval cursors are reconciled at rehydration and the bare continue now fails the run like the sibling WaitingCollector arm, so the loop can no longer spin.
**Severity:** LOW — real and unbounded (one core pegged, run never terminates), but reachable only from an already-inconsistent checkpoint, with no data loss and a working abort escape hatch.
**Files:** `src/runtime.rs:2823` (the bare `continue`), `:2793` (`running_tasks.is_empty()`), `:2663` (`is_aborted`), `:2680` (abort break), `:2681` (`process_immediate_cursors`), `:2790` (`changed`-gated persist), `:2794-2798` (M15's arm), `:2801`, `:2805-2821` (`WaitingCollector` arm), `:2941-2946`, `:3441` (`activate_next_approval` early return), `:3413-3414` (`queue_approval`), `:2262-2292` (`rehydrate_checkpoint_for_execution` consistency check), `:2277` (`cancel_requested` retain), `:4599`, `:4605-4607` (`handle_approval_resolution`); `src/storage.rs:186-203` *(pre-existing)*
**Description:** The bare `continue` at `src/runtime.rs:2823` is taken when `running_tasks` is empty, no approval is active, cursors exist, and not all are `WaitingCollector`. The iteration contains no pending await — `is_aborted` (`:2663`) hits an uncontended mutex fast path, `process_immediate_cursors` (`:2681`) reaches `activate_next_approval`'s early `Ok(false)` at `:3441` and then filters zero runnable cursors (`:2941-2946`) — and `persist_checkpoint` (`:2790`) is gated by `changed`, so the branch performs no emit, no persist, and no sleep. Result: **100% of one core with zero DB writes**, and on a current-thread runtime it would also starve the executor. The run stays Running indefinitely with a frozen `updated_at`. No guard exists: no iteration counter, no elapsed check (`start_instant` at `:2640` is used only for final duration), no watchdog, and `max_total_steps` is enforced in `prepare_cursor_visit`, which never runs here.

*Correction to the original write-up — the cited reachability path is refuted.* The `cancel_requested` retain at `:2277` is immediately followed by the guard at `:2286-2292`, which detects "pending_approval set but no `WaitingApproval` cursor" and routes it to `drop_inconsistent_pending_approval`. If the dropped cursor was the only `WaitingApproval` one, the branch is unreachable; and any surviving `WaitingApproval` cursor was placed there by `queue_approval` (`:3413-3414`), which sets the state and pushes the `queued_approvals` entry in the same synchronous block, so the loop parks in the M15 arm instead. Mid-run `fail_fast_cancel` (`:5961`) always sets `terminal_reason` first, and `restart_from` (`:1467-1486`) replaces `active_cursors` wholesale.

*Actual reachable paths:* (1) `handle_approval_resolution` clears `pending_approval` at `:4599` then returns early at `:4605-4607` if the approval node is absent from the cursor's graph — effectively unreachable while `workflow_json` is frozen at INSERT (`src/storage.rs:186-203`); (2) any externally inconsistent persisted checkpoint — partial write, hand-edited DB, older-version row, or a future cursor-state bug — since `rehydrate_checkpoint_for_execution` validates pending-without-cursor but **never** cursor-without-pending.

*Distinct from M15:* M15's spin is the `active_approval.is_some()` arm at `:2794-2798`, which sleeps 250 ms and writes a full checkpoint per tick. L10's branch writes nothing at all. The fixes are adjacent in the file but functionally independent.

*Escape hatch:* abort works. `is_aborted` is the first statement of every iteration (`:2663`), so the spinning loop observes the flag within microseconds and breaks at `:2680`.

*Governing precedent:* `docs/issues/feature-tmux-panes-code-review-20260620-092605.md:341-342` handled the mirror-image inconsistency by surfacing it loudly (warn + `workflow_warn` event) and **explicitly rejected** reconstructing the approval as riskier than dropping it.
**Fix:** Close the entry path at the boundary that already owns this repair, and make the loop branch a permanent tripwire rather than the mitigation.
- Extend the consistency check at `src/runtime.rs:2286-2292` with its converse: any cursor in `WaitingApproval` with no matching `pending_approval` and no `queued_approvals` entry is reconciled through the established `drop_inconsistent_pending_approval` convention — `tracing::warn!` plus a `workflow_warn` event naming the cursor and node, then drop the orphan cursor. Follow the `20260620` precedent: surface loudly, drop rather than reconstruct.
- Decide and document the case where the orphan is the run's only cursor: dropping it leaves `active_cursors` empty, which breaks cleanly at `:2801`, so verify that path finalizes with a sensible status rather than a bare Completed.
- Replace the bare `continue` at `:2823` with the internal-error shape already used by the sibling `WaitingCollector` arm at `:2805-2821`: emit `workflow_error` naming the stuck cursor and node, set `status = Failed` and `terminal_reason = "failed"`, clear cursors, and `break`. This makes the loop total — every state either progresses or terminates.
- Do **not** simply add a sleep: that leaves the run hanging forever and merely converts a visible CPU pin into an invisible permanent stall.
- Add two regressions: a rehydrated checkpoint carrying a `WaitingApproval` cursor with empty `pending_approval`/`queued_approvals` is reconciled at load with a warn event; and, bypassing rehydration, the loop branch terminates the run as Failed instead of spinning.

---

### L11. `migrate_output_schema` misclassifies real JSON Schemas as legacy and corrupts them (FIXED)
> **Fix:** Legacy outputSchema migration is now gated to v2/v3 to v4 with a positive primitive-type predicate and the serde hook is passthrough, so real JSON Schemas round-trip intact.
**Severity:** LOW — the trigger is narrow and the schema currently feeds nothing but a prompt hint, so there is no execution or validation breakage; it stays on the list because the loss is permanent user data and it must land before or with M14.
**Files:** `src/model.rs:325-352` (`migrate_output_schema`), `:806-811` (`deserialize_output_schema` serde hook), `:973`, `:981` (`normalize_workflow_value`), `:3852-3946` (legacy-shorthand tests), `:3885-3910`; `src/api.rs:520-522` (save), `:544`, `:721`; `src/storage.rs:333`, `:335-343` (`get_run` CAS write-back), `:851`, `:871`, `:880-886`, `:943`, `:1053` (`backfill_legacy_tmux_sessions` precedent); `src/runtime.rs:3670` (`json_schema = None`), `:4026`; `src/driver.rs:895-925` (`schema_to_prompt_hint`); `docs/workflow-schema.md:392`
**Description:** Legacy detection is `!obj.is_empty() && !obj.contains_key("type") && obj.values().all(|v| v.is_string())` (`src/model.rs:325-352`), which sniffs format structurally rather than by version. A genuine schema such as `{"$ref":"#/defs/X"}` or `{"$schema":"…","$id":"…"}` satisfies it and is rewritten to `{"type":"object","properties":{"$ref":{"type":"#/defs/X"}},"required":["$ref"]}`. Because the conversion lives in a serde `deserialize_with` on the `output_schema` field (`:806-811`), it fires on **every** deserialization via `normalize_workflow_value` (`:973`, `:981`) — it is not gated on `migrate_workflow_value_to_v4` at all. The corruption is then persisted twice over: `save_workflow` normalizes and writes the migrated document over the user's `.json` (`src/api.rs:520-522` → `src/storage.rs:880-886`), and `get_run` CAS-writes the normalized workflow into `runs.workflow_json` (`src/storage.rs:335-343`). The original schema is destroyed with no backup.

*Corrections to the original write-up:* the trigger is narrower than "any real schema" — only top-level objects whose values are **all** strings qualify, so `$defs`, `properties`, `allOf`, `oneOf`, `enum`, and `items` carry object or array values and are already safe. The reviewer's proposed keyword list is therefore mostly dead weight; the live cases are `$ref`-only and `$schema`/`$id`/`title`/`description`-only schemas.

*Impact ceiling:* native schema passing is dead (`let json_schema = None;`, `src/runtime.rs:3670`) and the repo contains no JSON Schema validator, so nothing errors. The sole reader of `node.output_schema` is `wrap_prompt_for_json` → `schema_to_prompt_hint` (`src/driver.rs:895-925`), which emits a prompt line — a corrupted schema silently produces a nonsense hint such as `- $ref (#/defs/X, required)`. `decide_output_schema` (`src/runtime.rs:4026`) is constructed in memory and never deserialized, so it does not pass through this path; L9/M7 is independent.

*Prior history:* `docs/workflow-schema.md:392` documents the legacy `{"field":"type"}` shorthand and its auto-migration, pinned by tests at `src/model.rs:3852-3946`. No prior review has examined the heuristic.

**Sequencing — must land before or with M14.** M14 (`:388-406`) removes the `get_run` read-path write-back and moves canonicalization into a `Database::init()` startup pass; its fix text at `:403` calls for this ordering. While the read-path write-back exists, any `get_run` can persist this corruption; after M14 the exposure narrows to the startup pass and the write path, both of which need L11 correct anyway. Land L11 first or in the same commit — otherwise M14's startup migration would mass-corrupt every stored run in a single pass.
**Fix:** Stop sniffing on the hot path — gate the migration to genuine imports, and make the predicate sound inside it.
- Move the conversion out of the `deserialize_output_schema` serde hook (`src/model.rs:806-811`) into `migrate_workflow_value_to_v4`, so it runs only when the incoming `version < 4` and never touches a canonical v4 document. This follows the existing one-time-migration precedent at `src/storage.rs:1053`.
- Inside the v3 branch, replace the negative heuristic with a positive test: treat the object as legacy only when **every** value is one of the seven JSON Schema primitive type names (`string`, `number`, `integer`, `boolean`, `object`, `array`, `null`) and every key is a plain identifier. `{"$ref":"#/defs/X"}` fails because `"#/defs/X"` is not a type name. A closed set cannot be broken by new JSON Schema keywords the way a blocklist can.
- Accept the behavior change: v4 documents hand-written with legacy shorthand will no longer migrate. That is the point — canonical v4 is the authored format.
- Retarget the legacy-shorthand tests at `src/model.rs:3885-3910` to a v3 fixture, and add regressions asserting that a v4 document carrying `{"$ref":"…"}` or `{"$schema":"…","$id":"…"}` round-trips through save/load byte-identical.
- Do not attempt a repair pass over already-corrupted rows; the trigger is narrow enough that detection would be less reliable than leaving them.

---

### L12. Subflow cycle detection can collide the root workflow with a subflow of the same name (FIXED)
> **Fix:** Cycle detection keys vertices by a SubflowCallVertex discriminant separating root from catalog, so a root named after a subflow no longer warns spuriously.
**Severity:** LOW — warning-only and non-blocking; a spurious banner that mis-labels the root as a subflow, with no masking risk since merging only adds edges.
**Files:** `src/model.rs:2205-2216` (`validate_subflow_call_cycles`), `:2240` (warning severity), `:2252-2280` (`collect_subflow_calls`), `:2288` (Tarjan), `:865` (`WorkflowV3.name`), `:886-889` (global subflow-name contract), `:2001` (`validate_subflow_catalog`); `src/api.rs:581` (`hydrate_saved_subflows`), `:736` (error-only filter); tests `src/model.rs:4178`, `:4544`
**Description:** `validate_subflow_call_cycles` derives the root vertex key from `workflow.name`, falling back to the literal `"__root__"` (`src/model.rs:2206-2211`), then keys every subflow vertex by its catalog name (`:2214-2215`) in the same flat `BTreeMap`; `collect_subflow_calls` appends out-edges via `graph.entry(name).or_default()` (`:2257`, `:2280`), so equal names merge two vertices into one. Nothing forbids a root `name` equal to a catalog key — `WorkflowV3.name` is free-text `Option<String>` (`:865`) and `hydrate_saved_subflows` inserts under storage name into the same namespace (`src/api.rs:581`). The result is a "Subflow call cycle detected among subflows {…}" warning for an acyclic graph, mis-labeling the root as a subflow; the inverse holds too, where a blank root name collides with a catalog entry literally named `__root__`. Impact is bounded: the issue is `severity: "warning"` (`:2240`) and `src/api.rs:736` filters only `error`, so no workflow is rejected, and in the dominant hydration path (root named after a saved catalog entry whose body *is* the root) the merge is semantically correct and the reported recursion real.
**Fix:** Replace the `String` graph key with a discriminant that separates the two namespaces structurally.
- Introduce `enum Vertex { Root, Subflow(String) }` (or an equivalent `(bool, String)`) as the key type for the call graph in `validate_subflow_call_cycles` (`src/model.rs:2205`), `collect_subflow_calls` (`:2252`), and the Tarjan pass (`:2288`), updating the `BTreeMap`/`BTreeSet` generics accordingly.
- Key the root as `Vertex::Root` regardless of `workflow.name`, and every call edge as `Vertex::Subflow(callee)`. This removes the `"__root__"` sentinel and its inverse collision entirely.
- Add a small formatter rendering `Vertex::Root` as the root's display name (or "root workflow" when `name` is absent) so the warning text at `:2240` stays readable and no longer mis-labels the root as a subflow.
- Confirm real recursion is still caught: a self-referential hydrated body yields `Subflow(X) → Subflow(X)` and must still warn.
- Add regressions for a root whose `name` equals a catalog key over an acyclic graph (must produce no cycle warning) and for a catalog entry literally named `__root__` under a blank root name. `warns_on_subflow_call_cycles` (`:4178`) and `recursion_cycle_warning_includes_depth_bound_in_message` (`:4544`) both use distinct names and cover neither.

---

### L13. Resync blanks the terminal with no repaint when the socket isn't open (FIXED)
> **Fix:** Removed the eager term.reset() from resync(); resync now routes by pane status (open to requestResync, otherwise reconnect) and the stalled emit defers its reconnect so the status renders; PaneTerminal and pane-stream regressions added.
**Severity:** LOW — bounded at ~5 s with no permanently-blank path, but the premature reset discards xterm's 5000-line scrollback, and the dead `stalled` branch is a regression against S18's accepted fix intent.
**Files:** `ui/src/features/runtime/PaneTerminal.svelte:167-174` (`resync`), `:168` (unconditional reset), `:169-170` (`unavailable` arm), `:75` (label), `:101` (`scrollback: 5000`), `:152` (`onSnapshot` reset), `:201` (button `disabled`), `:299` (CSS); `ui/src/lib/api/client.ts:306-310` (`requestResync`), `:233` (`PaneStreamStatus`), `:342` (`PANE_RECONNECT_MAX_MS`), `:347` (`connecting` emit), `:385-386` (`stalled` emit), `:488-489` (close → `scheduleReconnect`); `ui/src/lib/stores/workflowStore.svelte.ts:64`, `:722-726`; `src/api.rs:2062` (snapshot opts); test `ui/src/lib/api/client.paneStream.test.ts:612`
**Description:** `resync()` calls `term?.reset()` unconditionally at `PaneTerminal.svelte:168` before knowing whether the request can be sent, while `requestResync` silently returns when `socket.readyState !== OPEN` (`client.ts:306-310`); the button is enabled whenever a run exists (`:201`), so `connecting` and `closed` are both clickable. The pane then goes blank for up to 5 s (`client.ts:342`), and the eventual repaint restores only the visible screen — `src/api.rs:2062` passes `CaptureAnsiOpts::default()` with `start: None` — so the premature reset discards the 5000-line scrollback (`:101`) rather than merely flickering. Separately, `"stalled"` is emitted once at `client.ts:385` and overwritten synchronously by `connect()`'s `"connecting"` at `:347`; since `setPaneStatus` is a bare `$state` write rendered on a microtask (`workflowStore.svelte.ts:722-726`), the DOM never shows it and the label (`:75`), CSS rule (`:299`), and `stalled` branch of `resync()` (`:169`) are all unreachable.

*Correction to the original write-up:* there is no permanently-blank path — `client.ts:488-489` unconditionally schedules a reconnect on every non-`giveUp` close, and the sole terminal state (`unavailable`, `:300-304`) is already routed to `handle.reconnect()` at `PaneTerminal.svelte:169-170`. The blanking is bounded, but it is *history-losing*, not cosmetic.

*Prior history:* `"stalled"` was deliberately introduced by S18 (`docs/issues/feature-tmux-panes-code-review-20260705-074320.md:463-466`, FIXED), whose fix text required that "the UI stops showing Live over a dead terminal." That half was never achieved, so this is a regression against an accepted fix intent rather than gratuitous dead code — which argues for repairing the status, not deleting it.
**Fix:** Make the clear snapshot-driven so no state check is needed, and make `stalled` observable so S18's intent is honored.
- Delete `term?.reset()` from `resync()` (`PaneTerminal.svelte:168`) entirely. `onSnapshot` (`:152`) already resets before writing, so the clear happens exactly when replacement bytes exist — this removes the blank window on *all* paths, including the `open` path where a send-gated reset would still clear early.
- Route `resync()` by status: `open` → `requestResync`, every other status → `handle.reconnect()`. Keep the existing `unavailable` handling, which already does this.
- Stop calling `reconnect()` synchronously after `onStatus?.("stalled")` (`client.ts:385-386`) — defer it (e.g. `setTimeout(reconnect, 0)` or a short grace window) so the status actually renders and the `stalled` branch at `PaneTerminal.svelte:169` becomes live. Do **not** delete the status; that would discard S18's diagnostic and invalidate `client.paneStream.test.ts:612`.
- Add regressions: pressing Resync while `connecting` must not clear the terminal and must trigger a reconnect; a `stalled` emission must remain observable to a status subscriber across the deferred reconnect.
- Note as an independent pre-existing issue, not part of this fix: `src/api.rs:2062` snapshots the visible pane only, so *every* reconnect repaint loses scrollback — worth tracking separately.

---

### L14. `abort_and_wait` awaits `drained_token` with no timeout (FIXED)
> **Fix:** The run task is spawned via a supervisor clearing the registry on JoinError or panic, and abort_and_wait bounds the drain await with a force-clear and warning on expiry.
**Severity:** MEDIUM — *raised from LOW*: unrecoverable without a server restart (the run id is wedged for resume, restart, and abort alike) and the hung request leaks a task with no server-side timeout; held down only by requiring a top-level panic. ID retained as `L14` for stability.
**Files:** `src/runtime.rs:951-957` (`abort_and_wait`), `:956` (unbounded await), `:952` (guard dropped), `:1071-1073` (`RunRegistry::clear`, sole `drained_token.cancel()`), `:1386`, `:1425`, `:1567` (bare `tokio::spawn` of the run task), `:1405` (`AlreadyActive`), `:1449` (`restart_from` await), `:2878-2896` (error path → `finalize_run`), `:6324-6325` (`cleanup_terminal_active_panes` before `clear`), `:4119-4145` (`apply_join_result`, child tasks only), `:5213` (per-item `catch_unwind`), `:6871-6905` (`PendingInteractionGuard` pattern); `src/api.rs:113-151` (router layers), `:2260` (`restart_run` handler); tests `src/runtime.rs:7665-7687`, `:9516`
**Description:** `abort_and_wait` cancels `abort_token` then awaits `drained_token.cancelled()` with no bound (`src/runtime.rs:956`); that token is cancelled only in `RunRegistry::clear` (`:1071-1073`), whose sole production caller is the tail of `finalize_run` (`:6325`). The run task is spawned as a bare `tokio::spawn` at `:1386`, `:1425`, and `:1567` — the `JoinHandle` is dropped immediately, nothing observes it, no `catch_unwind` wraps it, no `Drop` impl cancels the token, and `Cargo.toml` sets no `panic = "abort"` — so a panic unwinding out of `execute_workflow_to_terminal` never reaches `finalize_run`. `restart_from` awaits this at `:1449` and the axum handler `restart_run` (`src/api.rs:2260`) has no timeout layer (the router at `src/api.rs:113-151` carries only origin middleware), so the request hangs until the client gives up while the stale registry entry makes `resume_run` return `AlreadyActive` forever (`:1405`). No lock is held across the await — the map guard drops in the `let-else` at `:952` — so damage is confined to that run id plus the leaked request task.

*Verification notes:* the error-return path **is** covered (`execute_workflow_to_terminal` → `fail_workflow_after_error` → `finalize_run`, `:2878-2896`), so the finding's "outside `execute_workflow`'s covered path" wording is accurate. The existing unwind machinery is strictly inner: `catch_unwind` at `:5213` wraps a single parallel-batch item and `apply_join_result` (`:4119-4145`) handles `JoinError`/`is_cancelled` for child cursor tasks only — neither covers the top-level run task.

*Second hang path the original write-up missed:* `finalize_run` awaits `cleanup_terminal_active_panes` **before** `registry.clear` (`:6324-6325`), so a wedged tmux stalls the drain signal even when the run task is alive and healthy.

*Prior history:* not a regression, and not a previously accepted risk. `docs/issues/feature-tmux-panes-code-review-20260716-003531.md:275-287` L4 (FIXED) introduced `drained_token`, converting `abort_and_wait` from a `Notify` race to a latched token for lost-wakeup immunity; the unbounded await was accepted as correct *given a cooperative drain*. The panic/`JoinError` path was never considered there or in the `20260620`/`20260705` reviews.
**Fix:** Make every task-termination mode cancel the drain signal, and bound the await as defense in depth for the non-task stall.
- Replace the three bare `tokio::spawn` calls (`:1386`, `:1425`, `:1567`) with a supervisor that retains the `JoinHandle` and, on `Err(JoinError)`, calls `registry.clear(&run_id)`. `is_panic()` and `is_cancelled()` both land in `Err`, and `clear` is idempotent (`remove` returns `None` on the second call), so the normal path is unaffected.
- Additionally wrap the await at `:956` in `tokio::time::timeout`, force-clearing the registry entry on expiry. This is not redundant with the supervisor: it is the only thing that rescues the `cleanup_terminal_active_panes` stall at `:6324-6325`, where the task is healthy but the signal is blocked.
- Choose the timeout generously relative to `cleanup_terminal_active_panes`' own tmux budget so a legitimately slow drain is never cut short, and `warn!` with `run_id` on expiry so a force-clear is always visible.
- Known remaining gap to note in the change, not solved here: on panic the registry is cleaned but the run's DB checkpoint is still left `Running`. Reconciling persisted status on `JoinError` is a separate concern.
- Add regressions for the panic path specifically: a run task that panics must leave no registry entry and must let a subsequent `resume_run` proceed rather than returning `AlreadyActive`; and `abort_and_wait` must return within its bound when the drain signal never arrives. `abort_and_wait_returns_when_the_run_drains_concurrently` (`:7665-7687`) covers only the cooperative drain and `:9516` only `finalize_run` clearing on persistence error — there is zero panic-path coverage today.

---

### L15. `restore_pending_approval` binds a restored approval to the wrong cursor (FIXED)
> **Fix:** The restore_pending_approval fallback is now qualified by node_id match and queue ownership, falling through to the drop path otherwise; approval_required is aligned with the bound cursor; regressions added for all three branches.
**Severity:** LOW — reachable only via a corrupt or hand-edited checkpoint, with no in-process path producing the required state; retained on the list because the failure mode is a silently bypassed human approval gate with a fabricated user-success result.
**Files:** `src/runtime.rs:3477-3494` (`restore_pending_approval`), `:3482` (empty-id first matcher), `:3484-3489` (unqualified `.or_else`), `:3505-3507` (emitted prompt), `:3413-3414` (`queue_approval`), `:3444` (`activate_next_approval` `remove(0)`), `:3462-3465`, `:2680`, `:2708`, `:2924` (wholesale clears), `:4605` (resolution reads the cursor's own `node_id`), `:4614-4643` (synthetic user result), `:4707-4712`, `:2303-2325` (`drop_inconsistent_pending_approval`), `:2189`, `:2267` (rehydration-minted cursors), `:2330` (`update_checkpoint_summary`), `:133-134` (`PendingApproval::cursor_id` serde), `:558-559` (`active_cursors` serde); test `src/runtime.rs:9567`
**Description:** `restore_pending_approval` resolves the persisted approval by cursor id, then falls back at `src/runtime.rs:3484-3489` to *any* `WaitingApproval` cursor with no `node_id` check. That cursor already owns its own `queued_approvals` entry (`:3414`), and nothing prunes it — the only removals are `activate_next_approval`'s `remove(0)` (`:3444`) and wholesale `.clear()` on abort/fail (`:2680`, `:2708`, `:2924`), with no `retain` anywhere in `src/`. The cursor is therefore resolved twice: once from the restored oneshot, then again after re-activation (`:3444` → `:3462-3465`), each time stamping an `agent: "user"`, `success: true`, `"[Approved by user]"` result (`:4614-4643`) onto the cursor's *then-current* node, because `handle_approval_resolution` reads the cursor's own `node_id` at `:4605` rather than `pending.node_id`. A human gate is bypassed and a fabricated approval result lands on an unrelated node.

*Reachability correction to the original write-up:* the fallback is **not** the legacy-checkpoint path. The first matcher at `:3482` already accepts any `WaitingApproval` cursor when `pending.cursor_id.is_empty()`, and legacy rows deserialize with an empty id (`#[serde(default, skip_serializing_if = "String::is_empty")]`, `:133-134`; `active_cursors` `#[serde(default)]`, `:558-559`), so they take the first branch. The `.or_else` fires only for a **non-empty but unmatched** id, and `queue_approval` sets `WaitingApproval` and pushes the queued entry in one synchronous block (`:3413-3414`) with no path removing such a cursor while `pending_approval` points at it — so no ordinary run, restart, or resume produces that state. Entry requires a corrupt, hand-edited, or partially-written DB row. This mirrors the same review's L10 reachability correction (`:661`).

*Pre-existing mismatch inside the fallback:* the emitted `approval_required` carries `pending.node_id`/`prompt` (`:3505-3507`) while resolution applies to the bound cursor's current node (`:4605`) — the user is shown node X and node Y receives the result.

*Prior history:* the fallback is **not** deliberate recovery behavior added by a prior fix — `git log -L3477,3494` traces it to the initial commit `d54da2f`. Qualifying it therefore reverses no prior decision, though it does soften the "surface loudly, drop rather than reconstruct" precedent recorded at `…20260620-092605.md:341` and re-ratified at `…20260720-032528.md:669-671`.
**Fix:** Qualify the fallback so it can only bind a cursor that provably owns the persisted approval, and fall through to the drop path otherwise.
- In the `.or_else` at `src/runtime.rs:3484-3489`, require both that `cursor.node_id == pending.node_id` and that the cursor has no entry of its own in `checkpoint.queued_approvals`. This keeps recovery working for the genuine case — a rehydration-minted cursor (`:2189`, `:2267`) whose `node_id` matches `pending.node_id` via `update_checkpoint_summary` (`:2330`).
- When no cursor satisfies both conditions, fall through to `drop_inconsistent_pending_approval` (`:3492` → `:2303-2325`) so the inconsistency is warned and surfaced rather than guessed at.
- Handle the ambiguous case explicitly: if more than one `WaitingApproval` cursor matches `pending.node_id`, do not pick one — fall through to the drop path and `warn!` with the candidate cursor ids.
- Fix the prompt/resolution mismatch in the same change: the `approval_required` emitted at `:3505-3507` must describe the node the resolution will actually apply to (`:4605`), not `pending.node_id`, so the two can never disagree once the fallback binds.
- Add regressions for the fallback branch, which has none today: a non-empty unmatched `cursor_id` with a node-id-matching idle cursor must bind and resolve exactly once; the same with a node-id mismatch must drop and warn; and a bound cursor must never be resolved a second time via `activate_next_approval`. `resume_warns_when_pending_approval_has_no_waiting_cursor` (`:9567`) exercises only the `:2286-2292` drop and never reaches `:3484`.

---

## Conventions Verified (No Issues Found)

Union of both reviewers' verified-clean areas, deduplicated. Three entries are **narrowed** from their original wording because adjudicated findings fall inside their original scope — noted inline.

- **Origin / CSRF enforcement** — `src/api.rs:939-984`: prefix checks are colon-anchored (`http://localhost:`, `http://127.0.0.1:`), so `localhost.evil.com` cannot pass; mutations require same-origin or an allowlisted `Origin`; non-browser clients with no `Origin` are rejected; `Sec-Fetch-Site: cross-site` is a hard reject.
- **Stream-token authentication** — `src/api.rs:871-1040`: constant-time comparison, token never in a URL (SSE uses `X-Stream-Token`, the WebSocket uses the `Sec-WebSocket-Protocol` subprotocol), no run-existence leak in the error path, no token in any log.
- **`constant_time_eq`** — `src/util.rs:5-15`: length-independent accumulation loop, correct for the SHA-256-hex comparison at `src/app.rs:112`.
- **Unlock gate** — `src/app.rs:100-169`: documented threat model, exponential failed-attempt throttle (5 s → 30 s cap), `SecurityConfig` non-serializable and never logged/persisted, `sudo -u <user> -H --` without `-E`.
- **E2E unlock credentials** — test-only, per-run random secret; no production path reads `SILVERBOND_E2E_UNLOCK_SECRET`. No backdoor.
- **`send-keys` injection** — `send_text` uses `-l --`, so prompt text is literal and cannot smuggle options; yes/no replies use the same path.
- **Shell quoting** — `src/tmux_exec.rs:2528` implements correct POSIX `'…'` → `'"'"'` escaping; `wrap_keep_open` double-quotes correctly through the nested `zsh -lic`. (`resolve_tmux_bin`'s `-i` flag is intentional per project decision record.)
- **tmux session-name injection / traversal** — `sanitize_tmux_name` reduces to `[A-Za-z0-9_-]`, `normalized_session_name` forces the `silverbond-` prefix, `unique_session_name` appends a full UUID. (Socket names are *not* covered — see M18.)
- **Cross-run pane *targeting*** *(narrowed)* — send/wait/capture/kill resolve the target then check `owns_pane`/`owns_session` against the run-scoped registry; no path reaches tmux before the ownership check. This establishes *whose* pane it is only — it does **not** cover launch-configuration compatibility (H1) or reaper name-space collisions (M4).
- **`access_profile_override` on the `run_agent` path** — rejects any non-`read-only` override under `ReadOnly`, `full-access` under `Edit`/`Execute`, and unknown profile names.
- **Fail-closed interaction defaults** — `escalate_or_fallback` / `permission_request_reply` default to `"n"` with no interaction channel; destructive matches always escalate even with `auto_approve`.
- **Regex safety** — all patterns go through the Rust `regex` crate: linear time, no backtracking, no ReDoS. Invalid skip regexes validated at submission.
- **String slicing panics** — all byte indices used for slicing in the compound-extraction code are char boundaries; no panic path found.
- **Pane-stream refcounting and FIFO handling** *(narrowed)* — `PaneStreamGuard`/`PaneStreamRegistry` refcounting (`WeakSender` + `same_channel`), `PaneStream::poll_read` FIFO state machine, `WouldBlock`/`Interrupted` handling with no spin, `PaneStreamFifoGuard` unlink, FIFO permissions, `ensure_run_as_can_traverse_root`. Excludes the timeout primitive (H6) and teardown scoping (M12).
- **Stream fan-out and backpressure** — subscriber limits, sender-identity cleanup, lag resynchronization, bounded broadcast channels, invocation-scoped stream keys.
- **Concurrency / lock discipline** — `RunRegistry` always clones-and-releases `inner` before taking any per-run sub-mutex (no nesting, no lock-order inversion); `handle_parallel_batch_node`'s `parent_index` stays valid across its abort-token select, with `catch_unwind` per item; `apply_join_result` handles `JoinError` including `is_cancelled`; `PendingInteractionGuard` arm/disarm/Drop is correct.
- **Termination proofs** — `record_output_hash` stagnation window, `nearest_collectors_for_node` BFS, `hydrate_saved_subflows` loop, `collect_descendants` cycle guard, `compute_graph_metadata` BFS, `subflow_strongly_connected_components` as iterative Tarjan with explicit DFS frames (no native recursion → no stack overflow on deep chains).
- **`src/session.rs` deletion** — fully migrated: `pub mod session` removed; zero references to `session::`, `SessionManager`, `SessionSummary`, `SessionEntry` anywhere in `src/`, `tests/`, `src-tauri/`, `ui/src/`; `expectrl` removed from `Cargo.toml`; routes removed with their only consumer (`SessionHistory.svelte`); interaction escalation survived behind `/api/runs/{run_id}/respond-interaction`.
- **Schema versioning** — `migrate_workflow_value_to_v4` accepts only 2/3/4 and hard-errors otherwise; versions 0/1/5/99, a missing `version`, a string `"4"`, and a nested subflow declaring version 9 are all rejected, while v2/v3 flat nodes migrate into `kind`.
- **Workflow ingress and graph rules** — nested catalogs and oversized graphs rejected; subflow/call resolution remains root-catalog scoped.
- **Templates and stored workflows** — all six `templates/*.json` plus `workflows/Test Workflow.json` are `version: 4`, nested `kind` shape only, normalize and validate with zero errors, byte-stable across normalize → serialize → normalize.
- **`NodeKind` serde round-trip** — internally-tagged enum with `#[serde(flatten)] base: AgentDefaults` round-trips `model`, `maxTurns`, `maxBudgetUsd`, `accessMode`, `allowedTools`, `disallowedTools`; v2 top-level `agentConfig` migrates into `kind`; a stale top-level `decideConfig` on a v4 node is correctly ignored.
- **SQLite migrations and pooling** — `ensure_runs_stream_token_column` / `ensure_runs_tmux_invocation_columns` are idempotent `PRAGMA table_info` + `ALTER TABLE`; NULL/empty `stream_token` backfilled at init; `backfill_legacy_tmux_sessions` transactional and idempotent; WAL + `synchronous=NORMAL` + 5 s `busy_timeout`; `remove_tmux_sessions` opens with a write so the busy handler applies. All 15 storage tests pass. Every DB call wrapped in `spawn_blocking` over an r2d2 pool.
- **Immutable run-snapshot invariant** — `upsert_run` omits `workflow_json` and `stream_token` from `DO UPDATE SET`; asserted frozen across upsert and `update_run_checkpoint`.
- **Database/API hot paths** — per-pane observability lookups concurrency-bounded; no N+1 query or unbounded in-memory buffer found.
- **Frontend conventions** — zero `svelte/store` imports anywhere in `ui/`; runes-only throughout; `lib/stores/nodeMetadata.ts` is a plain const map.
- **XSS** — no `{@html}` anywhere; pane bytes go only to xterm `write()`; xterm not configured with the links/web-links addon; `attachCommand`, `paneLabel`, node names, and interaction text are plain-text interpolation.
- **Frontend event-stream epoch invalidation** *(narrowed)* — `streamRun` cancels the reader and honours owned and external `AbortSignal`s; the pane WebSocket `close()` clears the heartbeat interval, reconnect timer, and socket, with every handler guarding on `ws !== socket`; `ResizeObserver` disconnected on teardown; `runEpoch` correctly invalidates in-flight `kickoff`, `applyRunEvent`, and superseded `finally` blocks. Scoped to the **event stream** only — it does not cover which fields `beginRunStream` fails to reset (M5).
- **Frontend memory bounds** — `store.lines` capped by `capRunLines`/`MAX_RUN_LINES`, xterm `scrollback: 5000`, undo stack capped by `MAX_UNDO`.
- **Test hygiene** — no `#[ignore]`, `todo!`, `unimplemented!`, `test.skip`, `test.fixme`, or `.only(` in any new or modified test; `ui/vite.config.ts` correctly gained `ws: true` on the dev proxy; `src/test_support.rs` is a 5-line `#[cfg(test)]`-gated helper.
- **Executable coverage** — Codex ran the Rust suite: **331 library tests passed**; nine tmux/socket tests were sandbox-inconclusive (`EPERM` on local socket operations). Claude could not run `cargo test` at all (`libsqlite3-sys` build script `PermissionDenied`) and could not independently confirm. Frontend: `npm test` 88 pass; `npx svelte-check` **fails** with the 4 errors in M2.

## Smells (Judgement — not reranked)

Advisory only — no C/H/M/L rank, never displaces the findings above. Merged into `docs/issues/SMELLS-LEDGER.md` on 2026-07-20 (4 appended, 2 incremented). The in-diff Duplicated Code smell at `src/tmux_exec.rs:1180-1190` / `:2149-2192` was **promoted to M3** under the fail-closed promotion rule and is therefore absent from this list.

- `src/runtime.rs:1-12785` — Divergent Change: a single 12.8k-line module owns scheduling, cursors, split families, collector barriers, approvals, interaction escalation, subflow hydration, tmux reaping, and log-id minting · fix: split the split/collector state machine out from the run scheduler.
- `src/model.rs:2061` — Feature Envy: `validate_subflow_node_config` rebuilds and walks the *callee's* graph, terminal nodes, and variable set rather than asking the subflow for them · fix: move a `validate_call_against(&self, node)` method onto the subflow type (also removes the M10 recomputation).
- `src/driver.rs:291-305` — Primitive Obsession: access profiles are bare `String`s and the privilege ordering is expressed as literal name comparisons · fix: give the profile a small type carrying an explicit privilege rank.
- `ui/src/features/editor/InspectorPanel.svelte:1` — Divergent Change: one component (1,176 lines changed) hosts node config for every node kind plus agent capability probing, access-mode reconciliation, and unlock prompting · fix: extract a per-node-kind config panel and lift capability/unlock concerns to the shell.
- `src/api.rs:1375` — Divergent Change *(count 2)*: Axum handling, tmux process control, POSIX FIFO ownership, and the pane-stream protocol share one large API module · fix: move the transport behind a private `pane_stream` module exposing only start, stop, and read operations.
- `src/api.rs:1498` — Mysterious Name *(count 2)*: `termination_path` and `termination` carry both the ready and terminated lifecycle markers · fix: rename to `lifecycle_path` and `lifecycle`.

## Fix-Review Follow-ups (2026-07-21)

### H7. M6's extraArgs validator is bypassed by glued clap short-option syntax, leaving the access-ceiling escalation channel open (FIXED)
> **Fix:** Replaced the bypassable extra_args denylist with an exact per-agent allowlist that rejects glued/clustered/abbreviated/unknown/access-changing args; added regressions for -sdanger-full-access, -anever, -csandbox_mode=danger-full-access plus positive coverage.
**Severity:** MEDIUM — mechanically certain silent access widening, but `extra_args` is author-only with no interpolation or third-party injection path; consistent with M6's own adjudicated lowering to MEDIUM. ID retained as `H7` for stability.
**Files:** `src/model.rs:2285-2315` (validator), `:2318-2326` (`matches_long_flag`), `:3303-3464` (existing tests); `src/api.rs:743-758` (error gate); `src/tmux_exec.rs:2264-2270` (`build_agent_command` arg chaining); `src/driver.rs:929-936` (codex ReadOnly session args)
**Description:** The `extra_args` access-ceiling validator at `src/model.rs:2285-2315` splits each arg on the first `=` and then compares the flag as a whole token, so clap's attached-value short syntax escapes it entirely — `-sdanger-full-access`, `-anever`, and `-csandbox_mode=danger-full-access` match neither the exact short tests nor `matches_long_flag` (`:2318-2326`), which short-circuits on a `--` prefix. Validation is a hard gate (`src/api.rs:743-758` rejects a run start on any `severity == "error"` issue), so a clean pass means the run launches. `build_agent_command` chains `extra_args` after the driver's session args (`src/tmux_exec.rs:2264-2270`) and codex's flags are `ArgAction::Set`, so the glued flag overwrites the `--sandbox read-only -a never` emitted for `AccessMode::ReadOnly` (`src/driver.rs:929-936`). A workflow declaring `accessMode: read_only` with `extraArgs: ["-sdanger-full-access"]` therefore validates clean and launches codex fully unsandboxed — the exact silent downgrade M6 was filed to close. Existing tests (`:3303-3464`) cover spaced, `=`-joined, and abbreviated long forms but no glued short.
**Fix:** Replace the reject-known-bad denylist with a per-agent allowlist, so the untrusted free-text field fails closed instead of being audited one parse trick at a time.
- Per agent, permit only a curated benign set — `--model`, `--search`, plain positionals, and `-c`/`--config` whose key is not sandbox/approval-related — and reject every other arg with an error naming the offending arg and why.
- Reject unknown syntax as unknown: glued shorts (`-sdanger-full-access`), clustered shorts, and abbreviated long forms all fall through to rejection without needing enumeration, closing the escalation class permanently rather than per-form.
- Keep enforcement on the validation side only. Argv ordering is settled twice — `docs/issues/code-review-20260608_part_2_of_4.md:73` (F5, FIXED: driver `build_session_args` is "the single source of all dynamic config/safety flags", encoded by the test at `src/tmux_exec.rs:3332-3335`) and `20260620-092605:176` — so do **not** reorder `extra_args` ahead of session args.
- Provide a migration story for workflows using a legitimate but unlisted flag: enumerate flags in existing `workflows/` and `templates/*.json` before landing, and seed the allowlist from that survey.
- Add regressions for `-csandbox_mode=danger-full-access`, `-sdanger-full-access`, and `-anever` (all rejected), plus positive cases for each allowlisted flag so the list is pinned by test.
- *Prior history:* incomplete prior fix, not a regression. M6's fix text mandated "normalize-then-match, **not** naive string equality" and enumerated regressions for spaced, `=`-joined, abbreviated, and `-c sandbox_mode=…` forms — glued shorts were never listed.

---

### H8. H6 leaves run startup and traversal probe on the cancellation-unsafe timeout primitive (FIXED)
> **Fix:** Extracted H6's cancellation-safe timeout runner into a new shared src/proc.rs (status + tempfile-backed output variants, no reader threads), routed the two remaining unpatched call sites (resolve_tmux_bin_with_timeout, ensure_run_as_can_traverse_root) through it, and added an fd-inheriting-descendant regression; left the zsh -lic flag and the Cargo rev pin untouched.
**Severity:** HIGH — narrower trigger than H6 (requires `run_as` configured), but a hang on the resume path leaves the run permanently `AlreadyActive` and unstartable, and every occurrence irreversibly leaks a tokio blocking-pool thread plus an orphan process group.
**Files:** `src/tmux_exec.rs:127` (`resolve_tmux_bin_with_timeout`), `:119`/`:124` (`zsh -lic`); `src/api.rs:1738` (`ensure_run_as_can_traverse_root`), `:1736-1737` (probe's own `Stdio::null()`), `:1649-1698` (H6's inline status-only runner), `:1683` (group kill), `:1710`, `:224`; `src/runtime.rs:3412` (unbounded await), `:3408`, `:1393`, `:1438`, `:1441`, `:1557`, `:2764`; `Cargo.toml:28` (rev pin `69173d1`); upstream `core/src/tmux.rs:192`, `:196`, `:225-231`, `:249`
**Description:** Two call sites still route through the unpatched primitive: `resolve_tmux_bin_with_timeout` (`src/tmux_exec.rs:127`) and `ensure_run_as_can_traverse_root` (`src/api.rs:1738`). `command_output_with_timeout` sets `stdout(Stdio::piped()).stderr(Stdio::piped())` as the **first** statement of its body (upstream `core/src/tmux.rs:192`, `:196`), unconditionally overriding the probe's own `Stdio::null()` at `src/api.rs:1736-1737`, and its timeout arm is verbatim `child.kill(); child.wait(); join_reader(stdout); join_reader(stderr)` with `join_reader` an unbounded `handle.join()` over `read_to_end` (`:225-231`, `:242-249`). Any descendant inheriting the write end — an interactive `.zshrc` that backgrounds anything under the intentional `zsh -lic` (`src/tmux_exec.rs:119`), or `sudo` fork-and-wait leaving the exec'd child alive — therefore withholds EOF and blocks the join forever after the direct child is SIGKILLed. `src/runtime.rs:3412` awaits that `spawn_blocking` with no outer `tokio::time::timeout`, so `start_run` (`:1393`), resume (`:1441`), restart (`:1557`), `execute_workflow` (`:2764`) and the HTTP helper `src/api.rs:224` hang indefinitely; because `registry.register` runs before resolution on the resume path (`:1438`), the run is left permanently unresumable. Precondition: `run_as` must be configured (`:3408` short-circuits to `Ok(None)` otherwise).
**Fix:** Extract H6's cancellation-safe implementation into a shared command-runner seam and route both remaining call sites through it.
- Generalize the inline, private, status-only runner at `src/api.rs:1649-1698` (`run_tmux_status_with_timeout` — `process_group(0)`, null stdio, `libc::kill(-pgid, SIGKILL)` at `:1683`) into its own module (e.g. `src/proc.rs`), exposing both a status variant and an output-capturing variant.
- Implement capture **without pipes**: redirect stdout to a temp file via `Stdio::from(File)` and read it after the wait, so there are no reader threads to join and the deadline holds regardless of process topology.
- Route `src/tmux_exec.rs:127` and `src/api.rs:1738` through the new seam, removing the last `tmux::command_output_with_timeout` call sites in the repo.
- Do **not** take the upstream route: H6's fix note records "no upstream publish access", and `20260716-003531:71` rules `tmux-tools-core` "not editable in this repo" on the R2-1 precedent (`docs/issues/code-review-20260608_part_3_of_4.md:85`) of staying entirely within SilverBond. Leave `Cargo.toml:28` pinned at `69173d1`.
- Add a regression where a run-as wrapper forks a descendant that inherits the output pipes and delays the tmux command; assert run-invocation resolution returns by its deadline and that no reader thread remains. Nine tmux/socket tests are EPERM-inconclusive under sandbox, so the test must not depend on real socket operations.
- *Prior history:* not a regression — H6 named all three paths and consciously scoped its fix to enable/teardown, recording the traversal-probe remainder as unpatched. H8 is the legitimate re-raise of that deferred remainder.

---

### H9. H4's new kind-shaped backfill omits `capture` and `kill` — the two node kinds the backend actually does omit — so an ordinary save/reload still blanks the editor (FIXED)
> **Fix:** Made captureConfig/killConfig optional in the NodeKind union and rewired the InspectorPanel accessors to `?? DEFAULT_*` fallbacks matching their spawn/send/wait siblings, so configless capture/kill nodes render defaults after save/reload; added normalize/inspector regressions; backfill left untouched.
**Severity:** MEDIUM (top of band) — reachable from ordinary backend data with a single save/reload, unlike H4's import-only ingress, but the failure is scoped to selecting the node rather than loading the canvas; matches M2's calibration for the identical `sendConfig` defect. ID retained as `H9` for stability.
**Files:** `ui/src/lib/types/workflow.ts:232-233` (required decls), `:514-527` (`backfillRequiredKindConfig`), `:620-631` (legacy arm, still correct); `ui/src/features/editor/InspectorPanel.svelte:274-280` (unguarded accessor), `:262-272` (correct sibling pattern), `:1455`, `:1497`; `ui/src/lib/stores/nodeMetadata.ts:45,47`; `src/model.rs:732,736` (`skip_serializing_if`), `:720`, `:724`, `:728`, `:740` (same-family twins); `src/api.rs:527-534`; `src/storage.rs:978`; `ui/src/lib/types/workflow.normalize.test.ts:57-64`
**Description:** `captureConfig`/`killConfig` are declared required at `ui/src/lib/types/workflow.ts:232-233`, but the backend omits them whenever they equal the default (`src/model.rs:732,736`) — exactly the state of a freshly added Capture/Kill node, since `nodeMetadata.ts:45,47` produce `{all:false,ansi:false}` and `{}`, both `is_default`. Saves round-trip through the typed `WorkflowV3` (`src/api.rs:527-534` → `src/storage.rs:978`), so the field genuinely leaves the wire. On reload the node arrives as `{"type":"capture"}`, `backfillRequiredKindConfig` falls through to `default: return node` (`workflow.ts:525-526`), and `InspectorPanel.svelte:275` returns `undefined` with no `?? DEFAULT_*` fallback — unlike its spawn/send/wait siblings at `:262-272`. Selecting the node throws `TypeError: Cannot read properties of undefined (reading 'target')` at `InspectorPanel.svelte:1455` (kill: `:1497`); with no `<svelte:boundary>` anywhere in `ui/src`, the editor blanks with no recovery.

*Corrections to the original write-up:* (1) capture/kill are **not** unique in carrying `skip_serializing_if` — `spawnConfig` (`:720`), `sendConfig` (`:724`), `waitConfig` (`:728`), and `runAgentConfig` (`:740`) all do; capture/kill are distinctive only in that their accessors lack the default fallback. (2) The legacy arm still backfills correctly (`workflow.ts:620-631`), so only already-`kind`-shaped nodes are affected. (3) `flowNodes.ts` never dereferences these configs (only a color map at `:124-125`) and `updateConfig` merges over `DEFAULT_*` (`:404-411`), so the canvas loads fine — the blank occurs on selection, not on load.
**Fix:** Make the type match the wire contract rather than backfilling at one ingress — the continuation of M2's ruling, not H4's.
- Change `captureConfig` and `killConfig` to optional in the `NodeKind` union (`ui/src/lib/types/workflow.ts:232-233`).
- Rewrite the two accessors at `InspectorPanel.svelte:274-280` to match their spawn/send/wait siblings verbatim: `node.kind.captureConfig ?? DEFAULT_CAPTURE_CONFIG`, and the same for kill.
- Do **not** add `case "capture"` / `case "kill"` to `backfillRequiredKindConfig`: M2 ruled that a `skip_serializing_if` config must stay optional and be optional-chained, because a required type is a lie for any ingress that bypasses `normalizeWorkflowNode`. H4's backfill approach is correct only for decide/batch/subflow/call, which the backend always emits.
- Run `just typecheck` after the union change and guard any other deref sites it surfaces.
- Add kind-shaped normalize/inspector regressions for a configless `capture` and `kill` node — selecting each must render defaults without throwing. `workflow.normalize.test.ts:57-64` currently covers only the legacy path.
- *Related, already tracked:* M2 flagged `spawnConfig` and `waitConfig` as latent twins of this family; their accessors already carry the fallback, so they are not part of this fix.

---

### M19. M14's startup migration bricks boot on a single bad run row (FIXED)
> **Fix:** upgrade_run_workflow_json now warns-and-skips rows with invalid JSON or an unmigratable schema instead of bare ?-propagating out of init/boot, and narrows the SELECT to skip already-v4 rows (with a NOT json_valid guard added for SQLite safety); added a damaged-row regression asserting boot succeeds without rewriting.
**Severity:** MEDIUM (lower band) — unreachable with today's writers, but a genuine upgrade-path landmine: the next `WORKFLOW_SCHEMA_VERSION` bump without a matching migrate arm bricks boot for every user at once, with no in-app recovery.
**Files:** `src/storage.rs:1247-1268` (`upgrade_run_workflow_json`), `:1257-1258` (bare `?`), `:1249-1252` (unbounded scan), `:1260-1262` (write-back suppression), `:223` (init call), `:239-240`, `:247-249` (`upsert_run` normalize + freeze), `:390` (`get_run`), `:914-943` (`WorkflowStore::list` warn-and-skip), `:1210-1212` (`backfill_legacy_tmux_sessions` skip), `:1907`, `:2010` (existing tests); `src/app.rs:344`; `src/main.rs:19-23`; `src/model.rs:1135-1142` (`migrate_workflow_value_to_v4`)
**Description:** `upgrade_run_workflow_json` SELECTs every `runs` row and applies `serde_json::from_str` (`src/storage.rs:1257`) and `normalize_workflow_value` (`:1258`) with bare `?`. The caller chain `init()` (`:223`) → `Application::boot` (`src/app.rs:344`) → `main` (`src/main.rs:19-23`) propagates that Err all the way out, so one failing row aborts process startup with no recovery short of hand-editing SQLite — where before M14 the same row failed only its own `get_run`. The realistic trigger is schema evolution rather than corruption: `migrate_workflow_value_to_v4` (`src/model.rs:1135-1142`) accepts only 2, 3, and the current `WORKFLOW_SCHEMA_VERSION` and hard-bails otherwise, so a bump to 5 without adding a `4` arm makes every stored row bail and every user's server refuse to boot. The scan is also unbounded — no version guard, no `LIMIT`, a full parse+normalize+reserialize per row on every boot (`:1249-1252`), though write-back is correctly suppressed when unchanged (`:1260-1262`).

*Correction to the original write-up:* no current writer can produce a bad row — `upsert_run` normalizes before INSERT (`:239-240`) and freezes `workflow_json` on conflict (`:247-249`), and the only other writer is this migration itself. The defect is latent today and severe on the next schema bump. The divergence-from-convention point is stronger than stated: besides `WorkflowStore::list` (`:914-943`), the *sibling init migration* `backfill_legacy_tmux_sessions` already skips unparseable rows at `:1210-1212`.
**Fix:** Restore the file's damaged-data convention at the seam that aborts boot, and narrow the scan.
- Replace the bare `?`s at `src/storage.rs:1257-1258` with a `match` that `tracing::warn!`s the `run_id` and `continue`s, mirroring `:914-943` and the sibling migration at `:1210-1212`. The affected run then fails only at `get_run`'s own `?` (`:390`) — exactly the blast radius M14 had.
- Add `WHERE json_extract(workflow_json,'$.version') IS NOT 4` to the SELECT at `:1249-1252` so the steady-state query returns zero rows. This is the same narrowing-predicate remedy already ruled for this identical smell at `20260716-003531` L1, where `PRAGMA user_version` bookkeeping was explicitly considered and rejected as disproportionate.
- Accept that the narrowing predicate skips v4 rows `ensure_defaults` would otherwise canonicalize; those rows are already valid and are canonicalized on their next write.
- Add a regression: a `runs` row with garbage `workflow_json` must leave `init()` succeeding with a warn, and must not be deleted or rewritten. Existing coverage (`:1907`, `:2010`) exercises only well-formed v3 and v4 rows.
- Do **not** move repair back onto the read path — M14 explicitly banned writes in `get_run`.
- *Deferred alternative:* quarantining the row (a `runs_upgrade_failures` marker surfaced through `get_run` and the run list) is the ideal end state but disproportionate while nothing produces bad rows; revisit if one is ever observed.

---

### M20. H3 freshness hook destroys working-tree `public/` and validates the wrong tree (FIXED)
> **Fix:** Freshness pre-commit hook now builds and diffs only the staged snapshot (--outDir into a temp dir, symlinked node_modules) so it never touches working-tree public/; added a real-Vite partial-stage regression test.
**Severity:** MEDIUM — a real false-pass in the freshness guarantee plus a destructive, false-rejecting side effect on every run; bounded by being git-recoverable and reachable only for developers who opted into the hook.
**Files:** `.githooks/pre-commit:29` (snapshot), `:31` (live-tree build), `:33` (diff), `:24-27` (EXIT trap), `:36` (reject exit), `:8-10`, `:12-16`, `:18-21`; `ui/vite.config.ts:25-27`; `package.json:8`; `justfile:23-26`
**Description:** `.githooks/pre-commit:29` materializes an index snapshot via `git checkout-index --all --prefix=…`, but `:31` runs `npm run build` against the **live working tree** and `:33` diffs the snapshot against it — the snapshot is never built, serving only as a comparison baseline. The check therefore compares staged assets against a bundle built from staged *plus unstaged* sources, admitting commits whose `public/` does not correspond to their own `ui/src` (stage subset A of `ui/src`, leave B unstaged, stage assets built from A+B → hook rebuilds A+B → match → pass) and falsely rejecting ordinary partially-staged commits with a message that misdescribes the cause. Because `package.json:8` runs `vite build --config ui/vite.config.ts` and `ui/vite.config.ts:25-27` sets `outDir: ../public` with `emptyOutDir: true`, the check also wipes and rewrites the developer's `public/` on every non-early-exit run, including the reject path at `:36`; if vite fails mid-build, `set -e` aborts with `public/` emptied. The `trap cleanup EXIT` at `:24-27` removes only the temp dir.

*Impact bound:* the index is never mutated, so committed content is never corrupted — working-tree damage only. `public/` is fully tracked and nothing is permanently lost, but recovery needs **two** commands: `git checkout -- public/` restores the tracked files and `git clean -fd public/` is additionally required because the rebuild emits new content-hashed filenames that are untracked. The hook does early-exit without building when `ui/src` is unstaged (`:8-10`) and rejects fast when `public/` is unstaged (`:12-16`). There is zero test or CI coverage of the hook itself.
**Fix:** Build inside the snapshot with an explicit out-directory override, so the check never writes outside its temp dir.
- In `.githooks/pre-commit`, `cd "$SNAP"` and run `npm run build -- --outDir "$SNAP/out" --emptyOutDir`, then compare `diff -qr "$SNAP/public" "$SNAP/out"`. Output and baseline never collide, so no copy-aside step is required and the live working tree is never touched on either the pass or the reject path.
- This makes the snapshot at `:29` load-bearing rather than decorative: build input and comparison baseline both come from the index, closing the false-pass and the false-reject together.
- Ensure `node_modules` is reachable from the snapshot (symlink the repo's, consistent with the existing `node_modules` presence check at `:18-21`). Note that root `node_modules` is the relevant one; `ui/node_modules` is an empty stub.
- Add a hook regression covering staged-source-plus-unstaged-source divergence: a partial stage over `ui/src` with correctly staged assets must pass, and staged assets built from unstaged sources must fail. There is currently no coverage at all.
- Prefer the flag over restructuring `vite.config.ts`: `--outDir` keeps "this build must not touch the repo" explicit and greppable at the call site. Guard against drift by asserting in the regression that the config's own `outDir` is not written.
- *Recurrence context:* the false rejects and the `public/` clobber are exactly what would get this hook uninstalled — the abandonment pattern behind H3's three prior recurrences. A pre-commit hook was never previously proposed or rejected; it first appears in H3's fix. Branch protection remains the load-bearing control and stays tracked separately under M21.

---

### M21. H3's authoring-time guard is entirely opt-in and unwired, repeating the recurrence pattern H3 exists to break (FIXED)
> **Fix:** just setup now chains install-hooks; documented the hook, git-config opt-out, and residual bypasses in CLAUDE.md and getting-started, and recorded the required-status-check follow-up with owner and date.
**Severity:** MEDIUM — process/enforcement defect on a four-time recurrence with a demonstrated failure history; no runtime impact, and the load-bearing control is out-of-repo.
**Files:** `justfile:8-9` (`setup`), `:19-21` (`build`), `:23-26` (`install-hooks`); `.githooks/pre-commit`; `docs/getting-started.md:17-26`; `CLAUDE.md`; `.github/workflows/frontend-freshness.yml:3-5`
**Description:** `.githooks/pre-commit` is dormant by default: git consults it only when `core.hooksPath` is set, which happens solely if a human runs `just install-hooks` (`justfile:23-26`). `just setup` is exactly `npm install` (`:8-9`) and does not chain it; `docs/getting-started.md:17-26` documents the fresh-clone flow as `git clone` → `cd` → `just setup`, the precise sequence that leaves the hook uninstalled; and a grep over `docs/`, `README.md`, `CLAUDE.md`, `ARCHITECTURE.md`, and `.github/` for `install-hooks|hooksPath|githooks|pre-commit` returns zero hits outside this review file. `core.hooksPath` is unset even in the clone where the hook was authored. `.github/workflows/frontend-freshness.yml:3-5` triggers on bare `push:`/`pull_request:`, so it does run and does go red on a stale bundle — but it is not a required status check, and history proves red-alone is insufficient: the workflow landed in `482ba08` (2026-06-21) and `public/` still went stale twice afterward (rebuilt `3790df1`, `da27009`, again `c1659c8`). H3 therefore landed as one more manual rebuild plus two controls both requiring unprompted manual opt-in.

*Verified, and relevant to the fix:* the repo has **no** existing hook manager — no `husky`, `lefthook`, `simple-git-hooks`, `lint-staged`, or `prepare` script in `package.json` — so setting `core.hooksPath` clobbers nothing. `just setup` currently has no side effects outside the repo directory, and `.git/config` is per-clone rather than global, so the write is low-surprise and reversible with `git config --unset core.hooksPath`.
**Fix:** Arm the guard by default in-repo, document it, and stop leaving the load-bearing control as an unowned aside.
- Chain `install-hooks` into `setup` (`justfile:8-9`) so `npm install` is followed by `just install-hooks` and a fresh clone is guarded without an extra step.
- Document the hook in `docs/getting-started.md` (alongside the `just setup` step at `:17-26`) and in `CLAUDE.md`, naming what it checks and the `git config --unset core.hooksPath` escape hatch, so the git-config write is disclosed rather than surprising.
- Record "make `frontend-freshness.yml` a required status check on `main`" as an explicit open item with a named owner and date — it is the only mechanism a developer cannot skip, it is not verifiable from this branch's diff, and burying it in a fix note is exactly what H3 already did once.
- Accept the residual gaps: existing clones stay unarmed until someone re-runs `setup`, and `--no-verify` still bypasses the hook. Both are covered by the required status check, which is why it must stay tracked.
- Do **not** delete the hook in favour of CI alone: it is the only control the repo can enforce itself, and M20's fix makes it non-destructive.
- *Prior history:* no constraining precedent — no prior ruling exists on `just setup`, onboarding automation, or git-config manipulation from recipes, and `SMELLS-LEDGER.md` has no entry for this lineage. Advisory nags were considered and rejected: this repo has a demonstrated record of ignoring red signals.

---

### M22. H1's editor-time continuation check fails open rather than closed when privilege cannot be resolved (FIXED)
> **Fix:** Restructured the editor-time continuation check to a match on both resolved_access_profile results, emitting a severity="warning" (not error) issue that pre-announces the runtime pane-adoption refusal when either profile is unrankable; added regressions for the unregistered-agent and unrankable-custom-profile branches.
**Severity:** LOW — *lowered from MEDIUM*: no path reaches an unsafe pane adoption because the runtime site `?`-propagates the same failure; the trigger requires an operator-authored registry with a non-canonical, non-alias profile name, and the sole impact is a late cryptic error instead of an early editor diagnostic. ID retained as `M22` for stability.
**Files:** `src/model.rs:2080-2086` (editor-time check), `:2273-2281` (M11 warning precedent), `:4708-4870`, `:4770` (existing continuation tests); `src/tmux_exec.rs:481-533` (`ensure_reused_pane_compatible`), `:503-507`, `:509-510`, `:414-422`; `src/driver.rs:287-329` (`declared_profile_privilege`), `:355`, `:382-384`, `:155-164`; `src/api.rs:743-758`; `ui/src/app/AppShell.svelte:114-115`; `ui/src/features/editor/flowNodes.ts:40-41`; `ui/src/features/editor/InspectorPanel.svelte:698`
**Description:** The editor-time continuation check is written as `if let (Ok((current_profile, current_privilege)), Ok((source_profile, source_privilege))) = (…) && source_privilege > current_privilege` (`src/model.rs:2080-2086`) with no `else`, so either `Err` silently skips the ceiling comparison and validation reports green. `resolved_access_profile` returns `Err` for an unknown agent (`src/driver.rs:355`, `:382-384`) or a profile with no declared rank — `declared_profile_privilege` (`:287-329`) ranks only `read-only`/`workspace-write`/`full-access`, the `cursor`/`plan` special case, and argv-identical aliases. The author then starts the run and receives a mid-run node failure ("agent X access profile Y has no declared privilege rank") from `src/tmux_exec.rs:509-510`. Existing tests (`src/model.rs:4708-4870`, incl. `validates_continue_session_from_broader_access_profile` at `:4770`) never exercise the `Err` branch.

*Correction to the original write-up:* the runtime twin does **not** fail open, so this is a late-vs-early diagnostic defect, not a safety hole. `ensure_reused_pane_compatible` propagates the same failure with `?` at `src/tmux_exec.rs:509-510`, its metadata arms hard-error at `:503-507`, and `PaneGuard::acquire` invokes it before any adoption (`:414-422`). No path lets a run proceed on an unranked profile.
**Fix:** Replace the silent skip with a warning that pre-announces the runtime refusal, keeping severity policy identical to the neighbouring M11 case.
- Restructure `src/model.rs:2080-2086` as a `match` on the two `resolved_access_profile` results, retaining the existing mismatch error for the decidable `Ok/Ok` case.
- On either `Err`, push a `severity: "warning"` issue naming the agent, the profile that could not be ranked, and stating that pane adoption will be refused at run time — mirroring the M11 pattern at `:2273-2281`. Warnings reach the author via `AppShell.svelte:114-115`, `flowNodes.ts:40-41`, and `InspectorPanel.svelte:698`.
- Keep the message wording aligned with the runtime error text at `src/tmux_exec.rs:509-510` so the advisory and the eventual failure read as the same problem.
- Do **not** emit `severity: "error"`: it would block at `src/api.rs:743-758` for a merely-unrankable-but-compatible pair, contradict the same validator's warning treatment of unregistered agents (`:2273-2281`), and risk blocking offline/first-run environments where `~/.config/tmux-tools/agents.toml` is absent (`src/driver.rs:155-164`).
- Add regressions for both `Err` branches (unregistered agent; registered agent with an unrankable custom profile) asserting exactly one warning and no error.
- *Prior history:* no precedent rules that editor validation may be advisory *because* the runtime enforces, but M11 in this same review settled warning-for-unverifiable in this very function, and H1's own text scopes fail-closed to the pane-adoption site, describing the `model.rs` half as an "editor-time projection … so authors see the error before the run starts" — visibility, not enforcement.

---

### M23. M12's new Terminating wait state becomes a permanent silent hang if the owner task panics (FIXED)
> **Fix:** Wrapped the pane-stream owner task in a supervisor that retains the JoinHandle and force-cleans + cancels owner_done on panic/cancellation, and bounded the subscribe wait with tokio::time::timeout (force_clear on expiry with a warn) instead of an unbounded loop; added owner-panic and owner-never-completes regressions.
**Severity:** LOW — *lowered from MEDIUM*: the mechanism is live (unwind is enabled), but the window between termination and cleanup is currently panic-free code, so this is latent structural fragility rather than a reachable defect. ID retained as `M23` for stability.
**Files:** `src/api.rs:2166-2225` (`spawn_pane_stream_task`), `:2184`, `:2218-2224`, `:2250` (`begin_termination`), `:2009-2013` (unbounded wait), `:2020-2023` (revive path), `:84-121` (`PaneStreamGuard`), `:1601-1630`, `:1633-1641`, `:5145-5192` (existing test); `src/app.rs:221`, `:270`, `:305-318`; `src/runtime.rs:3072-3093` (`spawn_supervised_run`), `:979-988` (`abort_and_wait`), `:5504-5511` (`catch_unwind` precedent), `:8124`, `:8148`; `Cargo.toml` (no `[profile]`)
**Description:** `spawn_pane_stream_task` (`src/api.rs:2166-2225`) discards its `JoinHandle`, so nothing observes the owner task's exit, and the waiting side parks unconditionally: `:2009-2013` reads `entry.is_terminating()`, clones the `CancellationToken` (`src/app.rs:221`, `:270`), drops the lock, and awaits `owner_done.cancelled()` inside a loop with no timeout, no log, and no abort path. If the owner unwinds anywhere after `begin_termination()` (`:2250`), the entry stays `Terminating` for the process lifetime and every later `subscribe_pane_stream` for that `PaneStreamKey` hangs silently — a permanently dead terminal pane with zero diagnostics. `Cargo.toml` declares no `[profile]` section and there is no `.cargo/config.toml`, so `panic = "unwind"` is in effect and a panic kills only that task rather than aborting the process. Coverage is happy-path only (`pane_stream_subscribe_waits_for_terminal_owner_exit`, `:5145-5192`); no owner-panic test exists.

*Corrections to the original write-up:* (1) no plausible panic exists in the window today — between `:2250` and `:2224` the owner runs only `stop_pane_stream` (`:2184` → `:1601-1630`), a `tracing::debug!`, and `remove_terminal_sender` (`src/app.rs:305-318`), none containing `unwrap`/`expect`/indexing/slicing; the sole indexing site (`invocation.prefix[0]`, `src/api.rs:1633-1641`) is `is_empty()`-guarded *and* runs on a separate OS thread, so a panic there surfaces as `Err` at `:1629` and is logged at `:2185`. (2) The "panic before termination" half is milder than stated: the entry stays `Active`/`Draining` and a later subscribe takes `:2020-2023` (`refcount += 1`, `revive()`, reuse sender) with no pump behind it — a dead-quiet stream, not a hang.
**Fix:** Follow the supervisor-plus-bounded-wait shape L14 already established on this branch for the identical bug class.
- Wrap the spawn in an outer supervisor task that retains the inner `JoinHandle` and, on any `JoinError` — panic **or** cancellation — logs with the `PaneStreamKey`, force-cleans the entry, and cancels `owner_done`, mirroring `spawn_supervised_run` (`src/runtime.rs:3072-3093`).
- Bound the wait at `src/api.rs:2012` with `tokio::time::timeout`; on expiry `warn!` and force-clear or replace the entry rather than re-looping, mirroring `abort_and_wait` (`src/runtime.rs:979-988`). This also covers an owner merely wedged in a slow `stop_pane_stream`, which neither panic-handling approach reaches.
- Do **not** use a bare `catch_unwind` wrap: it misses task cancellation and drop, and future code added outside the wrap silently escapes it. Do **not** use a Drop guard alone: cleanup here is async, so `Drop` must `tokio::spawn`, inheriting `PaneStreamGuard`'s deferred-cleanup wart and losing the spawn at runtime shutdown.
- Add regressions for an owner task that panics after `begin_termination()` and for one that never completes: a later `subscribe_pane_stream` must return or error within the bound, with a warning logged, rather than hanging. Model them on `src/runtime.rs:8124`, `:8148`.
- *Prior history:* directly governed by L14 in this same review (FIXED, raised LOW→MEDIUM) — the identical unbounded-await-plus-dropped-handle shape on the run task, which explicitly noted the absence of `panic = "abort"` and chose neither `catch_unwind` nor a Drop guard, but a supervisor task plus bounded wait with warn and force-clear.

---

### M24. M18 still puts privileged no-runAs runs on the default tmux server (FIXED)
> **Fix:** Added a resolve-free run_scoped_tmux_invocation(run_id) helper and made the no-run_as arm (plus the boot reaper fallback) return a per-run silverbond-<run_id> socket instead of the default server, so every run now persists a run-scoped socket; reworked the attach-command test and added start/resume/reaper regressions.
**Severity:** LOW — *lowered from MEDIUM*: no privilege boundary is crossed and the run-scoped ownership check blunts the only cross-run targeting path; the concrete residue is documentation inaccuracy plus tmux-server hygiene. ID retained as `M24` for stability.
**Files:** `src/runtime.rs:3408-3410` (`resolve_workflow_invocation_with`), `:1393-1400` (`start_run`), `:2764-2769` (`execute_workflow`), `:3384-3390` (`load_or_resolve_run_tmux_invocation`), `:6805-6819` (reaper fallback); `src/api.rs:797-804`, `:844`, `:233-250` (`build_attach_command`), `:3450` (affected test); `src/tmux_exec.rs:95-99`, `:2581` (`stable_session_name`), `:2585-2588` (`unique_session_name`), `:624` (`owns_session`), `:925` (`execute_kill`); `docs/architecture-overview.md:50`
**Description:** `resolve_workflow_invocation_with` returns `None` when `workflow.run_as` is absent (`src/runtime.rs:3408-3410`), and `start_run` (`:1400`), `execute_workflow` (`:2764-2769`), and `load_or_resolve_run_tmux_invocation` (`:3390`) all collapse that to `TmuxInvocation::default()`, whose `socket: None` emits no `-L` and therefore uses the invoking user's ordinary tmux server; the boot reaper repeats the fallback literally at `:6805-6819`. The path is reachable in a supported mode: `src/api.rs:797-804` injects `run_as` only when `security.agent_user` is configured, and with none configured `resolved_run_as_is_privileged(None, None)` is `true` (`:844`), so the run proceeds password-unlocked with `run_as == None`. `docs/architecture-overview.md:50` states flatly and without hedging that "Each run has its own tmux server on a `silverbond-<run-id>` socket", which is false for this path; M18's applied-fix note ("runtime and reaper **always** derive the per-run socket") and `20260716-003531` M7 ("Every run gets a per-run socket") overstate the same way.

*Impact, concretely:* agent panes get full-UUID-suffixed session names (`src/tmux_exec.rs:2585-2588`), so no cross-run collision is possible there. The only collision surface is `stable_session_name` (`:2581`, unsuffixed), used solely by `execute_kill` (`:925`) — and `owns_session` (`:624`) bails with "not owned by this run" whenever an `ActivePaneRegistration` exists. The residue is SilverBond sessions landing in the user's live interactive tmux (clutter, accidental manual `kill-server`/detach), pane-id reuse ambiguity, and no alignment of server lifetime with run lifetime. Same UID, same trust domain — not privilege escalation.
**Fix:** Make the per-run socket genuinely universal, and bring the documentation with it.
- In `resolve_workflow_invocation_with`'s `None` arm (`src/runtime.rs:3408-3410`), return `Some(TmuxInvocation { prefix: vec![], socket: Some(format!("silverbond-{run_id}")), tmux_bin: "tmux".into() })`, and mirror the same construction in the reaper's `unwrap_or_else` at `:6805-6819`.
- Keep that construction **resolve-free**: build it directly rather than routing through `build_tmux_invocation`, which would add a `zsh -lic` via `resolve_tmux_bin` to every run start.
- Update `docs/architecture-overview.md:50` — the universal claim becomes true, so the wording stands, but verify it and drop any lingering `runAs`-conditional phrasing nearby.
- Attach UX needs no change: `build_attach_command` (`src/api.rs:233-250`) already emits `-L <socket>` whenever a socket is present, so the UI hint stays correct. Rework the unit test `build_attach_command_omits_socket_flag_for_default_invocation` (`:3450`), which encodes the old behavior.
- Accept the tradeoffs: one extra tmux server process per concurrent run, and users lose plain `tmux attach`/`tmux ls` discovery in favour of the emitted `-L` command. Server lifetime is self-managing — tmux servers exit when their last session dies — and the reaper already groups by `tmux_invocation_key`.
- Add start, resume, and reaper regressions for the no-`runAs` case asserting the run-scoped socket is constructed and persisted.
- *Prior history:* scope gap, not a regression. `20260620-092605:158-163` M12 (FIXED) chose per-run sockets as the isolation boundary and explicitly noted the same property holds for non-`run_as` runs on the default server, then scoped its fix to `run_as` workflows — knowingly leaving this case out. `:134-139` M10 (FIXED) requires kills to stay invocation-scoped and per-run sockets to land with reaper enumeration, which this fix preserves.

---

### M25. L7's password-dialog wiring is duplicated verbatim across both unlock sites (dismissed: overstated by ~2 bands — advisory, not MEDIUM; returned to the smells ledger, revisit at a third call site. The duplication is exactly 10 lines per site (2 imports, 1 instantiation, a 7-line `<PasswordDialog …/>` element; `ui/src/app/AppShell.svelte:504-510` and `ui/src/features/editor/InspectorPanel.svelte:1897-1903` are byte-identical), and it is pure declarative mounting — all prompt state, the deferred resolver, re-entrancy cancel, and defaults already live in the shared `ui/src/lib/components/passwordPrompt.svelte.ts:8-48`, with all markup in `PasswordDialog.svelte`. L7 already performed the extraction this finding asks for and its write-up (`:622`) planned exactly this shape. The two sites are also not the same use case — `AppShell.svelte:208-215` gates privileged run start (cancel → `"Run cancelled"` → retry `api.createRun`) while `InspectorPanel.svelte:196-203` gates single-node preview (cancel → `"Preview cancelled"` → retry `api.testNode`) — so a shared app-level instance would couple independent concerns. A context hoist would additionally break the standalone-mount test at `InspectorPanel.test.ts:101`/`:117-155`. Rule of three is not met at 2 sites, and `SMELLS-LEDGER.md` precedent files Duplicated Code as advisory unless the extraction is substantive (`PaneGuard` RAII, shared `constant_time_eq`, `runLifecycle` collapsing a triplication). Follow-up: add a `ui/src/app/AppShell.svelte:504` Duplicated Code row to `SMELLS-LEDGER.md` — no such row exists today.)

---

### M26. L5's `secure_database_files` is invoked twice within a single `connect()` (dismissed: not a defect — the two calls are distinct lifecycle jobs by one idempotent, missing-tolerant helper, and both proposed fixes are already-shipped or a regression. `secure_database_files` already opens the main file with `OpenOptions::new().create(true).mode(0o600)` (`src/storage.rs:1086-1091`) and silently `continue`s on an absent sidecar (`:1100-1102`), so no "sidecars may not exist yet" flag is needed and proposed fix (b) is verbatim what `:1090` does. The `:63` call is load-bearing: SQLite's `findCreateFileMode` copies the main database's mode onto `-wal`/`-shm`, and for a database left at 0644 by a pre-L5 build `OpenOptions::mode()` is a no-op, so only the explicit `set_mode(0o600)` at `:1093` running *before* open prevents SQLite minting 0644 sidecars — deleting it per proposed fix (c) would be a security regression on upgrade. Verified empirically that no residual race window exists: with the main db pre-created 0600, both sidecars are born 0600. The `:66` call is the direct discharge of L5's instruction to verify the mode applies to the WAL and shared-memory sidecars, and `20260716-003531:73`/`:628` ruled an explicit post-creation `set_permissions` mandatory in both branches. Cost is ~9 syscalls per call and `connect()` runs a handful of times per process (`max_size(8)`, `:155-159`), not per query. Both sites already carry comments stating their distinct rationale (`:61-62`, `:65`). Follow-up: no `SMELLS-LEDGER.md` row exists for `secure_database_files`; adding one recording this adjudication would stop it being re-raised.)

---

### M27. H2's three `match killed` arms repeat a near-identical warn loop (dismissed: premise factually wrong on the first arm, and the real duplication is 4 lines — advisory, not MEDIUM. `src/runtime.rs:6894-6909` (`Ok(true)`) is not a warn loop at all: it is a DB mutation (`ctx.db.remove_tmux_sessions(…).await`, `:6896-6899`) whose warn fires only inside `if let Err(error)`, differing in control flow, asyncness, and field source. Only `Ok(false)` (`:6910-6918`) and `Err` (`:6919-6928`) are bare warn loops, and they differ in structured fields (2 vs 3, the latter adding `error = %error`), not merely prose. The longest verbatim repeated block is 4 lines of scaffold; net removable duplication is 4-6 lines, not the ~37 the cited `:6893-6929` range implies. The three messages are deliberately distinct terminal verdicts ("killed but registration remains" / "failed to kill" / "kill task failed") — exactly what H2's finalized fix mandated at `:70` ("retain the registration and `warn!` with `run_id` and `session_name`", per-target contextual warnings). The proposed helper cannot cover arm 1 without becoming async and taking `&ctx`, and for arms 2/3 it must `match outcome` internally, relocating the match rather than removing it; a dynamic message string is used by zero of the 27 `tracing::warn!` call sites in `src/runtime.rs`, so it would break the repo's structured-logging and grep-by-literal convention for no net line saving. No overlap with M28: that finding iterates `cleanup_verdicts` filtered on `!verdict.absent` logging `pane_id`/`session_name` off `verdict.target` for one `run_id`, a different collection, field set, and function. Follow-up: no `SMELLS-LEDGER.md` row covers `src/runtime.rs:6600-6960`; add a `src/runtime.rs:6893` Duplicated Code row and revisit at a fourth arm.)

---

### M28. Cleanup-verdict warning logic is duplicated across two pane-cleanup paths — re-scoped to the `spawn_blocking` wrapper clone (FIXED)
> **Fix:** Extracted the duplicated spawn_blocking(cleanup_panes) block into a shared run_pane_cleanup async helper called from both pane-cleanup sites; left the two H2-mandated contextual warn! blocks verbatim as required.
**Severity:** LOW — *lowered from MEDIUM*: the filed claim has zero duplicated lines; the re-scoped item is a genuine 9-line byte-identical clone, but it is a behavior-free maintainability cleanup with no correctness impact. ID retained as `M28` for stability.
**Files:** `src/runtime.rs:6676-6684` and `:6940-6948` (the real clone); `:6697-6711` (`cleanup_terminal_active_panes` warns), `:6713-6730` (verdict-driven deletions), `:6951-6958` (`kill_active_run_panes` warn), `:6679/6681`, `:6943/6945` (the only two `cleanup_panes` callers), `:9426` (existing test); `src/tmux_exec.rs:68` (`PaneCleanupVerdict`)
**Description:** As filed, the finding does not reproduce: a literal `diff` of `src/runtime.rs:6697-6711` against `:6951-6958` returns `1,15c1,8` — **zero byte-identical lines**, even after normalizing indentation. The loop headers differ (`.iter()` vs `.into_iter()`), the field sets differ, the message texts differ, and the control flow differs (an if/else on `verdict.target.session_name` vs a single unconditional warn). The two warns encode different post-conditions by design: `cleanup_terminal_active_panes` distinguishes session from pane targets because it then *acts* on the verdicts, deleting registrations for absent sessions at `:6713-6730` ("retaining registration"), while `kill_active_run_panes` deliberately performs no DB work and defers to the reaper ("registration will be reconciled by the reaper"). H2 mandated exactly this per-path contextual split (`:62`, `:70`). No third site exists — `cleanup_panes` has exactly two callers crate-wide and `!verdict.absent` appears only at these two places.

*The real duplication, which the finding does not name:* `src/runtime.rs:6676-6684` and `:6940-6948` — the `let inv = ctx.run_invocation.clone();` plus `spawn_blocking(with_invocation … cleanup_panes)` block — are **9 lines byte-identical**.
**Fix:** Extract the duplicated execution wrapper and leave both warning blocks untouched.
- Add `async fn run_pane_cleanup(ctx, targets) -> Result<Vec<PaneCleanupVerdict>, JoinError>` wrapping the shared block at `src/runtime.rs:6676-6684` / `:6940-6948`, and call it from both sites.
- Leave the `warn!` blocks at `:6697-6711` and `:6951-6958` verbatim: their message texts and field sets are the H2-mandated per-path contextual verdicts and must stay diagnostically distinguishable in production logs.
- Do **not** introduce `warn_unconfirmed_cleanup_verdicts` as originally proposed: site A needs the session/pane branch and borrows via `.iter()` (it reuses `cleanup_verdicts` at `:6713`) while site B consumes, so the helper would require either a non-literal message — breaking the `field = %x, "literal"` convention used by all 27 `tracing::warn!` sites in this file — or a `match mode` longer than both call sites combined.
- Do **not** hang the warning off `PaneCleanupVerdict` (`src/tmux_exec.rs:68`): the divergent part is the caller's remediation policy, which the verdict type has no business knowing.
- Verify no behavior change: `src/runtime.rs:9426` exercises `cleanup_terminal_active_panes` but asserts only DB retention, and `kill_active_run_panes` has no test at all, so the extraction must be a pure move.
- *Prior history:* `SMELLS-LEDGER.md` has no row for `src/runtime.rs:66xx`/`:69xx`. The cited precedents at `20260620-092605:324` (L6) and `:342` (L8) did instruct factoring a shared warn-emit helper, but in both cases the two sites emitted the *same event with the same message and fields*; that precondition does not hold here, so those rulings do not extend to this finding.

---

### M29. L6's sequence dedupe-and-wrap logic is written three times (FIXED)
> **Fix:** Introduced a SeqFilter { max_seq } type with accept()/observe(), merged the twinned RunStreamJournalItem/RunStreamBufferedItem::Event into one RunStreamItem, and routed all four sequence touchpoints through it (removing the threaded &mut u64 params); added five direct SeqFilter unit tests covering the previously-untested resync and live-arm dedupe paths.
**Severity:** LOW — *lowered from MEDIUM*: genuine rule-of-three duplication with real type-twinning, but only 6 shared lines, zero drift between the copies today, and no correctness exposure; the live hazard is the untested paths, not the duplication itself. ID retained as `M29` for stability.
**Files:** `src/api.rs:937-946` (live `recv()` arm), `:1013-1022` (`drain_buffered_run_events`), `:1042-1051` (`resync_run_stream_from_journal`), `:891-895` (fourth seq touchpoint, replay-loop max-tracking), `:994-997` (`RunStreamJournalItem`), `:1000` (`RunStreamBufferedItem::Event`), `:1006`, `:1037` (`&mut max_seq` params), `:974` (`unwrap_or(0)`), `:3399`, `:3433` (existing tests); `src/runtime.rs:6969-6970` (`emit_event`, the only non-test sender)
**Description:** The dedupe-and-wrap sequence appears three times: `src/api.rs:937-946` inside the `stream!` live `recv()` arm, `:1013-1022` in `drain_buffered_run_events`, and `:1042-1051` in `resync_run_stream_from_journal`. Sites 2 and 3 are 9-of-10 lines identical, differing only in which of two field-identical wrapper types they construct — `RunStreamBufferedItem::Event` (`:1000`) versus `RunStreamJournalItem` (`:994-997`), both `{ sse: Event, is_done: bool }` — and that twinning is precisely why the wrap step cannot already be shared. Site 1 shares the same 6-line core but yields directly instead of pushing. Test coverage is asymmetric and is the real hazard: `run_stream_dedupes_buffered_live_event_after_replay` (`:3399`) and `run_stream_buffered_drain_signals_resync_on_lagged` (`:3433`) exercise only `drain_buffered_run_events`, while the resync path and the live arm carry **zero** dedupe assertions, so drift in two of the three copies would go undetected.

*Verified — no drift exists today:* all three use `<=` (`:938`, `:1014`, `:1043`), all three advance `max_seq` (`:941`, `:1017`, `:1046`), and all three share the same `event.kind == "done"` terminal test and `run_stream_sse_event(&event)` wrap. No off-by-one, no `<` vs `<=`, no missing advance. All broadcast events are stamped by `emit_event` (`src/runtime.rs:6969-6970`, the only non-test sender), so `seq` is never `None` in production and the `unwrap_or(0)` at `:974` is currently unreachable.
**Fix:** Give the dedupe owned state in a small type, and unify all four sequence touchpoints behind it.
- Introduce `SeqFilter { max_seq: u64 }` with `fn accept(&mut self, event) -> Option<RunStreamItem>` (discard when `seq <= max_seq`, else advance and wrap) and `fn observe(&mut self, event)` for max-tracking without emission.
- Merge `RunStreamJournalItem` (`:994-997`) and `RunStreamBufferedItem::Event` (`:1000`) into a single `RunStreamItem { sse, is_done }`, leaving `RunStreamBufferedItem` as `Item(RunStreamItem)` + `Resync`. This is what unblocks sharing the wrap step.
- Replace all three dedupe sites plus the replay-loop max-tracking at `:891-895` with `SeqFilter` calls, removing the `&mut u64` parameters threaded through `:1006` and `:1037`.
- Add unit tests directly against `SeqFilter` (duplicate seq discarded, equal seq discarded, advancing seq accepted, `is_done` propagation) — this is the point of the change, since it closes the coverage gap on the resync and live-arm paths that no existing test reaches.
- Do **not** settle for a free `dedupe_and_wrap(event, &mut max_seq)` helper: it leaves `max_seq` as a threaded `&mut u64` and misses the fourth touchpoint at `:891-895`.
- *Prior history:* L6's finalized fix specified a single dedupe location ("drain the already-buffered receiver discarding any event whose seq is ≤ the maximum replayed seq") and separately required fixing the `Lagged` arm; it did not anticipate that the resync path would itself need dedupe, so the third copy is an emergent consequence of the two-pronged fix rather than a review oversight. L6 also mandated regressions for both the replay/subscribe window and the lagged receiver — only the former landed with a dedupe assertion. `SMELLS-LEDGER.md` has no row for `src/api.rs:9xx`/`10xx`.

---

### M30. The agent-resolution chain `agent_name_for_node` owns is re-implemented inline twice (FIXED)
> **Fix:** Added explicit_agent_for_node(node) -> Option<&str> (nested-config-first) and reimplemented agent_name_for_node on it with an unchanged String contract; rewrote the Spawn and RunAgent validation arms to derive both the agent name and has_agent from it, removing the duplicated inline chains; added the two missing validation regressions.
**Severity:** LOW — *lowered from MEDIUM*: 10 duplicated lines across two sites (three copies crate-wide), all behaviorally equivalent, with no user-visible impact; pure maintainability. ID retained as `M30` for stability.
**Files:** `src/model.rs:61-91` (`agent_name_for_node`), `:65-74` (chain), `:86-89` (Task arm), `:1701-1710` (Spawn arm), `:1774-1783` (RunAgent arm), `:2049-2050`, `:2084-2086`, `:2098-2099` (H1's landed consolidation), `:4738`, `:4765` (only agent-resolution tests); `src/runtime.rs:1766-1769` (third copy), `:3871`, `:4500`, `:5556`, `:5616` (other callers); `src/api.rs:660`
**Description:** `src/model.rs:1701-1705` and `:1774-1778` each re-derive the `config.agent → node.agent → DEFAULT_AGENT` chain that `agent_name_for_node` (`:61-91`) exists to own, and are immediately followed by 5 more duplicated lines computing `has_agent` (`:1706-1710`, `:1779-1783`); the two 10-line blocks are byte-identical except the config binding (`spawn_config` vs `run_agent_config`). A third, narrower copy sits at `src/runtime.rs:1766-1769` in `run_node_preview`, which the finding does not name. Neither validation message ("spawn node requires an agent or command", "run_agent node requires an agent") has any test — the only agent-resolution coverage is the continuation check around `:4738`/`:4765`.

*Corrections to the original write-up:* (1) the implied premise that a `Cow<str>` return is needed to unblock the callers is wrong — `agent_name_for_node` returns **`String`**, not `&str`, so both sites could call it today (`validate_agent_launch_config` takes `&str`, and `&String` derefs). (2) There are three inline re-implementations crate-wide, not two. (3) **No copy resolves differently**: both `model.rs` copies use `Option`-level `.or()` exactly matching `:65-74` (`Some("")` yields `""` in both), and the `runtime.rs:1766` copy omits the nested-config level but is reachable only for `NodeKind::Task` (`src/api.rs:660` rejects all other kinds), whose arm at `:86-89` is precisely `node.agent → DEFAULT_AGENT`. Equivalent on every reachable input.
**Fix:** Extract the *explicit* chain so both the agent name and the `has_agent` predicate derive from one source.
- Add `fn explicit_agent_for_node(node: &WorkflowNode) -> Option<&str>` returning `config.agent → node.agent`, and reimplement `agent_name_for_node` as `explicit_agent_for_node(node).unwrap_or(DEFAULT_AGENT).to_string()` so the public contract is unchanged.
- Rewrite both dispatch arms as `let explicit = explicit_agent_for_node(node); let agent = explicit.unwrap_or(DEFAULT_AGENT); let has_agent = explicit.is_some_and(|a| !a.trim().is_empty());`, removing all 20 duplicated lines rather than only the 10 the original fix targets.
- Do **not** change the return type to `Cow<str>`: it solves a lifetime problem that does not exist, forces edits at six unrelated call sites (`src/runtime.rs:3871`, `:4500`, `:5556`, `:5616`, `src/model.rs:2049-2050`) — four needing `.into_owned()` for `NodeResult.agent: String` — and still leaves `has_agent` duplicated.
- Preserve the nested-first precedence established at `docs/issues/code-review-20260608_part_4_of_4.md:72-73` (`run_agent_config.agent` before `node.agent`); the inline copies currently honor it, so the extraction must not reorder.
- Add the two missing validation regressions ("spawn node requires an agent or command", "run_agent node requires an agent") while the code is open — they are the only genuinely defect-catching part of this change.
- *Relationship to H1:* independent, not unfinished H1 work. H1's mandate targeted continuation validation and landed — `src/model.rs:2049-2050` now calls `agent_name_for_node` on both sides, extended to access profile (`:2084-2086`) and `cwd` (`:2098-2099`). M30's sites are the `validate_node_kind` dispatch arms, which H1 never mentioned. No `SMELLS-LEDGER.md` row covers `src/model.rs:17xx` or agent resolution.

---

### M31. The privilege-rank `ok_or_else` block is repeated four times (FIXED)
> **Fix:** Added a require_privilege(spec, profile) helper wrapping declared_profile_privilege(...).ok_or_else(...) with unified wording, collapsed all four driver.rs call sites to it, updated the fallback assertion, and added three previously-missing error-path tests.
**Severity:** LOW — *lowered from MEDIUM*: four sites clears the rule of three, but it is 20 lines with zero behavioral drift and ~8 net lines saved; the access-control location is what keeps it above cosmetic. ID retained as `M31` for stability.
**Files:** `src/driver.rs:340-344`, `:356-360`, `:390-394`, `:414-418` (the four sites), `:337`, `:353`, `:382` (`spec` bindings), `:291-305`, `:301`, `:311-314` (privilege inference and its TODO), `:1941-1971`, `:1968` (only error-path test); `docs/issues/SMELLS-LEDGER.md:45`, `:58`, `:62-63`
**Description:** The `Option → Result` lift around `declared_profile_privilege` is written four times — `src/driver.rs:340-344`, `:356-360`, `:390-394`, `:414-418` — at 5 lines apiece. All four have `spec: &agents::AgentSpec` (`:337`, `:353`, `:382`) and `agent: &str` in scope, and `spec.name` equals the registry lookup key (`Registry::get` is a plain `BTreeMap` lookup with no aliases), so the error message is fully reconstructible from `(spec, profile)`. Test coverage is lopsided: only `:414` has an error-path test (`registry_edit_rejects_unranked_default_fallback`, `:1941-1971`, asserting the exact string at `:1968`), while `:340`, `:356`, and `:390` have no assertion on this error at all. The residual risk is the "fix three, miss one" class landing in privilege-relevant code.

*Corrections to the original write-up:* the four blocks are **not** byte-identical — only `:340-344` and `:356-360` are; `:390-394` differs in binding name and indentation (same rendered text), and `:414-418` uses distinct wording, `"agent {agent} default access profile has no declared privilege rank"` with no `{profile}` interpolation, because the profile name there is the literal `"default"`. **No security-relevant drift exists:** all four `ok_or_else` into a hard error and no site substitutes a silent `ReadOnly` or other default. `declared_profile_privilege` has exactly four call sites crate-wide — none missed.
**Fix:** Extract the lift into one helper so the four sites become provably identical.
- Add `fn require_privilege(spec: &agents::AgentSpec, profile: &str) -> anyhow::Result<AccessPrivilege>` with body `declared_profile_privilege(spec, profile).ok_or_else(|| anyhow::anyhow!("agent {} access profile {profile} has no declared privilege rank", spec.name))`.
- Collapse all four call sites to `require_privilege(spec, profile_name)?`; `:414` becomes `require_privilege(spec, "default")?`.
- Update the assertion at `src/driver.rs:1968` for the unified wording, which renders "access profile default has no declared privilege rank" for the fallback site.
- Add error-path regressions for `:340`, `:356`, and `:390`, which currently have none — the fix-three-miss-one risk this finding is really about is invisible without them.
- *Sequencing:* land this **before** L19 if L19 is scheduled soon — L19 rewrites `declared_profile_privilege`'s body, and the wrapper turns that into a single-function change instead of four.
- *Deferred alternative:* the ledger's Primitive Obsession fix (`SMELLS-LEDGER.md:58`, `:62-63` — give the profile a type carrying its rank) would subsume both M31 and L19 and delete this helper, but it needs `tmux-tools-core` to attach privilege metadata to `AccessProfile`, and H8 established there is no upstream publish access. The comment at `src/driver.rs:311-314` confirms that metadata does not exist yet.
- *Prior history:* `SMELLS-LEDGER.md` carries three open rows on exactly this code (`src/driver.rs:291-305`, `:291` Primitive Obsession, `:301` Repeated Switches); this extraction is a step toward them, being the seam the profile type would eventually absorb. `:45` records the precedent of retiring a `src/driver.rs:648-674` Duplicated Code row as "not independently actionable" when a larger fix would collapse it.

---

### M32. `kill_tmux_pane`'s absence probe uses a CANFAIL tmux command and never confirms absence (FIXED)
> **Fix:** Replaced kill_tmux_pane's CANFAIL display-message absence probe with list-panes (whose nonzero exit genuinely means absent), fixed the mis-modelled tmux test fake (list-panes exits 1, not display-message), and added a negative regression pinning that the old display-message form cannot confirm absence.
**Severity:** MEDIUM — reframed from a duplication smell to a correctness defect: H2's verification mechanism is inert on the pane path and emits false "could not confirm" warnings on every cleanup, but no DB row is wrongly deleted and no pane is leaked. ID retained as `M32` for stability; title rewritten because the filed duplication is incidental.
**Files:** `src/tmux_exec.rs:1305-1310` (`kill_tmux_pane`, broken probe), `:1298-1303` (`kill_tmux_session`, correct), `:4297-4345`, `:4331` (mis-modelled tmux fake), `:4266` (session-path fake); `src/runtime.rs:6697-6710`, `:6706`, `:6712-6716`, `:6949-6956`, `:6950`
**Description:** `kill_tmux_pane` probes absence with `display-message -p -t <pane_id> '#{pane_id}'`, but that command declares its target `CMD_FIND_CANFAIL` — an unresolvable pane target is **not** an error and exits **0**. Verified empirically against tmux 3.7b: `tmux -L probe3 display-message -p -t '%9999' '#{pane_id}'` exits 0 with empty stdout on a live server, while `tmux -L probe3 list-panes -t '%9999'` exits 1 with `can't find pane: %9999`. The `absent = exit_code != 0` test at `src/tmux_exec.rs:1305-1310` is therefore inverted in the dominant case: it returns `true` essentially only when the whole tmux server is down, reporting "pane still present" for every pane it successfully killed. `kill_tmux_session` (`:1298-1303`) uses `has-session`, which genuinely exits nonzero when absent, and is correct. The regression fake at `:4297-4345` hardcodes `case "$*" in *has-session*|*display-message*) exit 1;;` — it encodes the wrong assumption, so `cleanup_panes_kills_registered_sessions_and_pane_fallbacks` passes green while real tmux behaves inversely. H2's mandated nonzero-exit fakes landed for the session path (`:4266`); the pane path is covered only by that mis-modelled fake.

*Blast radius, traced:* pane-only verdicts never delete a DB row — `killed_sessions` (`src/runtime.rs:6712-6716`) filters on `verdict.target.session_name`, which is `None` for the pane branch. The impact is a permanently-false confirmation signal producing spurious `"Terminal tmux cleanup could not confirm pane absence"` (`:6706`) and `"Abort cleanup could not confirm tmux target absence"` (`:6950`) warnings on every abort and terminal cleanup, plus a dead H2 verification mechanism on the external/reused-pane path. `kill-pane` is still issued, so panes are not leaked. The server-down case is handled identically and correctly by both functions (`no server running` exits 1). No third kill+probe site exists — `src/tmux_exec.rs:3926` is an inverse liveness probe in tests, not a kill site.
**Fix:** Replace the CANFAIL probe with one whose nonzero exit actually means "absent", and correct the fake that masked it.
- In `kill_tmux_pane` (`src/tmux_exec.rs:1305-1310`), probe with `tmux::run(&["list-panes", "-t", pane_id])` instead of `display-message -p -t … '#{pane_id}'`.
- Fix the tmux fake at `:4331` to model real semantics (`*list-panes*) exit 1`) rather than asserting `display-message` exits 1 — the current fake is why the suite reports green on inverted behavior.
- Add a negative regression asserting the old `display-message` form would *fail* the absence check, so this specific masking cannot recur.
- Confirm `list-panes` exit semantics on the tmux versions the project supports; behavior is verified on 3.7b only.
- Do **not** factor `kill_and_confirm_absent(kill_args, probe_args)` as originally proposed *before* fixing the probe: the helper's entire premise is that a probe's nonzero exit means absence, which is precisely what is false here, so the extraction would consolidate a broken probe with a working one. If it is factored afterwards, its contract must document that the probe must exit nonzero when the target is absent.
- *Prior history:* `SMELLS-LEDGER.md:21` (`src/tmux_exec.rs:812` → PaneGuard RAII) and `:22` (`:1347` → shared `dispatch_pattern_match`) show this file has a precedent of substantive extractions, but neither applies: those consolidated real behavior.

---

### L16. M17's stale-temp cleanup races a concurrent save and fails the write it was meant to protect (FIXED)
> **Fix:** Made workflow save temp files per-writer (<safe>.json.<pid>-<nonce>.tmp) so concurrent same-name saves no longer share a temp, and taught list to reap only foreign-pid/legacy temps via workflow_temp_disposition(); added own-pid-preserved and concurrent-save valid-JSON regressions.
**Severity:** LOW — the cited race is a data-safe, retryable 500 in a sub-10ms window; the adjacent same-name concurrent-save path found during verification can publish corrupt JSON (MEDIUM in isolation) and is covered by the same fix.
**Files:** `src/storage.rs:903-911` (`list` temp unlink), `:977-983` (`save` temp write + rename), `:1424-1455` (test that locks in the unguarded unlink); `src/api.rs:136` (shared route), `:505-507`, `:527-535`
**Description:** `WorkflowStore::list` unlinks every `*.json.tmp` it walks past (`src/storage.rs:903-911`) with no age or ownership test, while `save` writes `<safe>.json.tmp` and renames it **by path** (`:977-983`) — so the open `File` handle does not save the rename. `GET /api/workflows` and `POST /api/workflows` are independent handlers on the multi-threaded runtime with no lock over the store (`src/api.rs:136`; `WorkflowStore` is just `{ dir: Arc<PathBuf> }`), and TanStack Query keeps `refetchOnWindowFocus: true`, so a window focus or second tab during a save fires the racing GET; `rename` then fails ENOENT and the user's save 500s with the prior `.json` intact. Because the temp path is per-workflow rather than per-attempt (`:977`), two overlapping saves of the same workflow additionally truncate and interleave `write_all` into one file and can publish corrupt JSON. The cleanup was introduced by M17 in this same review, which explicitly accepted the read-path-mutates layering smell but never considered concurrency.
**Fix:** Make the temp file private to its writer so no live temp is ever a deletion candidate.
- In `save`, write to `<safe>.json.<pid>-<nonce>.tmp` (per-attempt unique) and rename that path to `<safe>.json`.
- In `list`, parse the pid segment of each `*.tmp` filename and unlink only entries whose pid differs from `std::process::id()`; leave this process's in-flight temps alone. Unparseable/legacy `<safe>.json.tmp` names are foreign by definition and remain reapable.
- Update `lists_workflows_and_skips_invalid_files` (`src/storage.rs:1424-1455`), which currently asserts the unguarded unlink of `stale.json.tmp`, to cover both cases: a foreign-pid temp is removed, an own-pid temp is preserved.
- Add a regression that two concurrent saves of the same workflow name both produce well-formed JSON (no interleaved write into a shared temp).

---

### L17. M15 checkpoint dedupe cache never retires completed runs, leaking memory unbounded (FIXED)
> **Fix:** Relocated checkpoint_persist_hashes into RunRegistry with last_persist_hash/record_persist_hash accessors and drop-on-clear so entries retire on every exit path; added a lifecycle retirement test.
**Severity:** LOW — pure unbounded growth at ~80–100 bytes per completed run (~1 MB per 10k runs); no correctness impact and no user-visible symptom short of six-figure run counts in one uptime.
**Files:** `src/runtime.rs:1358` (declaration), `:6990-6992` (read), `:6997-7000`, `:7011-7014` (writes), `:917` (`RunRegistry` impl), `:1102-1106` (`RunRegistry::clear`), `:6648` (`finalize_run` clear call), `:3084-3091` (panic/abnormal-exit path), `:8195` (existing dedupe test); `src/app.rs:345` (single `RuntimeContext::new`)
**Description:** `checkpoint_persist_hashes` is an `Arc<Mutex<HashMap<String, u32>>>` (`src/runtime.rs:1358`) with two writers and one reader and — verified by grep across `src/` — zero `.remove(`/`.retain(`/`.clear()` call sites. `finalize_run` ends by calling `ctx.registry.clear(&run_id)` (`:6648`) but leaves the hash entry behind, and `dismiss_run` (`:1618`) never touches it; since `RuntimeContext::new` runs once (`src/app.rs:345`) and every per-run clone shares the same `Arc`, the map is process-lifetime. Each completed run permanently retains a 40-byte UUID key plus `String` header, `u32` value, and hashbrown slot overhead. Two claims in the original write-up do not survive verification: `RunRegistry::clear` is a method on a *sibling* field of `RuntimeContext` with no access to the map, so the proposed fix location is not directly implementable; and the 32-bit djb2 collision risk is ~2⁻³² per transition rather than a birthday bound, because only one hash is retained per run. The restart/run-id-reuse correctness angle is refuted — `restart_from` mints a new run id (`:1560`), and `resume_run` reloads from the DB so a stale match is correct.
**Fix:** Move the map to the component that already owns per-run in-memory lifecycle, so retirement happens in one place for every exit path.
- Relocate `checkpoint_persist_hashes` from `RuntimeContext` (`:1358`) into `RunRegistry` (impl at `:917`), exposing `last_persist_hash(run_id)` and `record_persist_hash(run_id, hash)` accessors; update the read at `:6990-6992` and the writes at `:6997-7000`, `:7011-7014` to go through them.
- Drop the entry inside `RunRegistry::clear` (`:1102-1106`), alongside the existing `active.drained_token.cancel()`. This covers `finalize_run` (`:6648`), `dismiss_run`, and the abnormal-exit path at `spawn_supervised_run:3084-3091` without further call-site changes.
- Accept that `clear` also runs on failed registration (`:1453`, `:1578`), dropping a still-valid entry: harmless, costing at most one redundant checkpoint write.
- Leave the 32-bit djb2 hash as-is; widening it addresses a ~2×10⁻⁷-per-run risk with a benign worst case and is out of scope for this finding.
- Add the missing lifecycle test asserting the entry is gone after a run completes; `persist_checkpoint_skips_redundant_writes_when_unchanged` (`:8195`) covers dedupe behavior only.

---

### L18. H6's local process-group fix discards tmux's stderr on non-zero exit, narrowing `pipe-pane` failure diagnostics (FIXED)
> **Fix:** Extended H8's src/proc.rs seam with file-backed stderr capture (WaitResult enum; timeout returns captured partial output) and wired the args list plus trimmed stderr into both the non-zero-exit and timeout bails of run_tmux_status_with_timeout and ensure_run_as_can_traverse_root; added a stderr-surfacing regression.
**Severity:** LOW — a diagnostics-only gap scoped to tmux `pipe-pane` non-zero exits; no correctness or safety impact, and the operator still gets an exit code and the operation name.
**Files:** `src/api.rs:1649-1698` (`run_tmux_status_with_timeout`), `:1657-1660` (null stdio + `process_group(0)`), `:1674-1677` (non-zero-exit bail), `:1687` (timeout bail), `:1723-1754` (`ensure_run_as_can_traverse_root`, `:1746-1752` pre-existing stderr discard); superseded call site `c1659c8^:src/api.rs:1484-1489`
**Description:** `run_tmux_status_with_timeout` sets `Stdio::null()` on all three streams (`src/api.rs:1657-1660`), so its non-zero-exit bail reports only `"tmux command failed with exit code {} while {operation}"` (`:1674-1677`) — the superseded call site surfaced `String::from_utf8_lossy(&output.stderr).trim()` plus the `args` list (`c1659c8^:src/api.rs:1484-1489`), and both are now gone. An operator hitting a `pipe-pane` failure (bad target, unwritable FIFO, socket permission) sees "exit code 1 while starting pane stream" at the websocket client instead of tmux's own explanatory line, and nothing in the code records the suppression as deliberate.

*Corrections to the original write-up:* the **timeout** path is not a regression — the upstream helper discarded both readers with `let _ = join_reader(...)` and returned a bare `Timeout`, so no stderr was ever available there; the new timeout bail (`:1687`) also carries no exit code, so "both bail paths can now only report an exit code" is inaccurate. The cited `run_as`-wrapper and sudo-policy scenarios do **not** flow through this function: they run through `ensure_run_as_can_traverse_root` (`:1723-1754`), which still calls `command_output_with_timeout` and already ignored `output.stderr` at `:1746-1752` — a pre-existing gap untouched by H6 and owned by H8's seam. This runner is `std::process` with a `try_wait` + `thread::sleep` loop, not tokio.
**Fix:** Recover diagnostics through H8's shared process seam rather than reintroducing pipes into this runner.
- Sequence this behind **H8**, whose finalized fix already extracts this runner into `src/proc.rs` with a status variant and an output-capturing variant implemented without pipes (stdout redirected to a temp file via `Stdio::from(File)`).
- Extend that capture to **stderr** as well, and have the status variant read the file's tail into both the non-zero-exit bail (`:1674-1677`) and the timeout bail (`:1687`), restoring the `args` list alongside the stderr text.
- File-backed capture is required rather than optional: a piped stderr in this synchronous `try_wait` loop needs hand-rolled `O_NONBLOCK` fd reads and a drop-not-join discipline on deadline, which is the exact shape H6 was filed to eliminate. File writes never block and carry no EOF dependency.
- Apply the same seam to `ensure_run_as_can_traverse_root` (`:1746-1752`), closing the pre-existing `run_as`/sudo diagnostic gap in the same change.
- Add a regression asserting the non-zero-exit bail contains the command's stderr text; existing coverage (`src/api.rs:4283`, `:4361`, `:4460-4478`) exercises only the timeout path.

---

### L19. M16 infers privilege from canonical profile names (escalated: ISSUE-260722-0818-1)
**Severity:** LOW — reachable only by the operator mislabeling profiles in their own `agents.toml`, crossing no trust boundary; workflow authors cannot add or edit profiles, and the name-only path at `src/driver.rs:405-407` already grants the same escalation with less effort.
**Files:** `src/driver.rs:291-296` (`declared_profile_privilege` name table), `:311-314` (comment), `:315-329` (shape-matching fallback), `:340`, `:356`, `:390-400`, `:405-407` (unguarded name-mapped return), `:414`, `:419` (gate), `:1971-1999` (existing fallback test); upstream `core/src/agents/mod.rs:29-31` (`AccessProfile`), `:264-268` (`AccessProfileConfig`), `:175-186`, `:335-350` (operator registry merge); `Cargo.toml:28` (rev pin)
**Description:** `declared_profile_privilege` (`src/driver.rs:291-296`) ranks `read-only`/`workspace-write`/`full-access` purely by string, and for non-canonical names the fallback at `:315-329` accepts a profile only if its argv is byte-identical to a canonically-named sibling. An operator registry containing `[custom.access.read-only] args=["--broad"]` plus an identical `default` therefore makes `declared_profile_privilege(spec, "default")` return `ReadOnly`, pass `default_privilege <= config.access_mode.privilege()` at `:419`, and launch broad argv under `Edit` — the widening M16 was filed to stop. The larger hole is on the primary path: `:405-407` returns the name-mapped profile with **no rank check at all**, so broad argv placed under `[custom.access.workspace-write]` launches under `Edit` without ever reaching `declared_profile_privilege`. Upstream `AccessProfile` carries `{ args: Vec<String> }` only (`core/src/agents/mod.rs:29-31`), so SilverBond currently has no channel through which a profile could declare its own privilege.

*Corrections to the original write-up:* M16 did prohibit name-keying (`:452`), but its stated reason was that such a map *"cannot rank `default` at all, which is the entire question"* — **not** L19's claim that names cannot prove what free-form argv permits. The shipped fix does not rank `default` by name; it shape-matches it, and M16's own fix header (`:431`) records this as the deliberate accepted compromise *"using exact local profile-shape validation because an upstream rev bump was not authorized."* The `access_profile_override` arm workflow authors can reach **is** rank-guarded (`:390-400`); they can select a profile name but cannot add or edit profiles.
**Fix:** Give profiles a first-class privilege declaration upstream and consume it, failing closed when it is absent.
- Add `privilege: Option<Privilege>` to `AccessProfile` (`core/src/agents/mod.rs:29-31`) and to `AccessProfileConfig` parsing (`:264-268`), declare it on all four builtin profile sets, and bump the `tmux-tools` rev pin at `Cargo.toml:28`. This is a coordinated two-repo change; the H6 precedent establishes that upstream edits are in scope.
- Delete the name table (`src/driver.rs:291-296`) and the shape-matching fallback (`:315-329`), resolving privilege solely from the declaration and treating a missing declaration as unranked so the existing `ok_or_else` bails at `:340`, `:356`, `:390`, `:414` fail closed.
- **Gate `:405-407` on the same declaration** — this is the load-bearing part of the change. Without it the finding is largely cosmetic, since that path grants the same escalation unconditionally.
- Tolerate `privilege: None` on operator-authored profiles as unranked rather than a parse error, so existing `agents.toml` files keep loading while losing only their ability to satisfy `Edit`/`Execute` gates.
- Correct the comment at `:311-314`, which currently describes rigor the resolution chain will no longer implement in the same terms.
- Add a regression with a deceptively named broad-argv profile (a `read-only` profile carrying wide args) asserting the launch is refused, and a companion asserting a mislabeled `workspace-write` profile is refused at `:405-407`. The existing positive test `registry_edit_accepts_narrow_ranked_default_fallback` (`:1971-1999`) exercises this exact path with honest argv and must be reworked onto the declaration.

---

### L20. L7's rewritten unlock test runs close to the default Vitest timeout (dismissed: not reproducible, and the stated mechanism is impossible. Measured in the main checkout under heavy load (load avg 10.8, concurrent agents): the test ran 508 / 588 / 675 / 534 ms across four consecutive full `npm test` runs — all green, 105/105, suite 3.9–4.2 s wall — and 313 ms in file isolation. Peak is 13% of the 5000 ms budget, not "close to the timeout". The premise "dominated by real-time async resolution" is factually wrong: `ui/src/lib/components/PasswordDialog.svelte` contains no `transition:`/`fade`/`fly` directive and no `setTimeout`, its `$effect` (`:24-33`) only calls `showModal()`/`close()` — both shimmed synchronously at `ui/src/test/setup.ts:5-28` — and `ui/src/lib/components/passwordPrompt.svelte.ts:8-48` is a pure deferred-promise resolver with no timers. Decisively, every await in the test is already bounded at 1000 ms by testing-library defaults (`findByLabelText` at `:144`, `waitFor` at `:149`), so a resolution failure fails at ~1 s with a testing-library error and can never reach Vitest's 5000 ms; the cited timeout would require ~5 s of raw CPU starvation, which is what the original audit most likely observed. The real cost is first-mount of the 1958-line `InspectorPanel.svelte` plus Svelte CSS injection (`css: true`, `ui/vite.config.ts:32`) — 313 ms for test 1 vs 6–51 ms for tests 2–10 mounting the same component. Suite headroom is not thin: next-slowest tests are 246 ms, 182 ms, 135 ms. The reviewer's fake-timers branch is inapplicable — there is no timer to fake and `vi.useFakeTimers()` would break `waitFor`, which needs real intervals. A per-test `{ timeout: 15000 }` cannot prevent the described symptom either, since the testing-library sub-bounds fire first. Note the test's standalone `render(InspectorPanel, …)` shape at `:101`/`:117-155` is load-bearing for M25's dismissal and was deliberately left untouched. Follow-up if CI contention ever produces a genuine flake: raise `testTimeout` globally in `ui/vite.config.ts:29-34` rather than singling out one test; the durable fix for mount cost is decomposing `InspectorPanel.svelte`.)

---

### M33. Legacy no-runAs runs are moved to a socket they never used (FIXED)
> **Fix:** Split new-run vs legacy-resume/reap tmux invocation; load_or_resolve_run_tmux_invocation and reaper fallback now reconstruct/persist TmuxInvocation::default() for NULL legacy no-runAs rows; updated resume/reaper tests.
**Severity:** MEDIUM — silent pane unreachability plus a reaper leak of surviving default-server sessions; same UID/trust domain (no privilege boundary) and reachable only for a pre-migration run resumed in a non-terminal state, so it trends toward LOW.
**Files:** `src/runtime.rs:3423` (no-runAs reconstruction arm), `:3389-3408` (`load_or_resolve_run_tmux_invocation`), `:6836-6838` (reaper fallback), `:1422` (new-run persist); `src/storage.rs:1208-1229` (`ensure_runs_tmux_invocation_columns`), `:1231-1243` (`decode_tmux_invocation`)
**Description:** Legacy rows predating the `tmux_bin/tmux_socket/tmux_prefix_json` columns migrate to NULL and decode to `tmux_invocation = None` (`src/storage.rs:1231-1243`); a no-`run_as` run of that era ran on the default tmux server (`socket: None`). On resume, `load_or_resolve_run_tmux_invocation` (`src/runtime.rs:3389-3408`) unconditionally mints a run-scoped socket at `:3423` (`silverbond-<run_id>`) and persists it at `:3405`, making the surviving default-server panes unreachable. The reaper fallback (`:6836-6838`) enumerates the same wrong socket, finds nothing, and drops the registrations without killing (`:6886-6902`) — leaking the old sessions. New runs are unaffected because `start_run` always persists `Some(invocation)` at `:1422`, so a NULL invocation reliably signals "legacy."
**Fix:** Split new-run creation from legacy-row resume/reap. Retain `run_scoped_tmux_invocation` only at new-run creation (`start_run`). In `load_or_resolve_run_tmux_invocation` and the reaper fallback, when a no-`run_as` row has a NULL persisted invocation, reconstruct and persist `TmuxInvocation::default()` (`{tmux_bin:"tmux", prefix:[], socket:None}`) — it round-trips cleanly through `decode_tmux_invocation` (non-empty `tmux_bin`, `"[]"` prefix) so the persisted value sticks with no re-reconstruction loop. Apply the same correction consistently across all funnel sites (resume, the `api.rs:231` attach path, and the reaper). Add upgrade tests that place a legacy no-runAs session on the default invocation and assert resume/reap target the default server rather than a run-scoped socket.

---

### M34. Output capture is unbounded after the deadline (FIXED)
> **Fix:** Bounded read_capture to a 64 KiB tail read with an elision marker (COMMAND_CAPTURE_TAIL_BYTES / COMMAND_CAPTURE_TRUNCATION_MARKER) in src/proc.rs; added command_timeout_capture_is_bounded_to_output_tail regression.
**Severity:** MEDIUM (lower band) — `run_as.command` is operator-arbitrary and the read materializes a process-wide `Vec<u8>`, so a large capture can OOM the whole server; but the read is finite (child is killed and reaped before the read runs), realistic output is KB, and it sits on an already-failed path. The original "output-volume hang" framing is overstated — there is no hang, only a memory spike.
**Files:** `src/proc.rs:131-147` (`read_capture`), `:132` (full-length read), `:81` (`wait_for_child` kills/reaps before read), `:82-83` (read calls); callers `src/tmux_exec.rs:137` (`resolve_tmux_bin_with_timeout`), `src/api.rs:1672` (`run_tmux_status_with_timeout`), `:1733` (`ensure_run_as_can_traverse_root`)
**Description:** `read_capture` (`src/proc.rs:131`) reads the entire capture tempfile into a `Vec<u8>` with no cap — `:132` takes `metadata()?.len()` and the loop at `:136-145` accumulates all bytes. On the timeout path the child had up to `DEFAULT_TMUX_COMMAND_TIMEOUT = 5s` to write to the disk-backed tempfile before being SIGKILLed and reaped by `wait_for_child` (`:81`, `:113-118`), so the file can already be hundreds of MB by the time `read_capture` runs at `:82-83` and pulls it all into memory. This contradicts L18's finalized fix text, which called for reading the file's tail (`:1107`); the shipped code reads the whole file. No caller truncates downstream (`parse_tmux_lookup_output` at `src/tmux_exec.rs:148` iterates `.lines()` over the fully-materialized buffer).
**Fix:** Bound `read_capture` to a tail read. Seek to `len.saturating_sub(CAP)` and read only the last `CAP` bytes into the buffer, prepending an explicit truncation marker when the file exceeded the cap so callers/diagnostics see that output was elided. Tail (not head) is deliberate — it preserves the terminal error line that matters most for stderr diagnostics and matches L18's "tail" requirement. Keep the pipe-free file topology H6/H8 mandate (no reader threads). Add a regression in which a timed-out child emits substantially more data than the cap while the call still returns within its time budget and the returned buffer is bounded with the truncation marker present.

---

### L21. Traversal-probe timeouts still discard stderr (FIXED)
> **Fix:** Added shared proc::bail_timeout_stderr and wired ensure_run_as_can_traverse_root and run_tmux_status_with_timeout through it so timeout errors carry trimmed stderr plus probe args; added traverse_root_probe_timeout regression.
**Severity:** LOW — the common failure (permission-denied exit) already surfaces stderr at `src/api.rs:1750`; only the rarer timeout branch is affected, and it still reports that it timed out. The dropped data (partial sudo/PAM/wrapper diagnostics) is provably available but hidden.
**Files:** `src/api.rs:1733-1743` (`ensure_run_as_can_traverse_root` timeout propagation), `:1750` (exit-code path, already correct), `:1678-1685` (`run_tmux_status_with_timeout` reference extraction); `src/proc.rs:13-16` (`TimedOutOutput`), `:20` (fixed `Display` string), `:82-83`/`:91` (captured bytes populated on timeout), `timeout_captured_output`; `src/tmux_exec.rs:137` (`resolve_tmux_bin_with_timeout`, third candidate caller)
**Description:** `ensure_run_as_can_traverse_root` (`src/api.rs:1733-1743`) calls `proc::command_output_with_timeout(...).with_context(...)?` and propagates the timeout error directly — it never calls `proc::timeout_captured_output`. On timeout the `TimedOutOutput` struct holds `stdout`/`stderr` (populated at `src/proc.rs:82-83`, `:91`) but its `Display` is the fixed string "command timed out with captured output" (`:20`), so the captured stderr is discarded. The non-zero-exit path already prints trimmed stderr (`:1750`); only the timeout bail is missing, contradicting L18's FIXED summary (`:1099`) which claimed the timeout bail was wired. Impact: on a hanging/slow sudo prompt or wrapper the diagnostic explaining why is silently dropped.
**Fix:** Hoist the timeout-stderr extract-trim-format logic into a shared helper in `proc.rs` (e.g. `bail_timeout_stderr(error, operation, args)`) — `proc.rs` already owns `timeout_captured_output`, making it the correct seam — and have both `ensure_run_as_can_traverse_root` and the reference `run_tmux_status_with_timeout` (`src/api.rs:1678-1685`) call it, so the two sites cannot drift; the third caller `resolve_tmux_bin_with_timeout` can adopt it later. The returned traversal error must include the trimmed stderr plus the attempted probe arguments. Do **not** add byte-bounding here — bounding is owned by M34's tail-read change to `read_capture` (`src/proc.rs:131`) with its truncation marker, so this fix must land after or with M34 and simply consume the already-bounded capture. Add a timeout-stderr regression asserting the traversal error carries the captured stderr and probe args.

---

### L22. Timed-out waiter can clear a replacement pane-stream owner (FIXED)
> **Fix:** Gated force_clear_stuck_terminating_owner on same_channel(sender) && is_terminating(); waiter captures entry.sender under the lock; added ABA regression asserting a replacement Terminating owner survives a stale waiter force-clear.
**Severity:** LOW — cheap hardening/consistency rather than a live defect. The false-clear only fires after a genuine 600s-stuck owner, and a replacement entry is born `Active` (never `Terminating`) and can only reach `Terminating` via a full pump cycle that cannot complete inside the sub-millisecond lock window, so the ABA is effectively unreachable in practice.
**Files:** `src/app.rs:321-335` (`force_clear_stuck_terminating_owner`, the only asymmetric mutator), `:294` (`unsubscribe` identity check), `:313` (`remove_terminal_sender` identity check), `:224-238` (`PaneStreamEntry::new`); `src/api.rs:2008-2026` (waiter), `:2010` (clones only `owner_done`), `:2023` (force-clear call), `:2049` (entry insert), `:2272`/`:2284` (`claim_pane_stream_task_exit`/`begin_termination`), `:68-73` (`pane_stream_owner_wait_timeout`, 600s)
**Description:** The waiter in `subscribe_pane_stream` (`src/api.rs:2008-2026`) clones only `owner_done` (`:2010`), drops the lock, and awaits `owner_done.cancelled()` under the 600s `pane_stream_owner_wait_timeout`. On timeout it calls `force_clear_stuck_terminating_owner(&key)` (`:2023`), which re-locks and removes+cancels whatever entry now sits at `key` if it is `Terminating` — without confirming it is the same owner it waited on (`src/app.rs:324-332`). This is the single registry mutator lacking the `same_channel` identity guard present at `:294`, `:313`, and `src/api.rs:2272`. In the (near-impossible) window where the original owner finishes and a replacement reaches `Terminating` before force-clear re-acquires the lock, the stale waiter cancels the replacement, permitting overlapping teardown/restart of the same pipe.
**Fix:** Add the missing owner-identity guard to force-clear, matching the established idiom. Capture `entry.sender.clone()` (a `broadcast::Sender<Vec<u8>>`, which supports `same_channel`) at the waiter's lock hold in `src/api.rs:2010`, thread it into `force_clear_stuck_terminating_owner(key, sender)`, and gate the remove+cancel on `entry.sender.same_channel(&sender) && entry.is_terminating()` so a replacement owner at the same key is left untouched. Add an ABA regression that plants a replacement `Terminating` entry at the key before the stale waiter force-clears and asserts the replacement survives.

---

### L23. Panic regression does not exercise the production supervisor (FIXED)
> **Fix:** Extracted the outer supervision body into supervise_pane_stream_owner helper called by both the production call site and the panic regression, so the real production supervision path is exercised under test.
**Severity:** LOW — a test-quality gap, not a live defect. The termination→cleanup window is currently panic-free (M23 itself was LOW), so the untested supervisor guards only latent fragility; but the coverage gap is total-zero, so the exact M23 regression could silently ship if the supervisor is deleted, detached, or mis-wired.
**Files:** `src/api.rs:5359` (test `pane_stream_owner_supervisor_clears_terminating_entry_on_panic`), `:5381-5398` (inline supervisor copy), `:5401`/`:5422` (assertions), `:2183-2259` (production supervisor `spawn_pane_stream_task`), `:2247-2257` (the copied force-clear body), `:2052` (sole production call site), `:2168` (definition)
**Description:** The regression at `src/api.rs:5359` plants a `Terminating` entry, then at `:5381-5398` `tokio::spawn`s a fresh task that hand-copies the production supervisor line-for-line — inner panic, `JoinError` guard, identical `tracing::warn!`, `remove_terminal_sender`, `owner_done.cancel()` — mirroring `:2247-2257`. Its assertions (`:5401`, `:5422`) observe only registry state produced by that copy; the production supervisor at `:2183-2259` is never invoked (the only references to `spawn_pane_stream_task` are the production call site `:2052` and the definition `:2168` — zero test callers). If the real supervisor were removed, detached (handle dropped, restoring the original M23 hang), or wired to the wrong sender/key, the test stays green.
**Fix:** Extract the outer supervision body (`:2183-2258`) into a `supervise_pane_stream_owner(owner_future, key, sender, owner_done, pane_streams)` helper that spawns the inner owner handle, awaits it, and force-clears the Terminating entry on `JoinError`, matching the existing small-helper idiom (e.g. `claim_pane_stream_task_exit`). `spawn_pane_stream_task` calls it with the real owner body; the regression calls it with a future that panics after `begin_termination`, so the same production supervision code runs under test. The owner future takes a `Send + 'static` bound / generic param. Rewrite the test to drive the helper and assert the Terminating entry is force-cleared and no waiter hangs. This also provides the production regression harness for L22's new `same_channel` identity guard.

---

### L24. Continuation warning fabricates the failure cause (FIXED)
> **Fix:** Continuation Err arms in validate_workflow (src/model.rs, both target and source side) now bind the resolver error and surface its Display verbatim instead of the hardcoded privilege-rank string; removed unused configured_access_profile_name; added unknown-agent/missing-profile/unranked-default regressions asserting warning matches resolver Display.
**Severity:** LOW — a misleading pre-run advisory only; runtime enforcement is unaffected and fail-closed. It does not rise (no unsafe pane adoption becomes reachable) and does not fall to trivial (it violates M22's explicit alignment mandate and can name the wrong access level, actively misdirecting diagnosis).
**Files:** `src/model.rs:2103-2113` (target-side `Err(_)` arm), `:2114-2124` (source-side twin), `:113-122` (`configured_access_profile_name` mapping); `src/driver.rs:352-421` (`resolved_access_profile` error variants), `:332-338` (`require_privilege`, the sole source of the hardcoded text); runtime refusal `?`-propagates the same resolver error verbatim (`src/tmux_exec.rs`)
**Description:** The editor-time continuation check at `src/model.rs:2103`/`:2114` matches `Err(_)` — discarding the resolver's actual error — and hardcodes a "has no declared privilege rank" message, though `resolved_access_profile` (`src/driver.rs:352-421`) also fails for registry-load failure, unknown agent, missing override profile, profile-would-widen, missing read-only profile, and default-would-widen; only `require_privilege` (`:332-338`) emits the hardcoded text. Compounding it, `configured_access_profile_name` (`:113-122`) maps `Edit|Execute → "workspace-write"`, so when resolution failed on the "default" fallback the warning names the wrong access level. The run is still correctly refused at runtime (fail-closed via `?`-propagation of the same error), so the defect is purely the pre-run diagnostic wording, which M22 required to align with the runtime refusal.
**Fix:** Bind the resolver error and surface its `Display` verbatim in both arms. Change the `Err(_)` matches to `Err(e)` and format the warning as e.g. `"\"{node.name}\" cannot verify session continuation: agent {current_agent} {e}; pane adoption will be refused at run time."`, dropping `configured_access_profile_name` from this message. This structurally guarantees alignment with the runtime refusal (the same string the runtime `?`-propagates), removes the misleading-profile-name bug at its source, and keeps a single source of truth so a newly added resolver error cannot silently drift out of the warning. Apply identically to the source-side twin at `:2114-2124`. Add regressions covering unknown-agent, missing-profile, and unranked-default failures asserting the warning text matches the resolver error.

---

### L25. Failed saves leave same-process temp files permanently unreapable (FIXED)
> **Fix:** Added WorkflowSaveTempGuard RAII guard in WorkflowStore::save whose Drop removes the per-attempt temp on error/cancellation, disarmed after rename succeeds; added failed-save regression, L16 same-PID preservation intact.
**Severity:** LOW — a real unbounded-over-uptime temp leak on a client-reachable cancellation path (`src/api.rs:540`), but no correctness, data-loss, or security impact and saves rarely fail. Does not rise (small files, error path) and does not fall to trivial (genuinely unbounded, not theoretical).
**Files:** `src/storage.rs:1011-1029` (`WorkflowStore::save`), `:1016-1021` (per-attempt temp name), `:890`/`:1015` (`WORKFLOW_SAVE_NONCE`), `:1023-1027` (create/write/sync/rename, all `?`-propagating), `:935-948` (L16 reaper), `:938` (same-PID preservation), `:950` (`list` filters `.json`); sole caller `src/api.rs:540` (`save_workflow` axum handler)
**Description:** Each `save` attempt writes a uniquely-named temp `{safe}.json.{pid}-{nonce}.tmp` (`src/storage.rs:1016-1021`) using the process-global `WORKFLOW_SAVE_NONCE` counter (`:890`, `:1015`), and every step — `File::create` (`:1023`), `write_all` (`:1024`), `sync_all` (`:1025`), `rename` (`:1027`) — `?`-propagates with no `remove_file` and no RAII guard. On any I/O error (disk full, `EXDEV`), or when the async future is dropped mid-flight on client disconnect (`save` is async, sole caller `src/api.rs:540`), the temp is left behind. L16's reaper (`:935-948`) then preserves it forever because it carries this process's PID (`:938`, `pid != std::process::id()`). Result: one orphaned `.tmp` per failed/cancelled save, unbounded over process lifetime, invisible to `list()` (which filters `.json`, `:950`) but consuming disk.
**Fix:** Add a cancellation-safe RAII guard local to `save`. Immediately after `File::create`, construct a `TempGuard { path }` whose `Drop` calls blocking `std::fs::remove_file(&self.path)` (ignoring `NotFound`); `disarm()` it exactly once (e.g. a flag or `mem::forget`) after `rename` succeeds. Because `Drop` runs on both error return and future-drop cancellation and a blocking unlink needs no await, this cleans up at the moment the failure happens rather than deferring to the next `list`. The guard touches only its own per-attempt temp, so L16's per-attempt isolation (and its concurrent-save race fix) is preserved. Add a regression that forces a post-create failure (e.g. save into a directory made read-only after create, or inject a write error) and asserts no `.tmp` remains, plus a companion assertion that a genuinely concurrent same-PID save is still preserved so L16 is not regressed.

---

### L26. Process-wide environment mutation in parallel in-process tests (FIXED)
> **Fix:** Driver XDG_CONFIG_HOME tests switched to Registry::load_with_user_path (dropping REGISTRY_ENV_LOCK and the env restore dance); added RuntimeContext tmux-bin/default-invocation override seam so the stale-reaper regression injects a fake tmux via DI instead of mutating process-global PATH.
**Severity:** LOW — real test-suite order-dependence and a panic-leak window, but no production path is affected and most sibling reaper tests already inject absolute-path scripts and are shielded.
**Files:** `src/driver.rs:1976` (`install_temp_agent_registry` sets `XDG_CONFIG_HOME`, restored `:1981-1989`), `:1966` (module-private `REGISTRY_ENV_LOCK`), `:156` (`agent_config_home` reads `XDG_CONFIG_HOME`); `src/runtime.rs:8700` (stale-reaper prepends fake `tmux` to `PATH`, restored `:8725-8733`), `:1675` (`check_cli` reads `PATH`)
**Description:** Two in-process `#[test]`-family tests mutate process-global environment without shared serialization. `install_temp_agent_registry` sets `XDG_CONFIG_HOME` (`src/driver.rs:1976`) under the driver-module-private `REGISTRY_ENV_LOCK` (`:1966`), which cannot be referenced from `runtime.rs`; the stale-reaper regression prepends a fake-`tmux` temp dir to `PATH` (`src/runtime.rs:8700`) under no lock at all. Cargo's default harness runs both on parallel threads in one binary, so a concurrent reader — `check_cli` reading `PATH` (`src/runtime.rs:1675`) or `agent_config_home` reading `XDG_CONFIG_HOME` (`src/driver.rs:156`) — can observe the temporary registry or fake binary, making the suite order-dependent; a panic between mutation and restore leaks the change permanently.
**Fix:** Remove the process-global mutation at its source via explicit dependency injection rather than serializing access to it. Switch the two `XDG_CONFIG_HOME` tests to `agents::Registry::load_with_user_path(Some(path))` — the seam already exists and is used by neighboring driver tests (`src/driver.rs:1949, 2056, 2092`) — so no env var is touched. For the `PATH` test, expose a small explicit tmux binary/search-path override seam on the reaper and inject the fake-`tmux` path through it instead of prepending to `PATH`. With both mutations gone, `REGISTRY_ENV_LOCK` and the manual restores become unnecessary for these tests and the cross-test-interference and panic-leak windows disappear.

---

## Smells — walked (2026-07-23)

User-pulled Divergent Change + Duplicated Code smells from the batch list, consolidated per module. Advisory; severity capped at MEDIUM on promote.

### S1. Divergent Change — `src/api.rs` (pane-stream transport crammed into the API module) (FINALIZED)
**Severity:** MEDIUM — advisory; navigation/blast-radius maintainability cost, not correctness. Capped at MEDIUM per in-diff smell-promotion precedent. Ledger keys `src/api.rs:1375` (count 2), `:1441-1610`, `:1601` all marked `fixed: S1`.
**Files:** `src/api.rs:1073-2459` (contiguous pane-stream transport block), `:1265-1363` (`pane_stream_socket` select loop), `:1371` (`resolve_run_pane_context`), `:1435`-`:1541` (`resolve_or_wait_for_pane_context` → `start_pane_stream`), `:1618`-`:1756` (`run_tmux_status_until`/`tmux_command`/`stop_pane_stream`), `:1766`-`:1989` (`PaneStreamFifoGuard`/`prepare_pane_stream`/`make_cross_user_fifo`/`PaneStream` `AsyncRead`), `:2317`-`:2427` (`PaneWsFrame`/`send_*`), `:1990` (`subscribe_pane_stream`), `:2168` (`spawn_pane_stream_task`); related seams `src/app.rs:184-330` (`PaneStreamRegistry`), `src/proc.rs` (H8 timeout-runner extraction)
**Description:** `src/api.rs` is 5,473 lines (~2,715 non-test) and co-locates Axum HTTP routing with three transport mechanisms that change for unrelated reasons: tmux protocol control, POSIX FIFO ownership (`:1796-1821`), and the pane-stream wire format (`:2317-2427`). The transport is a ~1,250-line contiguous block (`:1073-2459`) wedged between route handlers — `create_run`/`stream_run` end at `:974`, `run_events`/`approve_run` resume at `:2461` — so a change to any single mechanism forces a reader through the whole API surface. The pure-transport functions take only primitives (`root`, `pane_target`, `&TmuxInvocation`, `sender`, `bytes`) and never touch `AppState`; the AppState coupling lives only in the orchestration layer (`subscribe_pane_stream`, `resolve_run_pane_context`, `spawn_pane_stream_task`), which already leans on the extracted `PaneStreamRegistry` (`src/app.rs:184-330`). Impact is maintainability (navigation, blast-radius), not correctness — hence advisory.
**Fix:** Extract the AppState-free pane-stream transport into a dedicated `pane_stream` module behind a small start/stop/read(+frame) interface, tracked and executed as a deliberate refactor rather than folded into this branch.
- Move ~900 lines of mechanism — `start_pane_stream`, `stop_pane_stream`, `PaneStream`/`poll_read`, the FIFO helpers (`prepare_pane_stream`, `make_cross_user_fifo`, `PaneStreamFifoGuard`), the `send_*` wire framing (`PaneWsFrame`), and `run_tmux_status_*`/`tmux_command` — into a new `src/pane_stream` module that sits *above* `src/proc.rs`.
- Leave `subscribe_pane_stream`, `resolve_run_pane_context`, and `spawn_pane_stream_task` in `api.rs` as the module's only internal callers; expose the frame helpers as `pub(crate)` because `pane_stream_socket`'s `tokio::select!` loop (`:1265-1363`) interleaves `send_*` with registry receivers.
- Extends the seam H8 already established (`src/proc.rs`) and the registry already extracted to `src/app.rs:184`, rather than inventing a new pattern.

### S2. Divergent Change — `src/runtime.rs` (run engine) (FINALIZED)
**Severity:** MEDIUM — advisory; the maintainability cost is real coupling (not just length), concentrated in the split/collector state machine. Capped at MEDIUM per smell-promotion precedent. Ledger keys `src/runtime.rs:1-12785` (stale line count — file is 14,570 lines) and `:1352` both marked `fixed: S2`.
**Files:** `src/runtime.rs` (14,570 lines; ~7,443 non-test, tests `:7444-14570`); state machine `:5714` (`handle_split_node`), `:5960` (`release_collectors_if_ready`), `:6215` (`handle_terminal_cursor_status`), `:1969` (`evict_idle_split_families`), `:6585` (`finalize_run`); scheduler core `:2771` (`execute_workflow`), `:3110` (`process_immediate_cursors`); leaf concerns `:7236` (`build_log_id`), `:7029-7170` (template resolution), `:1652-1745` (agent/CLI discovery), `:6667-6979` (tmux reaping)
**Description:** One module owns ~15 concerns and is 14,570 lines (~7,443 non-test). The maintainability cost is not mere length but coupling: the split/collector state-machine functions each mutate the scheduler's shared run state *and* perform IO inline — `release_collectors_if_ready` (`:5960`) and `handle_terminal_cursor_status` (`:6215`) both take `&mut RuntimeCheckpoint` (mutating `active_cursors`/`split_families`/`collector_barriers`) *and* `ctx` (DB + `emit_event`), and are `async` because they interleave state edits with persistence. Any change to split/collector semantics forces reasoning across five sites — `handle_split_node:5714`, `handle_terminal_cursor_status:6215`, `release_collectors_if_ready:5960`, `evict_idle_split_families:1969`, `finalize_run:6585` — with no interface boundary. Advisory: maintainability, not correctness.
**Fix:** Extract a `SplitCollectorMachine` type that owns only `split_families` + `collector_barriers` and returns decisions the scheduler applies — a tracked refactor **gated behind H5/M9 landing** (those changed how split-family liveness is derived).
- The machine takes `&[CursorState]` read-only for liveness (required because H5, review `:109-125`, made family liveness a read-derivative of `active_cursors.iter().flat_map(|c| &c.split_family_ids)`), and returns a `SchedulerDecision` enum (`SpawnCursors` / `CancelSiblings` / `ReleaseBarrier{cursor}` / `ForceFail`).
- The scheduler retains all `active_cursors` mutation and every DB/event IO call, applying the returned decisions — so the machine becomes synchronously unit-testable without a DB.
- Sequence *after* H5/M9 land, not concurrently, to avoid churn on the same code; do **not** take the verbatim lines-relocation route (moving `:5714-6215` as-is leaves the `&mut RuntimeCheckpoint`+`ctx` coupling intact and fails the deletion test).
- Optional low-risk down payment now: extract the IO-free leaf concerns (`build_log_id:7236`, template resolution `:7029-7170`, agent/CLI discovery `:1652-1745`, tmux reaping `:6667-6979`) into submodules; these have narrow inputs and no shared `checkpoint` mutation.

### S3. Divergent Change — `ui/src/features/editor/InspectorPanel.svelte` (escalated: ISSUE-260723-0823-1)
**Ledger key:** `ui/src/features/editor/InspectorPanel.svelte:1`
One component (1,176 lines changed) hosts node config for every node kind plus agent capability probing, access-mode reconciliation, and unlock prompting. Proposed fix: extract a per-node-kind config panel and lift capability/unlock concerns to the shell.

### S4. Duplicated Code — `src/api.rs:5381` (owner-panic cleanup re-implemented in test) (FINALIZED)
**Severity:** MEDIUM — advisory; below rule-of-three (2 sites, one a test double), so promoted for the test-fidelity value of the seam rather than churn reduction. Ledger key marked `fixed: S4`.
**Files:** `src/api.rs:5358-5424` (test `pane_stream_owner_supervisor_clears_terminating_entry_on_panic`), `:5382-5384` (stubbed panicking handle), `:5385-5397` (copied cleanup trio); `:2185` (`spawn_pane_stream_task` hardcodes `start_pane_stream`), `:2247-2258` (production panic-recovery branch — the duplicated original)
**Description:** The panic-recovery regression at `src/api.rs:5381-5398` copies the production supervisor's owner-panic cleanup branch (`:2247-2258`) verbatim inline — the identical tracing message `"pane stream owner task exited before cleanup; force-clearing registry entry"`, the `is_panic()`/`is_cancelled()` fields, `remove_terminal_sender`, and `owner_done.cancel()` — because `spawn_pane_stream_task` hardcodes `start_pane_stream` (`:2185`) and exposes no seam to inject a panicking owner. This is test-fidelity coupling, not repeated production logic: if the production cleanup changes (different log, extra step, ordering), the test keeps passing against its stale copy and no longer verifies the real path. Only 2 sites, one a test double, so churn risk is low; the value is confidence, not line reduction.
**Fix:** Add a seam so the test drives the real supervisor instead of re-implementing it. Make the owner future injectable into `spawn_pane_stream_task` — or extract the supervisor loop into a testable `run_supervised_owner(owner_future, …)` that `spawn_pane_stream_task` calls with the real `start_pane_stream` future. The regression then injects a panicking future and asserts on the *production* cleanup path, deleting the copied branch at `:5385-5397`. This eliminates the duplication at its root (the test exercises production code, so no stale copy can drift) and passes the deletion test with ~1 parameter of real cost; keep the change scoped to the spawn seam and preserve the existing panic-path assertions.

### S5. Duplicated Code — `src/model.rs:110` (deferred)
**Ledger key:** `src/model.rs:110`
Duplicated Code smell logged 2026-07-22.

### S6. Duplicated Code — `ui/src/features/editor/InspectorPanel.svelte:789` (escalated: ISSUE-260723-0823-1)
**Ledger key:** `ui/src/features/editor/InspectorPanel.svelte:789`
_Folded into ISSUE-260723-0823-1 as a sub-task (shared `PaneConfigFields` for run_agent + spawn); ledger row stays open until that refactor lands._
Duplicated Code smell logged 2026-07-21.

### S7. Duplicated Code — `ui/src/lib/stores/workflowStore.svelte.ts:104-108` (deferred)
**Ledger key:** `ui/src/lib/stores/workflowStore.svelte.ts:104-108`
_Kept advisory: token-grammar duplication (3 `refsIn` loops at `:217-225`) is contained within `compoundBoundary`; `[regex, form][]` table saves ~6 lines and marginally fails the deletion test. Previously deferred as S10 (`20260716-003531.md:666`)._
Duplicated Code smell logged 2026-07-19.

### S8. Duplicated Code — `ui/src/lib/stores/workflowStore.svelte.ts:147` (dismissed: retired — duplicate key of `workflowStore.svelte.ts:104-108` (S7); it is the retarget/print-side of the same compound token grammar (`retargetNodeRefsInText` `:147-158`), which S7 already owns on the parse side. No independent duplication axis; a single `TokenGrammar` fix would collapse both, so S7 is the single tracked owner. Mirrors ledger precedent for co-located dup-keys (row 50 `:401`→`:110`, row 46 `api.rs:1544-1547`→`:1498`).)

---
