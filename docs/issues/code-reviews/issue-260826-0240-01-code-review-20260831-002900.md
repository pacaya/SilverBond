# Code review — ISSUE-260826-0240-01 (2026-08-31T00:29:00Z)

Scope: issue diff vs 37548091386af33b95e2bf6944a7c1664e09a74a · Reviewers: Claude + Codex (cross-verified, adjudicated inline)

Diff under review (excluding `docs/issues/` bookkeeping): `ARCHITECTURE.md` (+10 bullets in `### Node types today`), `docs/backend.md` (1 line — the `WorkflowNodeType` entry under `**Core enums:**`). No untracked files.

### L1. `NodeKind` is missing from the `**Core enums:**` list it belongs in (deferred)

**Severity:** LOW · **File:** `docs/backend.md:59` · **Source:** Claude

The corrected line 60 fixes `WorkflowNodeType`, but the `**Core enums:**` block under `### model.rs — Workflow Schema + Validation` omits `NodeKind` entirely — the serde-tagged enum (`tag = "type"`, `rename_all = "snake_case"`) that actually defines the wire shape, and to which `WorkflowNodeType` is merely bridged by a `From` conversion. A reader of that section is left believing `WorkflowNodeType` is the node-type model. Claude filed it at confidence 55 as a genuine doc-truth gap, and confidence 95 that it is out of scope for this record.

**Cross-reference:** Codex DISAGREE — "`docs/backend.md:59` labels selected 'Core enums' without claiming completeness, and the unchanged omission is explicitly outside this diff's 'nothing else changes' scope."

**Adjudication (orchestrator, split verdict — read the code directly).** Both halves hold, and they do not conflict. Codex is right on the defect framing: `**Core enums:**` is a selective label and asserts no completeness, so this is *not* the false-statement-about-the-present class that this issue exists to correct — unlike `ARCHITECTURE.md`'s "Current first-class node types:", which does assert completeness. Claude is right that `NodeKind` appears nowhere in `docs/backend.md` (`grep -n 'NodeKind' docs/backend.md` returns nothing), which is a real gap in a section that purports to describe the model module. It is pre-existing, sits on a line this diff does not touch, and the brief's Desired behavior states "Nothing else in either document changes" — fixing it here would be an out-of-scope edit. **Deferred**, not dismissed: the gap is real but belongs to a separate record in the PRD-260826-0009-01 docs-truth family, per Claude's own recommendation.

**Fix:** No change in this diff. File as a separate docs-truth issue; do not fold it in.

---

## Verified Clean

- **AC1 — `ARCHITECTURE.md` `### Node types today` bullet list** (both reviewers) — the list is now exactly the 14 wire tags `NodeKind` serializes (`task`, `approval`, `split`, `collector`, `decide`, `parallel_batch`, `subflow`, `call`, `spawn`, `send`, `wait`, `capture`, `kill`, `run_agent`), derived independently by each reviewer from the `NodeKind` declaration under `#[serde(tag = "type", rename_all = "snake_case")]`. No variant-level `#[serde(rename)]` exists, so mechanical snake_case is correct (`ParallelBatch`→`parallel_batch`, `RunAgent`→`run_agent`). No extras, no duplicates. Both section-bounded observables from the brief re-run green.
- **AC2 — `docs/backend.md` `WorkflowNodeType` Core-enums entry** (both reviewers) — names all 14 variants of that enum in declaration order and remains a single physical line (167 chars, no wrap). Both line-anchored observables green. Keeps the list's existing `— \`A\`, \`B\`` spelling convention.
- **AC3 — the two lists agree kind for kind** (both reviewers) — both enums re-derived from the working tree at review time; both are 14 variants in identical order. Claude cross-checked three independent ways: `WorkflowNodeType::as_str()`, the exhaustive `From<&NodeKind> for WorkflowNodeType` bridge, and `supportedNodeTypes` in the capabilities endpoint — the same 14 tags in the same order.
- **AC4 — preservation, tests pass unchanged** (Claude, unrestricted environment) — `cargo test --locked` 473 passed / 0 failed; `npm test` 129 passed. No test reads either edited file: the only markdown-guarding test, `tests/docs_catalog.rs`, is pinned to `docs/workflow-schema.md`. `scripts/check-canonical-v4-docs.sh` exits 0. (Codex could not run the suites green in its read-only sandbox — 7 Rust tests hit `EPERM` on socket/tmux ops and `npm test` could not write under `node_modules`; filtered reruns passed 443/443 library tests and 128/129 UI tests, the one timeout passing in isolation. Environment-inconclusive there, not a defect signal; the orchestrator's own full-suite run is the authority.)
- **Out of scope — `### Edge outcomes today`** (both reviewers) — re-confirmed still complete and correct against `WorkflowEdgeOutcome` (`success`, `reject`, `branch`, `loop_continue`, `loop_exit`), matching `supportedEdgeOutcomes`. Left untouched. No stop-and-report condition triggered.
- **Out of scope — the `join`-nodes-are-planned sentence** (both reviewers) — preserved verbatim, sits after the bullet list, not inside it. Neither enum declares a join variant. The `spawn` observable is satisfied by the bullet, not by this sentence — the gate's round-2 necessary-not-sufficient note does not fire.
- **Out of scope — the tmux dispatch-table description** (both reviewers) — `docs/backend.md:105` untouched; only line 60 changed in that file.
- **Out of scope — no sync mechanism, no Rust source, canonical-version statement** (both reviewers) — the whole-tree diff outside `docs/issues/` is the two documents only. No generator, no marker blocks, no version strings touched.
- **Security / performance / DI / layering / test quality** (both reviewers) — not applicable: prose-only markdown diff, no executable content, no data flow, no new dependencies, no test files changed. `git diff --check` exits 0.

## Smells (advisory)

- `ARCHITECTURE.md:293-306` — Duplicated Code (borderline): the same 14-member enumeration is now hand-maintained in four places (the Rust string projection, the capabilities endpoint, and both documents); this diff widens two of them from 4 to 14 entries, increasing the drift surface the parent PRD names as its own failure mode. · fix: none here — the record's Out of scope explicitly defers the sync mechanism, and `ISSUE-260826-0520-01` already owns removing the hand-written projections. Noted for that record's blast radius.
- `ARCHITECTURE.md:291` — Mysterious Name (borderline): now that the list is the complete variant set, the "first-class" qualifier in "Current first-class node types:" partitions nothing and no second-class category exists in the document; it reads as a leftover from when the list was a subset. · fix: drop "first-class" or define the distinction — out of scope here per "Nothing else in either document changes"; carry to whoever next edits the section.

Neither smell was promoted under the in-diff duplication rule: the Duplication smell's other cited sites are pre-existing code outside `ISSUE_DIFF`, so it stays advisory.

## Outcome

**ACCEPTED** — zero unfixed findings after adjudication. One LOW finding, deferred to a separate record; zero fix rounds used (cap 4). Codex reported zero findings and zero smells; Claude's sole finding was self-scoped as out of bounds and Codex disagreed with its defect framing. No graduation rows emitted for the diff's files.
